# Lumina v0.2 (RAW и форматы) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить в работающий viewer (v0.1) поддержку RAW (rawler) и HEIC (libheif-rs) с двухстадийным декодом: мгновенный embedded-preview → базовый full-develop, с бесшовной подменой на экране.

**Architecture:** Сохраняем конвейер v0.1 (состояние в главном потоке, декод на rayon, готовый RGBA через `EventLoopProxy` с монотонным `generation`). Расширяем `trait Decoder` двумя стадиями (`decode_preview`/`decode_full`), добавляем роутер по расширению и два новых декодера. Одна rayon-задача на файл эмитит Preview затем Full; `ViewTransform` пересчитывает масштаб при подмене, сохраняя экранный размер.

**Tech Stack:** Rust 2021, rawler 0.7 (pure Rust RAW), libheif-rs 2.7 (FFI к libheif), image 0.25 (уже есть), winit 0.30, wgpu 22, rayon, glam.

**Дизайн:** `docs/superpowers/specs/2026-06-15-lumina-v0.2-design.md`

---

## Структура файлов

| Файл | Что делаем | Ответственность |
|---|---|---|
| `Cargo.toml` | Modify | + rawler, libheif-rs |
| `ROADMAP.md` | Modify | окружение (vcpkg/libheif/CMake/LLVM), статус v0.2 |
| `src/decoder/mod.rs` | Modify | trait с 2 стадиями, `DecodedImage`, роутер `supported`/`decoder_for` |
| `src/decoder/standard.rs` | Modify | миграция на `decode_preview`/`decode_full` |
| `src/decoder/raw.rs` | Create | `RawDecoder` (rawler) |
| `src/decoder/heic.rs` | Create | `HeicDecoder` (libheif-rs) |
| `src/catalog/mod.rs` | Modify | фильтр по `decoder::supported` |
| `src/view.rs` | Modify | `rescale_for_new_image` |
| `src/app.rs` | Modify | `Stage`, `UserEvent.stage`, двухстадийный rayon, подмена вида |
| `src/error.rs` | Modify | варианты ошибок RAW/HEIC |

Порядок: окружение/линковка (риск) → расширение trait с зелёным билдом на каждом шаге → роутер → декодеры → логика вида → двухстадийная связка → приёмка.

**Замечание о фикстурах:** синтезировать RAW/HEIC трудно. Юнит-тесты декода RAW/HEIC помечаем `#[ignore]` и они требуют реальных образцов `tests/fixtures/sample.raf` и `tests/fixtures/sample.heic` (положить вручную; у пользователя есть Fuji RAF). Чистая логика (роутер, вид, стадии) покрыта обычными тестами без фикстур. Ручная приёмка — на реальных RAW/HEIC.

---

## Task 1: Зависимости и окружение (libheif-линковка — главный риск)

**Files:**
- Modify: `Cargo.toml`
- Modify: `ROADMAP.md`
- Create: `src/bin/smoke_libheif.rs`

- [ ] **Step 1: Добавить зависимости в Cargo.toml**

В секцию `[dependencies]` добавить:
```toml
rawler     = "0.7"     # pure-Rust RAW decoder (LGPL-2.1)
libheif-rs = "2.7"     # HEIC/HEIF через libheif (FFI)
```

- [ ] **Step 2: Установить нативное окружение (выполняет пользователь, разово)**

libheif-rs тянет `libheif-sys`, которому нужны системный libheif, CMake и libclang (bindgen).
На Windows (MSVC) рекомендуемый путь — vcpkg:

```powershell
# 1. vcpkg (если ещё нет)
git clone https://github.com/microsoft/vcpkg "$HOME\vcpkg"
& "$HOME\vcpkg\bootstrap-vcpkg.bat"
# 2. libheif (тянет libde265/x265)
& "$HOME\vcpkg\vcpkg.exe" install libheif:x64-windows
# 3. переменные окружения (в текущей сессии Claude Code — через `! setx` или вручную)
#    VCPKG_ROOT          → путь к vcpkg
#    LIBCLANG_PATH       → каталог libclang.dll (из LLVM, напр. C:\Program Files\LLVM\bin)
#    PATH += vcpkg\installed\x64-windows\bin (рантайм-DLL libheif/de265)
```

LLVM/Clang ставится с https://releases.llvm.org/ или `winget install LLVM.LLVM`.
CMake — `winget install Kitware.CMake`.

> Если линковка не пойдёт через vcpkg, второй вариант — фича сборки из исходников у
> `libheif-sys` (см. её README на docs.rs/crates.io); фиксируем рабочий вариант в этом шаге.

- [ ] **Step 3: Smoke-бинарь линковки libheif**

Создать `src/bin/smoke_libheif.rs`:
```rust
// Проверка, что libheif линкуется и инициализируется.
// Запуск: cargo run --bin smoke_libheif
fn main() {
    let lib = libheif_rs::LibHeif::new();
    let (major, minor, patch) = lib.version();
    println!("libheif слинкован: версия {major}.{minor}.{patch}");
}
```

> `LibHeif::version()` возвращает кортеж версий рантайма libheif. Если метод называется иначе
> в установленной версии — заменить на доступный (главное, чтобы `LibHeif::new()` слинковался).

- [ ] **Step 4: Проверить сборку и линковку**

Run: `cargo run --bin smoke_libheif 2>&1 | tail -20`
Expected: печатает «libheif слинкован: версия X.Y.Z». Если падает на линковке/bindgen —
чинить окружение (Step 2), это и есть цель задачи: убедиться в работоспособности до интеграции.

Run: `cargo build 2>&1 | tail -5`
Expected: компилируется (rawler — pure Rust, скачается без проблем).

- [ ] **Step 5: Записать окружение в ROADMAP.md**

В разделе «Установленное окружение» отметить как 🟢 после успешной установки:
```markdown
- 🟢 LLVM/Clang (`libclang`) — bindgen для libheif-rs
- 🟢 vcpkg + libheif (libde265/x265) + CMake — HEIC
```

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock src/bin/smoke_libheif.rs ROADMAP.md
git commit -m "build: зависимости rawler + libheif-rs, smoke-линковка libheif"
```

---

## Task 2: Расширение trait Decoder двумя стадиями

**Files:**
- Modify: `src/decoder/mod.rs`
- Modify: `src/decoder/standard.rs`
- Modify: `src/app.rs` (единственный вызов декода)

- [ ] **Step 1: Обновить тесты декодера (red)**

В `src/decoder/mod.rs`, в блоке `#[cfg(test)] mod tests`, добавить тест на стадии и
заменить старый прямой вызов `decode`:
```rust
    #[test]
    fn standard_has_no_preview() {
        let d = StandardDecoder;
        // у обычного формата нет отдельного превью — одна стадия
        assert!(d.decode_preview(Path::new("x.jpg")).unwrap().is_none());
    }

    #[test]
    fn decodes_png_via_full() {
        let d = StandardDecoder;
        let img = d.decode_full(&fixture("red_2x3.png")).unwrap();
        assert_eq!(img.width, 2);
        assert_eq!(img.height, 3);
        assert_eq!(img.rgba.len(), 2 * 3 * 4);
        assert_eq!(&img.rgba[0..4], &[255, 0, 0, 255]);
    }
```
Удалить прежний тест `decodes_png_to_rgba` (он звал `decode`, которого больше не будет).

- [ ] **Step 2: Запустить — не компилируется**

Run: `cargo test decoder 2>&1 | head -20`
Expected: ошибки «no method `decode_preview`/`decode_full`».

- [ ] **Step 3: Расширить trait в mod.rs**

В `src/decoder/mod.rs` заменить определение trait:
```rust
pub trait Decoder {
    /// Поддерживает ли расширение (в нижнем регистре, без точки).
    fn supports(ext: &str) -> bool
    where
        Self: Sized;

    /// Быстрое встроенное превью. `None` — у формата превью нет (одна стадия).
    fn decode_preview(&self, path: &Path) -> Result<Option<DecodedImage>>;

    /// Полное декодирование (для RAW — базовый develop).
    fn decode_full(&self, path: &Path) -> Result<DecodedImage>;
}
```

- [ ] **Step 4: Мигрировать StandardDecoder**

В `src/decoder/standard.rs` заменить `impl Decoder for StandardDecoder`:
```rust
impl Decoder for StandardDecoder {
    fn supports(ext: &str) -> bool {
        EXTS.contains(&ext)
    }

    fn decode_preview(&self, _path: &Path) -> Result<Option<DecodedImage>> {
        Ok(None)
    }

    fn decode_full(&self, path: &Path) -> Result<DecodedImage> {
        let img = image::open(path)
            .map_err(|e| LuminaError::Decode(path.to_path_buf(), e))?;
        let rgba = img.to_rgba8();
        let (width, height) = rgba.dimensions();
        Ok(DecodedImage { rgba: rgba.into_raw(), width, height })
    }
}
```

- [ ] **Step 5: Поправить вызов в app.rs**

В `src/app.rs`, в `load_current`, заменить вызов декода (пока одностадийно):
```rust
        rayon::spawn(move || {
            let result = StandardDecoder
                .decode_full(&path)
                .map_err(|e| e.to_string());
            let _ = proxy.send_event(UserEvent::Decoded { generation, result });
        });
```

- [ ] **Step 6: Запустить тесты и сборку**

Run: `cargo test decoder 2>&1 | tail -10`
Expected: `test result: ok. 3 passed` (supports + standard_has_no_preview + decodes_png_via_full).

Run: `cargo build 2>&1 | tail -5`
Expected: компилируется.

- [ ] **Step 7: Commit**

```bash
git add src/decoder/mod.rs src/decoder/standard.rs src/app.rs
git commit -m "refactor: trait Decoder с двумя стадиями decode_preview/decode_full"
```

---

## Task 3: Роутер декодеров и фильтр каталога

**Files:**
- Modify: `src/decoder/mod.rs`
- Modify: `src/catalog/mod.rs`

- [ ] **Step 1: Тесты роутера (red)**

В `src/decoder/mod.rs` в `#[cfg(test)] mod tests` добавить:
```rust
    #[test]
    fn router_supported_covers_all_families() {
        assert!(supported("jpg"));   // standard
        assert!(supported("raf"));   // raw (fuji)
        assert!(supported("nef"));   // raw (nikon)
        assert!(supported("heic"));  // heic
        assert!(!supported("txt"));
    }

    #[test]
    fn router_picks_decoder_or_none() {
        assert!(decoder_for("jpg").is_some());
        assert!(decoder_for("raf").is_some());
        assert!(decoder_for("heic").is_some());
        assert!(decoder_for("txt").is_none());
    }
```

- [ ] **Step 2: Запустить — не компилируется**

Run: `cargo test decoder 2>&1 | head -20`
Expected: ошибки «cannot find function `supported`/`decoder_for`».

- [ ] **Step 3: Реализовать роутер**

В `src/decoder/mod.rs` добавить после `pub use` объявлений новые декодеры и функции роутера:
```rust
mod heic;
mod raw;
pub use heic::HeicDecoder;
pub use raw::RawDecoder;

/// Поддерживается ли расширение хоть одним декодером.
pub fn supported(ext: &str) -> bool {
    StandardDecoder::supports(ext) || RawDecoder::supports(ext) || HeicDecoder::supports(ext)
}

/// Подобрать декодер по расширению. Приоритет: Raw → Heic → Standard.
pub fn decoder_for(ext: &str) -> Option<Box<dyn Decoder + Send>> {
    if RawDecoder::supports(ext) {
        Some(Box::new(RawDecoder))
    } else if HeicDecoder::supports(ext) {
        Some(Box::new(HeicDecoder))
    } else if StandardDecoder::supports(ext) {
        Some(Box::new(StandardDecoder))
    } else {
        None
    }
}
```

> `RawDecoder`/`HeicDecoder` ещё не существуют — Task 4 и 5 их создадут. Сборка/тесты этой
> задачи пройдут только после Task 4–5. Поэтому Step 4 ниже выполняем после них (или создаём
> декодеры в этой же сессии). Это намеренная связка: роутер бессмыслен без декодеров.

- [ ] **Step 4: Переключить каталог на роутер**

В `src/catalog/mod.rs`:
- заменить импорт `use crate::decoder::{ext_lower, Decoder, StandardDecoder};`
  на `use crate::decoder::{ext_lower, supported};`
- в `open` заменить фильтр
  `.filter(|p| p.is_file() && StandardDecoder::supports(&ext_lower(p)))`
  на `.filter(|p| p.is_file() && supported(&ext_lower(p)))`
- в `from_files_for_test` заменить
  `.filter(|p| StandardDecoder::supports(&ext_lower(p)))`
  на `.filter(|p| supported(&ext_lower(p)))`

(Сборка каталога зависит от Task 4–5; тесты каталога v0.1 продолжат проходить — `jpg`/`png`
поддержаны и роутером.)

- [ ] **Step 5: (после Task 4–5) Запустить тесты роутера и каталога**

Run: `cargo test decoder 2>&1 | tail -10`
Expected: `router_supported_covers_all_families` и `router_picks_decoder_or_none` зелёные.

Run: `cargo test catalog 2>&1 | tail -10`
Expected: 5 тестов v0.1 проходят.

- [ ] **Step 6: Commit (после Task 4–5)**

```bash
git add src/decoder/mod.rs src/catalog/mod.rs
git commit -m "feat: роутер decoder_for/supported + фильтр каталога по всем форматам"
```

---

## Task 4: HeicDecoder (libheif-rs)

**Files:**
- Create: `src/decoder/heic.rs`

- [ ] **Step 1: Реализовать HeicDecoder**

Создать `src/decoder/heic.rs`:
```rust
use super::{Decoder, DecodedImage};
use crate::error::{LuminaError, Result};
use libheif_rs::{ColorSpace, HeifContext, LibHeif, RgbChroma};
use std::path::Path;

const EXTS: &[&str] = &["heic", "heif"];

pub struct HeicDecoder;

impl Decoder for HeicDecoder {
    fn supports(ext: &str) -> bool {
        EXTS.contains(&ext)
    }

    fn decode_preview(&self, _path: &Path) -> Result<Option<DecodedImage>> {
        Ok(None) // одна стадия: полный декод HEIC и так быстрый
    }

    fn decode_full(&self, path: &Path) -> Result<DecodedImage> {
        let path_str = path
            .to_str()
            .ok_or_else(|| LuminaError::Heic(path.to_path_buf(), "не-UTF8 путь".into()))?;
        let lib = LibHeif::new();
        let ctx = HeifContext::read_from_file(path_str)
            .map_err(|e| LuminaError::Heic(path.to_path_buf(), e.to_string()))?;
        let handle = ctx
            .primary_image_handle()
            .map_err(|e| LuminaError::Heic(path.to_path_buf(), e.to_string()))?;
        let image = lib
            .decode(&handle, ColorSpace::Rgb(RgbChroma::Rgba), None)
            .map_err(|e| LuminaError::Heic(path.to_path_buf(), e.to_string()))?;

        let width = image.width();
        let height = image.height();
        let planes = image.planes();
        let iv = planes
            .interleaved
            .ok_or_else(|| LuminaError::Heic(path.to_path_buf(), "нет interleaved-плоскости".into()))?;

        // Скопировать с учётом stride в плотный RGBA8.
        let w = width as usize;
        let h = height as usize;
        let stride = iv.stride;
        let src = iv.data;
        let mut rgba = Vec::with_capacity(w * h * 4);
        for y in 0..h {
            let start = y * stride;
            rgba.extend_from_slice(&src[start..start + w * 4]);
        }

        Ok(DecodedImage { rgba, width, height })
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn supports_heic_ext() {
        assert!(HeicDecoder::supports("heic"));
        assert!(HeicDecoder::supports("heif"));
        assert!(!HeicDecoder::supports("jpg"));
    }

    // Требует реальный образец tests/fixtures/sample.heic — запускать вручную:
    // cargo test --decoder heic_decodes -- --ignored
    #[test]
    #[ignore]
    fn heic_decodes_sample() {
        let d = HeicDecoder;
        let path = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"))
            .join("tests/fixtures/sample.heic");
        let img = d.decode_full(&path).unwrap();
        assert!(img.width > 0 && img.height > 0);
        assert_eq!(img.rgba.len(), (img.width * img.height * 4) as usize);
    }
}
```

> Если в установленной версии libheif-rs поле плоскости называется иначе (`planes.interleaved`)
> или `decode` имеет другую сигнатуру опций — свериться с `cargo doc -p libheif-rs --open`
> и поправить; smoke-бинарь из Task 1 уже подтвердил линковку.

- [ ] **Step 2: Добавить вариант ошибки Heic**

В `src/error.rs` в enum `LuminaError`:
```rust
    #[error("ошибка HEIC {0}: {1}")]
    Heic(std::path::PathBuf, String),
```

- [ ] **Step 3: Сборка и unit-тест расширения**

Run: `cargo build 2>&1 | tail -5`
Expected: компилируется.

Run: `cargo test heic 2>&1 | tail -10`
Expected: `supports_heic_ext` зелёный; `heic_decodes_sample` — ignored.

- [ ] **Step 4: (опц.) Проверить декод на реальном HEIC**

Положить образец в `tests/fixtures/sample.heic`, затем:
Run: `cargo test heic_decodes_sample -- --ignored 2>&1 | tail -10`
Expected: PASS (размеры > 0, длина RGBA сходится).

- [ ] **Step 5: Commit**

```bash
git add src/decoder/heic.rs src/error.rs
git commit -m "feat: HeicDecoder через libheif-rs (RGBA, учёт stride)"
```

---

## Task 5: RawDecoder (rawler)

**Files:**
- Create: `src/decoder/raw.rs`

- [ ] **Step 1: Реализовать RawDecoder**

Создать `src/decoder/raw.rs`:
```rust
use super::{Decoder, DecodedImage};
use crate::error::{LuminaError, Result};
use rawler::get_decoder;
use rawler::imgop::develop::RawDevelop;
use rawler::rawsource::RawSource;
use std::path::Path;

const EXTS: &[&str] = &[
    "raf", "nef", "nrw", "arw", "srf", "cr2", "cr3", "rw2", "orf", "pef", "dng", "rwl", "iiq",
];

pub struct RawDecoder;

impl Decoder for RawDecoder {
    fn supports(ext: &str) -> bool {
        EXTS.contains(&ext)
    }

    fn decode_preview(&self, path: &Path) -> Result<Option<DecodedImage>> {
        let source = RawSource::new_file(path)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let decoder = get_decoder(&source)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let preview = decoder
            .preview_image(&source, &Default::default())
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        Ok(preview.map(|dynimg| {
            let rgba = dynimg.to_rgba8();
            let (width, height) = (rgba.width(), rgba.height());
            DecodedImage { rgba: rgba.into_raw(), width, height }
        }))
    }

    fn decode_full(&self, path: &Path) -> Result<DecodedImage> {
        let source = RawSource::new_file(path)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let decoder = get_decoder(&source)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let raw = decoder
            .raw_image(&source, &Default::default(), false)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let intermediate = RawDevelop::default()
            .develop_intermediate(&raw)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let dynimg = intermediate
            .to_dynamic_image()
            .ok_or_else(|| LuminaError::Raw(path.to_path_buf(), "develop вернул None".into()))?;
        let rgba = dynimg.to_rgba8();
        let (width, height) = (rgba.width(), rgba.height());
        Ok(DecodedImage { rgba: rgba.into_raw(), width, height })
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn supports_raw_exts() {
        assert!(RawDecoder::supports("raf"));
        assert!(RawDecoder::supports("nef"));
        assert!(RawDecoder::supports("cr2"));
        assert!(!RawDecoder::supports("jpg"));
        assert!(!RawDecoder::supports("heic"));
    }

    // Требует реальный образец tests/fixtures/sample.raf — запускать вручную.
    #[test]
    #[ignore]
    fn raw_full_develops_sample() {
        let d = RawDecoder;
        let path = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"))
            .join("tests/fixtures/sample.raf");
        let img = d.decode_full(&path).unwrap();
        assert!(img.width > 0 && img.height > 0);
        assert_eq!(img.rgba.len(), (img.width * img.height * 4) as usize);
    }

    #[test]
    #[ignore]
    fn raw_preview_sample() {
        let d = RawDecoder;
        let path = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"))
            .join("tests/fixtures/sample.raf");
        // у большинства RAF есть embedded preview
        assert!(d.decode_preview(&path).unwrap().is_some());
    }
}
```

> **Важно про типы `image`:** `dynimg` — это `image::DynamicImage` из той версии крейта
> `image`, что использует rawler. Мы зовём только `.to_rgba8()` → `.into_raw()` (Vec<u8>) и
> `.width()/.height()` (u32) — примитивы, совместимые между версиями `image` (0.20+), поэтому
> возможное расхождение версии `image` у rawler и у нашего проекта не мешает.

- [ ] **Step 2: Добавить вариант ошибки Raw**

В `src/error.rs` в enum `LuminaError`:
```rust
    #[error("ошибка RAW {0}: {1}")]
    Raw(std::path::PathBuf, String),
```

- [ ] **Step 3: Сборка и unit-тест**

Run: `cargo build 2>&1 | tail -10`
Expected: компилируется. Если rawler-импорты (`rawler::rawsource::RawSource`,
`rawler::get_decoder`, `rawler::imgop::develop::RawDevelop`) не резолвятся — свериться с
`cargo doc -p rawler --open` и поправить пути (rawler документирован слабо; API подтверждён
для 0.7.x: `RawSource::new_file`, `get_decoder`, `Decoder::{raw_image,preview_image}`,
`RawDevelop::develop_intermediate`, `Intermediate::to_dynamic_image`).

Run: `cargo test raw 2>&1 | tail -10`
Expected: `supports_raw_exts` зелёный; develop/preview — ignored.

- [ ] **Step 4: Проверить на реальном RAF**

Положить `tests/fixtures/sample.raf`, затем:
Run: `cargo test raw_full_develops_sample raw_preview_sample -- --ignored 2>&1 | tail -15`
Expected: оба PASS.

- [ ] **Step 5: Commit**

```bash
git add src/decoder/raw.rs src/error.rs
git commit -m "feat: RawDecoder через rawler (embedded preview + базовый develop)"
```

После этой задачи вернуться к **Task 3 Step 4–6** (сборка/тесты роутера+каталога, commit).

---

## Task 6: ViewTransform.rescale_for_new_image (TDD)

**Files:**
- Modify: `src/view.rs`

- [ ] **Step 1: Тесты (red)**

В `src/view.rs` в `#[cfg(test)] mod tests` добавить:
```rust
    #[test]
    fn rescale_preserves_screen_size_in_fit() {
        let win = Vec2::new(1000.0, 800.0);
        let old_img = Vec2::new(1000.0, 667.0);   // preview
        let new_img = Vec2::new(6000.0, 4002.0);  // full, то же соотношение
        let mut v = ViewTransform::new();
        v.set_min_zoom(fit_zoom(win, old_img));
        v.set_zoom_immediate(fit_zoom(win, old_img));
        v.set_fit(true);
        let screen_before = old_img * v.zoom();
        v.rescale_for_new_image(win, old_img, new_img);
        let screen_after = new_img * v.zoom();
        assert!((screen_before - screen_after).length() < 1.0);
        assert!((v.zoom() - fit_zoom(win, new_img)).abs() < 1e-4);
    }

    #[test]
    fn rescale_preserves_screen_size_when_zoomed() {
        let win = Vec2::new(1000.0, 800.0);
        let old_img = Vec2::new(1000.0, 667.0);
        let new_img = Vec2::new(2000.0, 1334.0);
        let mut v = ViewTransform::new();
        v.set_min_zoom(fit_zoom(win, old_img));
        v.set_zoom_immediate(2.0); // ручной зум сверх fit
        let screen_before = old_img * v.zoom();
        v.rescale_for_new_image(win, old_img, new_img);
        let screen_after = new_img * v.zoom();
        assert!((screen_before - screen_after).length() < 1.0);
    }
```

- [ ] **Step 2: Запустить — не компилируется**

Run: `cargo test view 2>&1 | head -20`
Expected: ошибка «no method `rescale_for_new_image`».

- [ ] **Step 3: Реализовать метод**

В `src/view.rs` в `impl ViewTransform`, после `clamp_pan`, добавить:
```rust
    /// Подстроить вид под смену размеров изображения (preview→full),
    /// сохранив экранный размер. old_img/new_img имеют одинаковое соотношение сторон,
    /// поэтому коэффициент масштаба не зависит от выбранной оси.
    pub fn rescale_for_new_image(&mut self, win: Vec2, old_img: Vec2, new_img: Vec2) {
        if old_img.y > 0.0 && new_img.y > 0.0 {
            let ratio = old_img.y / new_img.y;
            self.zoom = (self.zoom * ratio).clamp(ZOOM_MIN, ZOOM_MAX);
            self.zoom_target = self.zoom;
        }
        self.set_min_zoom(fit_zoom(win, new_img)); // подтянет zoom до min при необходимости
        self.clamp_pan(win, new_img);
    }
```

- [ ] **Step 4: Запустить — проходят**

Run: `cargo test view 2>&1 | tail -10`
Expected: все тесты `view` зелёные (8 из v0.1 + 2 новых).

- [ ] **Step 5: Commit**

```bash
git add src/view.rs
git commit -m "feat: ViewTransform.rescale_for_new_image для бесшовной подмены preview→full"
```

---

## Task 7: Двухстадийная диспетчеризация в App

**Files:**
- Modify: `src/app.rs`

- [ ] **Step 1: Добавить Stage и поле в UserEvent**

В `src/app.rs` заменить определение `UserEvent`:
```rust
/// Стадия декодирования.
#[derive(Clone, Copy, PartialEq)]
pub enum Stage {
    Preview,
    Full,
}

/// События, приходящие в event loop извне (из rayon).
pub enum UserEvent {
    Decoded {
        generation: u64,
        stage: Stage,
        result: std::result::Result<DecodedImage, String>,
    },
}
```

- [ ] **Step 2: Поле отслеживания инициализации вида в AppState**

В `struct AppState` добавить поле:
```rust
    pub inited_generation: Option<u64>,
```
В `AppState::new()` инициализировать: `inited_generation: None,`.

- [ ] **Step 3: Двухстадийный rayon в load_current**

В `src/app.rs` заменить тело `rayon::spawn(...)` в `load_current`:
```rust
        let ext = crate::decoder::ext_lower(&path);
        rayon::spawn(move || {
            let Some(decoder) = crate::decoder::decoder_for(&ext) else {
                // нет декодера → шлём ошибку как стадию Full
                let _ = proxy.send_event(UserEvent::Decoded {
                    generation,
                    stage: Stage::Full,
                    result: Err(format!("нет декодера для .{ext}")),
                });
                return;
            };
            // Стадия Preview (если есть)
            match decoder.decode_preview(&path) {
                Ok(Some(img)) => {
                    let _ = proxy.send_event(UserEvent::Decoded {
                        generation,
                        stage: Stage::Preview,
                        result: Ok(img),
                    });
                }
                Ok(None) => {}
                Err(e) => log::warn!("превью {path:?}: {e}"),
            }
            // Стадия Full
            let result = decoder.decode_full(&path).map_err(|e| e.to_string());
            let _ = proxy.send_event(UserEvent::Decoded {
                generation,
                stage: Stage::Full,
                result,
            });
        });
```
Также в `src/app.rs` обновить импорты декодера: заменить
`use crate::decoder::{DecodedImage, Decoder, StandardDecoder};` на
`use crate::decoder::{DecodedImage, Decoder};`. Trait `Decoder` ОСТАЁТСЯ (методы
`decode_preview`/`decode_full` зовутся на `Box<dyn Decoder>` — trait должен быть в scope);
убирается только неиспользуемый теперь `StandardDecoder`.

- [ ] **Step 4: Обработка стадий в user_event**

В `src/app.rs` заменить тело `UserEvent::Decoded { ... }` в `user_event`:
```rust
            UserEvent::Decoded { generation, stage, result } => {
                if generation != self.state.generation {
                    return; // устаревший результат — игнор
                }
                match result {
                    Ok(img) => {
                        let new_img = glam::Vec2::new(img.width as f32, img.height as f32);
                        if let Some(r) = &mut self.renderer {
                            let old_img = r.image_size();
                            r.upload_texture(&img.rgba, img.width, img.height);
                            let win = r.surface_size();
                            if self.state.inited_generation != Some(generation) {
                                // первый кадр этой генерации → инициализация вида (как v0.1)
                                let z = crate::view::fit_zoom(win, new_img);
                                self.state.view.set_min_zoom(z);
                                self.state.view.set_zoom_immediate(z);
                                self.state.view.set_fit(true);
                                self.state.view.set_pan(glam::Vec2::ZERO);
                                self.state.inited_generation = Some(generation);
                            } else if let Some(old_img) = old_img {
                                // подмена preview→full: сохраняем экранный размер
                                self.state.view.rescale_for_new_image(win, old_img, new_img);
                            }
                        }
                        if let Some(w) = &self.window {
                            w.request_redraw();
                        }
                    }
                    Err(e) => {
                        // full упал; если preview уже показан (inited) — просто остаёмся на нём
                        log::warn!("декодирование ({}) не удалось: {e}",
                            if stage == Stage::Preview { "preview" } else { "full" });
                    }
                }
            }
```

- [ ] **Step 5: Сбрасывать inited_generation при открытии файла**

В `src/app.rs` в `open_file`, после установки нового каталога, сбросить флаг (новая генерация
ещё не инициализирована). В блоке `Ok(cat) => { ... }` добавить перед `self.load_current();`:
```rust
                self.state.inited_generation = None;
```

> `load_current` инкрементит `generation`, а `inited_generation` сравнивается с ним по
> значению — при навигации стрелками тоже корректно: новая генерация != inited, значит первый
> пришедший кадр (preview или full) переинициализирует вид. Явный сброс в `open_file` не строго
> обязателен, но делает намерение очевидным.

- [ ] **Step 6: Сборка и все тесты**

Run: `cargo build 2>&1 | tail -10`
Expected: компилируется.

Run: `cargo test 2>&1 | tail -15`
Expected: все юнит-тесты зелёные (decoder + catalog + view + input), ignored — RAW/HEIC.

- [ ] **Step 7: Commit**

```bash
git add src/app.rs
git commit -m "feat: двухстадийная диспетчеризация декода (preview→full) с подменой вида"
```

---

## Task 8: Интеграция и ручная приёмка

**Files:**
- Modify: `ROADMAP.md`

- [ ] **Step 1: Прогнать все тесты**

Run: `cargo test 2>&1 | tail -20`
Expected: все обычные тесты зелёные; RAW/HEIC — ignored.

- [ ] **Step 2: Ручная приёмка на реальных файлах**

Нужны папка с реальными RAF/NEF/ARW/CR2 и хотя бы один HEIC.

Run: `cargo run -- "путь\к\фото.RAF"`

Проверить (см. §«Чек-лист ручной приёмки v0.2» в дизайне):
1. RAW: превью появляется быстро (<200 мс), затем бесшовно сменяется полным develop без «прыжка».
2. HEIC открывается.
3. Навигация стрелками по смешанной папке (JPEG+RAW+HEIC) в natord-порядке.
4. Зум/pan работают одинаково для всех форматов.
5. Битый RAW/HEIC не роняет приложение (лог `warn`).
6. RAW без embedded preview открывается (сразу full).
7. Ресайз окна сохраняет fit.

- [ ] **Step 3: Прогнать ignored-тесты, если есть фикстуры**

Положить `tests/fixtures/sample.raf` и `tests/fixtures/sample.heic`, затем:
Run: `cargo test -- --ignored 2>&1 | tail -15`
Expected: `heic_decodes_sample`, `raw_full_develops_sample`, `raw_preview_sample` — PASS.

- [ ] **Step 4: Обновить ROADMAP.md**

Статус v0.2 🟡→🟢; отметить чекбоксы:
```markdown
### v0.2 — RAW и форматы
- [x] rawler интеграция (RAF, NEF, ARW, CR2)
- [x] HEIC через libheif-rs
- [x] Embedded preview для мгновенного показа
- [x] Async полное декодирование RAW
```
И в таблице стадий статус v0.2 → 🟢.

- [ ] **Step 5: Финальный commit**

```bash
git add ROADMAP.md
git commit -m "chore: v0.2 RAW и форматы завершён — отметка в roadmap"
```

---

## Самопроверка плана

**Покрытие дизайна:** trait с 2 стадиями (Task 2); роутер+каталог (Task 3); HEIC (Task 4);
RAW embedded preview + базовый develop (Task 5); подмена вида preview→full (Task 6 + Task 7);
двухстадийный rayon-конвейер с generation-гейтингом (Task 7); зависимости и риск libheif с
smoke-линковкой (Task 1); обработка ошибок RAW/HEIC (варианты в error.rs — Task 4/5, поведение
в Task 7); тестирование чистой логики + ignored-фикстуры (по задачам); ручная приёмка (Task 8). ✓

**Согласованность типов:** `DecodedImage{rgba,width,height}` (без изменений); trait
`Decoder::{supports, decode_preview→Result<Option<DecodedImage>>, decode_full→Result<DecodedImage>}`;
роутер `supported(&str)->bool`, `decoder_for(&str)->Option<Box<dyn Decoder + Send>>`;
`UserEvent::Decoded{generation:u64, stage:Stage, result}`; `Stage::{Preview,Full}`;
`ViewTransform::rescale_for_new_image(win,old_img,new_img)`; `LuminaError::{Raw,Heic}(PathBuf,String)`.
Имена согласованы между задачами. ✓

**Заметки исполнителю:**
- Task 1 — самый рискованный (нативная линковка libheif). Не двигаться дальше, пока
  `smoke_libheif` не печатает версию.
- rawler документирован слабо; при расхождении сигнатур сверяться с `cargo doc -p rawler`.
  Подтверждённый для 0.7.x путь: `RawSource::new_file` → `get_decoder` →
  `Decoder::{preview_image, raw_image}` → `RawDevelop::develop_intermediate` →
  `Intermediate::to_dynamic_image`.
- Из `image::DynamicImage` (и rawler, и libheif возвращают разные версии) извлекаем только
  `to_rgba8().into_raw()` + размеры — примитивы, кросс-версионно безопасно.
- Task 3 связан с Task 4–5 (роутер ссылается на новые декодеры) — собирать/коммитить роутер
  после декодеров.
