# Lumina v0.5a — дисковый кэш миниатюр + префетч ±2 — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ускорить навигацию: миниатюры карусели кэшируются на диск (переживают перезапуск),
полноразмерные соседи (±2) упреждающе декодируются в память для мгновенного перелистывания.

**Architecture:** Два независимых кэш-слоя «чистое ядро + тонкая обёртка». `thumbcache` — файлы
на диске (PNG, ключ = FNV-1a хеш `path+mtime+size+th`), консультируется в rayon-воркере декода
миниатюр. `prefetch` — in-memory LRU декодированных кадров с байтовым бюджетом; заполняется при
завершении `Full` и используется `load_current` для мгновенного показа соседа.

**Tech Stack:** Rust, крейт `image` (PNG энкод/декод миниатюр), `rayon` (декод), `std` (ФС/хеш).
Новых внешних зависимостей нет.

## Global Constraints

- Крейт `lumina` **бинарный**: тесты — `cargo test --bin lumina <фильтр>`, сборка — `cargo build`.
- Bash-инструмент НЕ видит cargo/env сборки. В начале КАЖДОГО bash-вызова с cargo выставлять inline:
  `export PATH="$HOME/.cargo/bin:$HOME/vcpkg/installed/x64-windows/bin:$PATH" VCPKG_ROOT="$HOME/vcpkg" LIBCLANG_PATH="C:\\Program Files\\LLVM\\bin" VCPKGRS_DYNAMIC=1`
- Комментарии и документация — на русском.
- Коммиты завершать трейлером: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- Работа в ветке v0.5 (создать от `main`: `git checkout -b v0.5a-perf`).
- Дизайн: `docs/superpowers/specs/2026-06-17-lumina-v0.5a-thumbnail-cache-prefetch-design.md`.
- Стиль слоёв: чистое ядро (`thumbcache` ключ/путь, `prefetch`) юнит-тестируется; GPU/rayon —
  тонкие обёртки. Дефолты: дисковый бюджет 256 МБ, префетч-бюджет 512 МБ.

## Файловая структура

- **Создаётся** `src/thumbcache.rs` — ключ/путь/IO/prune дискового кэша миниатюр + юнит-тесты.
- **Создаётся** `src/prefetch.rs` — `PrefetchCache` (байтовый LRU) + юнит-тесты.
- **Изменяется** `src/main.rs` — `mod thumbcache; mod prefetch;`.
- **Изменяется** `src/app.rs` — интеграция обоих кэшей (поле `prefetch`, ветки в `request_thumbnails`,
  `load_current`, `user_event`, новый `UserEvent::Prefetched`, prune при старте).
- **Изменяется** `ROADMAP.md` — отметить пункты v0.5a.

---

## Task 1: `thumbcache` — ключ и путь кэша (чистое ядро)

**Files:**
- Create: `src/thumbcache.rs`
- Modify: `src/main.rs` (добавить `mod thumbcache;`)

**Interfaces:**
- Produces: `cache_key(path: &Path, mtime: u64, size: u64, th: u32) -> String`;
  `cache_path(dir: &Path, key: &str) -> PathBuf`.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/thumbcache.rs` с содержимым:
```rust
//! Дисковый кэш миниатюр: ключ (стабильный FNV-1a hex), путь, IO, эвикция по бюджету.
//! Чистое ядро (ключ/путь) тестируется юнитами; IO/prune — на временной папке.
use crate::decoder::DecodedImage;
use std::path::{Path, PathBuf};

/// FNV-1a 64-bit по байтам. Стабилен между сборками (в отличие от std::DefaultHasher),
/// поэтому пригоден для персистентного ключа кэша.
fn fnv1a64(bytes: &[u8]) -> u64 {
    let mut h: u64 = 0xcbf29ce484222325;
    for &b in bytes {
        h ^= b as u64;
        h = h.wrapping_mul(0x100000001b3);
    }
    h
}

/// Ключ кэша миниатюры: hex от FNV-1a по (путь, mtime, размер, целевая высота).
/// mtime+size обеспечивают авто-инвалидацию при правке файла.
pub fn cache_key(path: &Path, mtime: u64, size: u64, th: u32) -> String {
    let mut buf = path.to_string_lossy().into_owned().into_bytes();
    buf.extend_from_slice(&mtime.to_le_bytes());
    buf.extend_from_slice(&size.to_le_bytes());
    buf.extend_from_slice(&th.to_le_bytes());
    format!("{:016x}", fnv1a64(&buf))
}

/// Путь файла кэша: dir/<key>.png
pub fn cache_path(dir: &Path, key: &str) -> PathBuf {
    dir.join(format!("{key}.png"))
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::path::Path;

    #[test]
    fn cache_key_is_deterministic() {
        let p = Path::new("C:/x/a.jpg");
        assert_eq!(cache_key(p, 111, 222, 120), cache_key(p, 111, 222, 120));
    }

    #[test]
    fn cache_key_varies_by_each_component() {
        let p = Path::new("C:/x/a.jpg");
        let base = cache_key(p, 111, 222, 120);
        assert_ne!(base, cache_key(Path::new("C:/x/b.jpg"), 111, 222, 120));
        assert_ne!(base, cache_key(p, 999, 222, 120));
        assert_ne!(base, cache_key(p, 111, 999, 120));
        assert_ne!(base, cache_key(p, 111, 222, 240));
    }

    #[test]
    fn cache_key_is_16_hex() {
        let k = cache_key(Path::new("a"), 1, 2, 3);
        assert_eq!(k.len(), 16);
        assert!(k.chars().all(|c| c.is_ascii_hexdigit()));
    }

    #[test]
    fn cache_path_appends_png() {
        assert_eq!(cache_path(Path::new("C:/c"), "deadbeef"), Path::new("C:/c/deadbeef.png"));
    }
}
```
В `src/main.rs` добавить строку `mod thumbcache;` (в алфавитном порядке между `mod renderer;` и `mod thumbnail;`).

- [ ] **Step 2: Запустить — убедиться, что компилируется и тесты проходят**

Run: `cargo test --bin lumina thumbcache::tests`
Expected: PASS (4 теста). Если `DecodedImage` импорт не используется — компилятор предупредит;
оставить импорт, он понадобится в Task 2.

- [ ] **Step 3: Commit**

```bash
git add src/thumbcache.rs src/main.rs
git commit -m "feat(thumbcache): ключ FNV-1a и путь дискового кэша миниатюр" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `thumbcache` — IO (load/store) и prune

**Files:**
- Modify: `src/thumbcache.rs`

**Interfaces:**
- Consumes: `cache_key`, `cache_path` (Task 1); `DecodedImage { rgba: Vec<u8>, width: u32, height: u32 }`.
- Produces: `cache_dir() -> PathBuf`; `load(dir: &Path, key: &str) -> Option<DecodedImage>`;
  `store(dir: &Path, key: &str, img: &DecodedImage)`; `prune(dir: &Path, budget: u64)`.

- [ ] **Step 1: Проверить поля `DecodedImage`**

Run: `grep -n "pub struct DecodedImage" -A6 src/decoder/mod.rs`
Expected: поля `rgba: Vec<u8>`, `width: u32`, `height: u32` (подставить точные имена ниже, если отличаются).

- [ ] **Step 2: Написать падающие тесты IO**

В `src/thumbcache.rs` в `mod tests` добавить:
```rust
    fn tmp_subdir(name: &str) -> std::path::PathBuf {
        let d = std::env::temp_dir().join(format!("lumina_thumbcache_test_{name}"));
        let _ = std::fs::remove_dir_all(&d);
        std::fs::create_dir_all(&d).unwrap();
        d
    }

    #[test]
    fn store_then_load_roundtrip() {
        let dir = tmp_subdir("roundtrip");
        // 2x1 RGBA: красный, зелёный
        let img = DecodedImage { rgba: vec![255,0,0,255, 0,255,0,255], width: 2, height: 1 };
        store(&dir, "k1", &img);
        let got = load(&dir, "k1").expect("должно прочитаться");
        assert_eq!((got.width, got.height), (2, 1));
        assert_eq!(got.rgba, img.rgba);
        let _ = std::fs::remove_dir_all(&dir);
    }

    #[test]
    fn load_missing_is_none() {
        let dir = tmp_subdir("missing");
        assert!(load(&dir, "nope").is_none());
        let _ = std::fs::remove_dir_all(&dir);
    }

    #[test]
    fn prune_evicts_oldest_over_budget() {
        let dir = tmp_subdir("prune");
        let img = DecodedImage { rgba: vec![0u8; 4 * 64 * 64], width: 64, height: 64 };
        // три записи; бюджет ~ под две → старейшая удаляется
        store(&dir, "old", &img);
        store(&dir, "mid", &img);
        store(&dir, "new", &img);
        // выставить mtime по возрастанию через повторную запись в нужном порядке не получится
        // надёжно — поэтому бюджет 0 удаляет всё, бюджет огромный не трогает ничего:
        prune(&dir, u64::MAX);
        assert!(cache_path(&dir, "old").exists());
        prune(&dir, 0);
        assert!(!cache_path(&dir, "old").exists());
        assert!(!cache_path(&dir, "new").exists());
        let _ = std::fs::remove_dir_all(&dir);
    }
```
> Примечание: тест prune проверяет границы (бюджет 0 → всё удалено; `u64::MAX` → ничего), а не
> точный порядок mtime — он недетерминирован в тесте. Логика «старейшие первыми» покрыта реализацией.

- [ ] **Step 3: Запустить — убедиться, что падают (нет функций)**

Run: `cargo test --bin lumina thumbcache::tests`
Expected: FAIL — `load`/`store`/`prune` не определены.

- [ ] **Step 4: Реализовать IO и prune**

В `src/thumbcache.rs` (после `cache_path`) добавить:
```rust
/// Папка кэша: %LOCALAPPDATA%\Lumina\thumbs, иначе temp/lumina/thumbs.
pub fn cache_dir() -> PathBuf {
    let base = std::env::var_os("LOCALAPPDATA")
        .map(PathBuf::from)
        .unwrap_or_else(|| std::env::temp_dir().join("lumina"));
    base.join("Lumina").join("thumbs")
}

/// Прочитать миниатюру из кэша (PNG → RGBA8). None при отсутствии/ошибке.
pub fn load(dir: &Path, key: &str) -> Option<DecodedImage> {
    let path = cache_path(dir, key);
    let bytes = std::fs::read(&path).ok()?;
    let img = image::load_from_memory_with_format(&bytes, image::ImageFormat::Png).ok()?;
    let rgba = img.to_rgba8();
    let (width, height) = (rgba.width(), rgba.height());
    Some(DecodedImage { rgba: rgba.into_raw(), width, height })
}

/// Записать миниатюру в кэш (RGBA8 → PNG), атомарно через временный файл + rename.
pub fn store(dir: &Path, key: &str, img: &DecodedImage) {
    if std::fs::create_dir_all(dir).is_err() {
        return;
    }
    let Some(buf) = image::RgbaImage::from_raw(img.width, img.height, img.rgba.clone()) else {
        return;
    };
    let final_path = cache_path(dir, key);
    let tmp_path = dir.join(format!("{key}.tmp"));
    if buf.save_with_format(&tmp_path, image::ImageFormat::Png).is_ok() {
        let _ = std::fs::rename(&tmp_path, &final_path);
    }
}

/// Удалять файлы кэша по возрастанию mtime, пока суммарный размер не уложится в бюджет.
pub fn prune(dir: &Path, budget: u64) {
    let Ok(rd) = std::fs::read_dir(dir) else { return };
    let mut entries: Vec<(PathBuf, u64, std::time::SystemTime)> = rd
        .filter_map(|e| e.ok())
        .filter_map(|e| {
            let md = e.metadata().ok()?;
            if !md.is_file() {
                return None;
            }
            let mtime = md.modified().unwrap_or(std::time::UNIX_EPOCH);
            Some((e.path(), md.len(), mtime))
        })
        .collect();
    let total: u64 = entries.iter().map(|(_, s, _)| *s).sum();
    if total <= budget {
        return;
    }
    // старейшие первыми
    entries.sort_by_key(|(_, _, t)| *t);
    let mut cur = total;
    for (path, size, _) in entries {
        if cur <= budget {
            break;
        }
        if std::fs::remove_file(&path).is_ok() {
            cur = cur.saturating_sub(size);
        }
    }
}
```

- [ ] **Step 5: Запустить — убедиться, что проходят**

Run: `cargo test --bin lumina thumbcache::tests`
Expected: PASS (7 тестов).

- [ ] **Step 6: Commit**

```bash
git add src/thumbcache.rs
git commit -m "feat(thumbcache): load/store (PNG) и prune по бюджету" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Подключить дисковый кэш к декоду миниатюр

**Files:**
- Modify: `src/app.rs` (`request_thumbnails` rayon-воркер; prune при старте в `resumed`/`open_file`)

**Interfaces:**
- Consumes: `thumbcache::{cache_dir, cache_key, load, store, prune}`; существующий `make_thumb`,
  `UserEvent::Thumbnail`.
- Produces: поведение — миниатюра берётся из кэша при наличии, иначе декодируется и кэшируется.

- [ ] **Step 1: В rayon-воркере `request_thumbnails` сперва читать кэш, при промахе — писать**

В `src/app.rs::request_thumbnails`, внутри `rayon::spawn(move || { ... })`, заменить тело
(блок от `let Some(decoder) = …` до отправки `UserEvent::Thumbnail`) на:
```rust
                // Ключ кэша по метаданным файла (mtime+size инвалидируют при правке).
                let key = std::fs::metadata(&path).ok().map(|md| {
                    let mtime = md.modified().ok()
                        .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
                        .map(|d| d.as_secs())
                        .unwrap_or(0);
                    crate::thumbcache::cache_key(&path, mtime, md.len(), th)
                });
                let dir = crate::thumbcache::cache_dir();
                // 1) попытка из дискового кэша
                if let Some(k) = &key {
                    if let Some(img) = crate::thumbcache::load(&dir, k) {
                        let _ = proxy.send_event(UserEvent::Thumbnail {
                            generation, index, rgba: img.rgba, w: img.width, h: img.height,
                        });
                        return;
                    }
                }
                // 2) промах → декод + ресайз + запись в кэш
                let Some(decoder) = crate::decoder::decoder_for(&ext) else { return };
                let decoded = match decoder.decode_preview(&path) {
                    Ok(Some(img)) => Some(img),
                    _ => decoder.decode_full(&path).ok(),
                };
                let Some(img) = decoded else {
                    let _ = proxy.send_event(UserEvent::Thumbnail { generation, index, rgba: Vec::new(), w: 0, h: 0 });
                    return;
                };
                let (rgba, w, h) = crate::app::make_thumb(&img.rgba, img.width, img.height, th);
                if let Some(k) = &key {
                    crate::thumbcache::store(&dir, k, &crate::decoder::DecodedImage { rgba: rgba.clone(), width: w, height: h });
                }
                let _ = proxy.send_event(UserEvent::Thumbnail { generation, index, rgba, w, h });
```
> `DecodedImage` уже импортирован в `app.rs` (`use crate::decoder::{… DecodedImage …}`). Если нет —
> используйте полный путь `crate::decoder::DecodedImage`, как выше.

- [ ] **Step 2: Запустить prune один раз при старте (фоном)**

В `src/app.rs::resumed`, сразу после `self.window = Some(window);` (перед открытием стартового файла),
добавить:
```rust
        // Разовая эвикция дискового кэша миниатюр (фоном, бюджет 256 МБ).
        rayon::spawn(|| {
            const THUMB_CACHE_BUDGET: u64 = 256 * 1024 * 1024;
            crate::thumbcache::prune(&crate::thumbcache::cache_dir(), THUMB_CACHE_BUDGET);
        });
```

- [ ] **Step 3: Сборка и прогон тестов**

Run: `cargo build`
Expected: компилируется без новых warnings.

Run: `cargo test --bin lumina`
Expected: PASS (юнит-тесты; `#[ignore]` пропущены).

- [ ] **Step 4: Ручная проверка кэша**

Run: `cargo run -- "tests/fixtures/red_2x3.jpg"`
Открыть папку с несколькими фото → дождаться миниатюр → закрыть → снова открыть: миниатюры
появляются мгновенно. Проверить, что в `%LOCALAPPDATA%\Lumina\thumbs` появились `.png`.

- [ ] **Step 5: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): дисковый кэш миниатюр в конвейере декода + prune при старте" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: `prefetch` — байтовый LRU-кэш кадров (чистое ядро)

**Files:**
- Create: `src/prefetch.rs`
- Modify: `src/main.rs` (`mod prefetch;`)

**Interfaces:**
- Consumes: `DecodedImage`.
- Produces: `PrefetchCache::new(budget: u64)`; `get(&self, path: &Path) -> Option<Arc<DecodedImage>>`;
  `contains(&self, path: &Path) -> bool`; `insert(&mut self, path: PathBuf, img: Arc<DecodedImage>) -> Vec<PathBuf>`;
  `clear(&mut self)`.

- [ ] **Step 1: Написать падающие тесты**

Создать `src/prefetch.rs`:
```rust
//! In-memory LRU декодированных кадров с байтовым бюджетом — для мгновенного
//! перелистывания (префетч ±2). Чистое ядро, без GPU/rayon.
use crate::decoder::DecodedImage;
use std::collections::HashMap;
use std::path::{Path, PathBuf};
use std::sync::Arc;

pub struct PrefetchCache {
    map: HashMap<PathBuf, Arc<DecodedImage>>,
    lru: Vec<PathBuf>, // фронт — давний, хвост — свежий
    budget: u64,
    bytes: u64,
}

impl PrefetchCache {
    pub fn new(budget: u64) -> Self {
        Self { map: HashMap::new(), lru: Vec::new(), budget, bytes: 0 }
    }

    fn img_bytes(img: &DecodedImage) -> u64 {
        img.rgba.len() as u64
    }

    pub fn contains(&self, path: &Path) -> bool {
        self.map.contains_key(path)
    }

    /// Получить кадр и пометить как свежеиспользованный (touch).
    pub fn get(&mut self, path: &Path) -> Option<Arc<DecodedImage>> {
        let img = self.map.get(path)?.clone();
        self.touch(path);
        Some(img)
    }

    fn touch(&mut self, path: &Path) {
        if let Some(pos) = self.lru.iter().position(|p| p == path) {
            let p = self.lru.remove(pos);
            self.lru.push(p);
        }
    }

    /// Вставить кадр; вытеснить LRU сверх бюджета. Возвращает вытесненные пути.
    pub fn insert(&mut self, path: PathBuf, img: Arc<DecodedImage>) -> Vec<PathBuf> {
        if let Some(old) = self.map.remove(&path) {
            self.bytes = self.bytes.saturating_sub(Self::img_bytes(&old));
            if let Some(pos) = self.lru.iter().position(|p| *p == path) {
                self.lru.remove(pos);
            }
        }
        self.bytes += Self::img_bytes(&img);
        self.map.insert(path.clone(), img);
        self.lru.push(path);
        let mut evicted = Vec::new();
        while self.bytes > self.budget && self.lru.len() > 1 {
            let old = self.lru.remove(0);
            if let Some(img) = self.map.remove(&old) {
                self.bytes = self.bytes.saturating_sub(Self::img_bytes(&img));
            }
            evicted.push(old);
        }
        evicted
    }

    pub fn clear(&mut self) {
        self.map.clear();
        self.lru.clear();
        self.bytes = 0;
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn img(bytes: usize) -> Arc<DecodedImage> {
        Arc::new(DecodedImage { rgba: vec![0u8; bytes], width: 1, height: (bytes / 4) as u32 })
    }

    #[test]
    fn insert_and_get() {
        let mut c = PrefetchCache::new(1000);
        c.insert(PathBuf::from("a"), img(100));
        assert!(c.contains(Path::new("a")));
        assert!(c.get(Path::new("a")).is_some());
        assert!(c.get(Path::new("b")).is_none());
    }

    #[test]
    fn evicts_lru_over_byte_budget() {
        let mut c = PrefetchCache::new(250);
        c.insert(PathBuf::from("a"), img(100));
        c.insert(PathBuf::from("b"), img(100));
        let evicted = c.insert(PathBuf::from("c"), img(100)); // 300 > 250 → вытеснить "a"
        assert_eq!(evicted, vec![PathBuf::from("a")]);
        assert!(!c.contains(Path::new("a")));
        assert!(c.contains(Path::new("b")));
        assert!(c.contains(Path::new("c")));
    }

    #[test]
    fn get_touches_lru_order() {
        let mut c = PrefetchCache::new(250);
        c.insert(PathBuf::from("a"), img(100));
        c.insert(PathBuf::from("b"), img(100));
        let _ = c.get(Path::new("a")); // "a" освежён → теперь "b" старейший
        let evicted = c.insert(PathBuf::from("c"), img(100));
        assert_eq!(evicted, vec![PathBuf::from("b")]);
        assert!(c.contains(Path::new("a")));
    }

    #[test]
    fn clear_empties() {
        let mut c = PrefetchCache::new(1000);
        c.insert(PathBuf::from("a"), img(100));
        c.clear();
        assert!(!c.contains(Path::new("a")));
    }
}
```
В `src/main.rs` добавить `mod prefetch;` (между `mod platform;` и `mod renderer;`).

- [ ] **Step 2: Запустить — убедиться, что проходят**

Run: `cargo test --bin lumina prefetch::tests`
Expected: PASS (4 теста).

- [ ] **Step 3: Commit**

```bash
git add src/prefetch.rs src/main.rs
git commit -m "feat(prefetch): байтовый LRU-кэш декодированных кадров" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Интегрировать префетч соседей в конвейер

**Files:**
- Modify: `src/app.rs` (поле `prefetch`, `UserEvent::Prefetched`, ветки в `load_current`,
  `user_event` для `Decoded{Full}`, новый метод `prefetch_neighbors`, сброс в `open_file`)

**Interfaces:**
- Consumes: `prefetch::PrefetchCache`; `DecodedImage`; существующий путь установки кадра в рендерер.
- Produces: мгновенный показ соседа при хите кэша; фоновый декод ±2.

- [ ] **Step 1: Добавить поле кэша и новое событие**

В `src/app.rs`:
1. В `enum UserEvent` добавить вариант:
```rust
    Prefetched {
        path: PathBuf,
        image: DecodedImage,
    },
```
2. В `struct AppState` добавить поле:
```rust
    pub prefetch: crate::prefetch::PrefetchCache,
```
3. В `AppState::new()` инициализировать (в конце литерала структуры):
```rust
            prefetch: crate::prefetch::PrefetchCache::new(512 * 1024 * 1024),
```

- [ ] **Step 2: Вынести установку Full-кадра в метод (для переиспользования из кэша)**

Найти в `user_event` обработку `UserEvent::Decoded { stage: Full, result: Ok(img) }` — код, который
заливает кадр в рендерер и инициализирует вид (вокруг строк 624–675). НЕ меняя её логику, убедиться,
что есть способ выполнить тот же путь для готового `DecodedImage`. Если установка кадра уже
инкапсулирована — переиспользовать; иначе добавить метод:
```rust
    /// Установить готовый полноразмерный кадр в рендерер и (при первом кадре генерации)
    /// инициализировать вид. Вызывается и из Decoded{Full}, и при хите префетч-кэша.
    fn install_full_image(&mut self, generation: u64, img: &DecodedImage) {
        if let Some(r) = &mut self.renderer {
            r.set_image(&img.rgba, img.width, img.height);
            if self.state.inited_generation != Some(generation) {
                let win = r.viewer_size();
                self.state.view.set_fit(true);
                let fit = self.state.view.fit_zoom(win, (img.width, img.height));
                self.state.view.set_min_zoom(fit);
                self.state.view.set_zoom_immediate(fit);
                self.state.view.set_pan(glam::Vec2::ZERO);
                self.state.inited_generation = Some(generation);
            }
        }
        if let Some(w) = &self.window { w.request_redraw(); }
    }
```
> ВАЖНО: точные имена методов рендерера (`set_image`/`viewer_size`/`image_size`) и сигнатуру
> `fit_zoom` сверить с существующим кодом обработки `Decoded{Full}` и **скопировать оттуда дословно**,
> чтобы поведение совпадало 1:1. Этот шаг — рефактор-извлечение без смены логики.

- [ ] **Step 3: Хит префетч-кэша в `load_current`**

В начале `load_current`, сразу после вычисления `let path = catalog.current_path().to_path_buf();`
и восстановления трансформа/инкремента generation, добавить ветку быстрого пути ПЕРЕД `rayon::spawn`:
```rust
        // Быстрый путь: сосед уже в префетч-кэше → показать мгновенно, без декода.
        if let Some(img) = self.state.prefetch.get(&path) {
            self.install_full_image(generation, &img);
            self.prefetch_neighbors();
            return;
        }
```
> Все обновления заголовка окна и списков каталога (title/thumb_count/active_index/raw_flags/…),
> которые сейчас идут до `rayon::spawn`, должны выполняться ДО этой ветки — перенести ветку
> быстрого пути после них, чтобы UI карусели/заголовка обновлялся в обоих случаях.

- [ ] **Step 4: Класть Full-кадр в кэш и запускать префетч соседей**

1. В `user_event`, в обработке `Decoded { stage: Full, result: Ok(img) }`, после установки кадра
   (после существующего кода, который сейчас заливает текстуру) добавить кэширование и префетч:
```rust
                            // Закэшировать текущий кадр и упредить соседей.
                            if let Some(cat) = &self.state.catalog {
                                let p = cat.current_path().to_path_buf();
                                self.state.prefetch.insert(p, std::sync::Arc::new(img.clone()));
                            }
                            self.prefetch_neighbors();
```
   > `img` здесь — `DecodedImage`; если он перемещается в рендерер до этого места, клонировать ДО
   > перемещения или переставить порядок. Сверить с фактическим кодом; цель — в кэше лежит копия
   > показанного кадра.
2. Добавить новую ветку в `match` `user_event`:
```rust
            UserEvent::Prefetched { path, image } => {
                self.state.prefetch.insert(path, std::sync::Arc::new(image));
                // вид не трогаем: это лишь наполнение горячего кэша
            }
```

- [ ] **Step 5: Метод `prefetch_neighbors`**

Добавить в `impl App` (рядом с `request_thumbnails`):
```rust
    /// Запустить фоновый декод соседей (±1, ±2), которых ещё нет в кэше.
    fn prefetch_neighbors(&mut self) {
        let Some(cat) = &self.state.catalog else { return };
        let cur = cat.current_index();
        let files = cat.files();
        let n = files.len();
        let mut targets: Vec<PathBuf> = Vec::new();
        for d in [1usize, 2usize] {
            if cur + d < n { targets.push(files[cur + d].to_path_buf()); }
            if cur >= d { targets.push(files[cur - d].to_path_buf()); }
        }
        for path in targets {
            if self.state.prefetch.contains(&path) {
                continue;
            }
            let proxy = self.proxy.clone();
            let ext = crate::decoder::ext_lower(&path);
            rayon::spawn(move || {
                let Some(decoder) = crate::decoder::decoder_for(&ext) else { return };
                if let Ok(img) = decoder.decode_full(&path) {
                    let _ = proxy.send_event(UserEvent::Prefetched { path, image: img });
                }
            });
        }
    }
```
> `decode_full` соседей сознательно: префетч-кэш хранит полноразмерный кадр (тот же, что показывается).
> Байтовый бюджет 512 МБ сам ограничит число удержанных кадров на тяжёлых RAW.

- [ ] **Step 6: Сброс префетча при смене папки**

В `src/app.rs::open_file`, рядом с `self.state.thumbs.reset();`, добавить:
```rust
                self.state.prefetch.clear();
```

- [ ] **Step 7: Сборка и тесты**

Run: `cargo build`
Expected: компилируется без новых warnings. Если `DecodedImage` не реализует `Clone` — добавить
`#[derive(Clone)]` к `struct DecodedImage` в `src/decoder/mod.rs` (это безопасно: только данные).

Run: `cargo test --bin lumina`
Expected: PASS.

- [ ] **Step 8: Ручная проверка префетча**

Run: `cargo run -- "путь\к\папке\с\тяжёлыми RAF\первый.RAF"`
Пройти вперёд по нескольким кадрам и назад: после первого показа кадра возврат к нему и переход
к уже посещённому соседу — мгновенные (без стадии декода). Память не растёт неограниченно.

- [ ] **Step 9: Commit**

```bash
git add src/app.rs src/decoder/mod.rs
git commit -m "feat(app): префетч соседей ±2 в память для мгновенной навигации" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: ROADMAP

**Files:**
- Modify: `ROADMAP.md`

- [ ] **Step 1: Отметить пункты v0.5a**

В `ROADMAP.md` в строке стадии **v0.5** статус оставить 🟡 (фаза начата). В разделе «Стадии» к строке
v0.5 при необходимости добавить ссылку на дизайн. Добавить (если ещё нет) детальный блок «Прогресс
v0.5a»:
```markdown
## Прогресс v0.5a (детально)

- [x] Дисковый кэш миниатюр (`src/thumbcache.rs`): ключ FNV-1a по `path+mtime+size+th`, PNG на диске
      в `%LOCALAPPDATA%\Lumina\thumbs`, эвикция по бюджету 256 МБ (prune при старте). Переоткрытие
      папки — миниатюры из кэша, без повторного декода
- [x] Префетч ±2 (`src/prefetch.rs`): in-memory LRU декодированных кадров с байтовым бюджетом 512 МБ;
      заполняется при завершении `Full`, используется `load_current` для мгновенного перелистывания
      ([дизайн](docs/superpowers/specs/2026-06-17-lumina-v0.5a-thumbnail-cache-prefetch-design.md))

Код и юнит-тесты готовы (проверить счётчик `cargo test --bin lumina`: +11 новых юнит-тестов —
thumbcache 7, prefetch 4).
```
> Точный счётчик подтвердить прогоном `cargo test --bin lumina` и указать фактическое число.

- [ ] **Step 2: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): v0.5a — дисковый кэш миниатюр + префетч ±2" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-review (выполнен при написании)

- **Покрытие спека:** дисковый кэш (ключ/IO/prune) → Task 1–2; интеграция кэша в декод + prune
  при старте → Task 3; `PrefetchCache` → Task 4; префетч соседей + быстрый путь в `load_current` →
  Task 5; ROADMAP → Task 6. Дефолты бюджетов (256/512 МБ) — Task 3 Step 2 и Task 5 Step 1.
- **Плейсхолдеров нет:** весь код приведён; места, требующие сверки с существующим кодом
  (`DecodedImage` поля, имена методов рендерера, точка установки Full-кадра), помечены явными
  инструкциями «скопировать дословно» — это рефактор-извлечение, а не догадка.
- **Согласованность типов:** `cache_key/cache_path/load/store/prune/cache_dir` — сигнатуры заданы в
  Task 1–2 и использованы в Task 3 без расхождений; `PrefetchCache::{new,get,contains,insert,clear}`
  заданы в Task 4, использованы в Task 5; `UserEvent::Prefetched { path, image }` объявлен и обработан
  в Task 5; `DecodedImage` требует `Clone` (отмечено в Task 5 Step 7).
- **Риск:** Task 5 опирается на дословное копирование пути установки кадра из обработчика
  `Decoded{Full}` — исполнителю предписано сверять имена, чтобы не разойтись с рендерером.
