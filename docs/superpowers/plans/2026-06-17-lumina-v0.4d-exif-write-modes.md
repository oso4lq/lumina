# Lumina v0.4d — починка записи EXIF + режимы бэкапа/перезаписи — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Починить запись EXIF для RAW (in-place вместо сломанного sidecar) и добавить два режима сохранения — обычный (бэкап `_original`) и необратимый (`-overwrite_original`) с действием «Стереть всё» (удаление всех метаданных, кроме Orientation/ICC).

**Architecture:** Расширяет `exif::write` и popup части 2. Построение аргументов exiftool — чистые функции (`edit_args`/`strip_args`, юнит-тест); запуск subprocess — тонкая обёртка `run_exiftool`. UI (`theme`/`layout`/`scene`/`hit`) тестируется без GPU. Состояние подтверждений обобщается из булева `confirm_close` в enum `ConfirmKind`. Новых GPU-пайплайнов нет.

**Tech Stack:** Rust, winit, wgpu, glyphon, внешний `exiftool` (subprocess). Дизайн: `docs/superpowers/specs/2026-06-17-lumina-v0.4d-exif-write-modes-design.md`.

---

## ВАЖНО для всех задач (окружение и команды)

- Крейт `lumina` **бинарный**: `cargo test --lib` падает. Тесты — `cargo test --bin lumina <фильтр>`, сборка — `cargo build`.
- Bash-инструмент НЕ видит cargo/env сборки. В начале КАЖДОГО bash-вызова с cargo выставлять inline:
  `export PATH="$HOME/.cargo/bin:$HOME/vcpkg/installed/x64-windows/bin:$PATH" VCPKG_ROOT="$HOME/vcpkg" LIBCLANG_PATH="C:\\Program Files\\LLVM\\bin" VCPKGRS_DYNAMIC=1`
- Коммиты завершать трейлером: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- Работа в ветке `v0.4b-exif` (часть 2 уже в ней; продолжаем).
- **exiftool** для интеграционных тестов: dev-shim `target/debug/exiftool.exe` (exiftool 13.59) уже на месте. Юнит-тесты exiftool не требуют; интеграционные помечены `#[ignore]`.
- **Бинарный крейт — нюанс компиляции:** Tasks 3 и 4 (scene/hit) меняют типы, на которые ссылается `app.rs`. Между ними и Task 5 бинарь **не компилируется** — это ожидаемо (как было в части 2: scene коммитился до app). Полный `cargo build`/`cargo test --bin lumina` прогоняется в Task 5. В Tasks 3/4 код и тесты пишутся, но прогон откладывается до Task 5 (отмечено в шагах).

---

## Файловая структура

- **Изменяется** `src/exif/write.rs` — `WriteMode`, `edit_args`, `strip_args`, `write_edits(.., mode)`, `strip_all`, `run_exiftool`; убирается ветка sidecar (+ тесты).
- **Изменяется** `src/ui/theme.rs` — поле `danger_bg`.
- **Изменяется** `src/ui/layout.rs` — `popup_footer_toggle`, `popup_footer_strip` (+ тест).
- **Изменяется** `src/ui/scene.rs` — `ConfirmKind`, поля `PopupEditState` (`confirm`/`overwrite_mode` вместо `confirm_close`), отрисовка тоггла/strip/вариантов confirm-бара (+ тесты).
- **Изменяется** `src/ui/hit.rs` — регионы `FooterToggle`/`FooterStrip`, переименование confirm-регионов, новая сигнатура `hit_popup_edit` (+ тесты).
- **Изменяется** `src/app.rs` — состояние (`exif_confirm`/`exif_overwrite_mode`/`exif_pending_strip_all`/`exif_close_after_save`), `exif_save(mode)`/`exif_strip_all`, диспетчер кликов/клавиатуры, проброс в `build_popup`.
- **Изменяется** `ROADMAP.md` — запись v0.4d.

---

## Task 1: `exif::write` — WriteMode, in-place fix, strip (Фаза 1)

**Files:**
- Modify: `src/exif/write.rs`
- Modify: `src/app.rs` (единственный вызов `write_edits`)
- Test: `src/exif/write.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты (чистые + интеграционные)**

В `#[cfg(test)] mod tests` в `src/exif/write.rs` добавить (рядом с существующим `write_real_jpg_set_and_backup`):
```rust
    #[test]
    fn edit_args_backup_is_plain() {
        let edits = vec![TagEdit::Set { group: "EXIF".into(), tag: "Artist".into(), value: "Jane".into() }];
        assert_eq!(edit_args(&edits, WriteMode::Backup), vec!["-EXIF:Artist=Jane".to_string()]);
    }

    #[test]
    fn edit_args_overwrite_adds_flag() {
        let edits = vec![TagEdit::Delete { group: "XMP".into(), tag: "Rating".into() }];
        assert_eq!(
            edit_args(&edits, WriteMode::Overwrite),
            vec!["-XMP:Rating=".to_string(), "-overwrite_original".to_string()]
        );
    }

    #[test]
    fn edit_args_empty_is_empty_any_mode() {
        assert!(edit_args(&[], WriteMode::Backup).is_empty());
        assert!(edit_args(&[], WriteMode::Overwrite).is_empty());
    }

    #[test]
    fn strip_args_keeps_orientation_and_icc() {
        assert_eq!(
            strip_args(),
            vec![
                "-all=".to_string(),
                "-tagsfromfile".to_string(),
                "@".to_string(),
                "-orientation".to_string(),
                "-icc_profile".to_string(),
                "-overwrite_original".to_string(),
            ]
        );
    }

    #[test]
    #[ignore]
    fn write_overwrite_no_backup() {
        let src = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures/red_2x3.jpg");
        let tmp = std::env::temp_dir().join("lumina_overwrite_test.jpg");
        std::fs::copy(&src, &tmp).expect("копия фикстуры");
        write_edits(&tmp, &[TagEdit::Set { group: "EXIF".into(), tag: "Artist".into(), value: "Over".into() }], WriteMode::Overwrite).expect("запись");
        let backup = tmp.with_file_name("lumina_overwrite_test.jpg_original");
        assert!(!backup.exists(), "в режиме Overwrite бэкапа быть не должно");
        let tags = crate::exif::read::read_tags(&tmp).expect("чтение");
        assert_eq!(crate::exif::tags::get(&tags, "EXIF", "Artist").as_deref(), Some("Over"));
        let _ = std::fs::remove_file(&tmp);
    }

    #[test]
    #[ignore]
    fn strip_all_removes_pii_keeps_orientation() {
        let src = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures/red_2x3.jpg");
        let tmp = std::env::temp_dir().join("lumina_strip_test.jpg");
        std::fs::copy(&src, &tmp).expect("копия фикстуры");
        // подготовка: PII (Artist) + Orientation
        write_edits(
            &tmp,
            &[
                TagEdit::Set { group: "EXIF".into(), tag: "Artist".into(), value: "Secret".into() },
                TagEdit::Set { group: "EXIF".into(), tag: "Orientation".into(), value: "6".into() },
            ],
            WriteMode::Overwrite,
        )
        .expect("подготовка");
        strip_all(&tmp).expect("стирание");
        let tags = crate::exif::read::read_tags(&tmp).expect("чтение");
        assert_eq!(crate::exif::tags::get(&tags, "EXIF", "Artist"), None, "PII должен быть удалён");
        assert!(crate::exif::tags::get(&tags, "EXIF", "Orientation").is_some(), "Orientation должен остаться");
        let _ = std::fs::remove_file(&tmp);
    }
```
Также **обновить** существующий `write_real_jpg_set_and_backup`: строку
`write_edits(&tmp, &edits).expect("запись прошла");` заменить на
`write_edits(&tmp, &edits, WriteMode::Backup).expect("запись прошла");`.

- [ ] **Step 2: Запустить — убедиться, что не компилируется**

Run: `cargo test --bin lumina write::tests::edit_args`
Expected: ошибка компиляции — нет `edit_args`/`strip_args`/`WriteMode`/новой сигнатуры `write_edits`.

- [ ] **Step 3: Реализовать в `write.rs`**

Заменить **весь** не-тестовый код `src/exif/write.rs` (от `use` до конца функций, перед `#[cfg(test)]`) на:
```rust
//! Обёртка: запись правок тегов через exiftool (subprocess). Логики нет — только
//! формирование команды. Все форматы (вкл. RAW) пишутся in-place; режим определяет,
//! оставлять ли восстановимый бэкап (`_original`) или перезаписывать необратимо.
use crate::error::{LuminaError, Result};
use crate::exif::tags::{edits_to_args, TagEdit};
use std::path::Path;
use std::process::Command;

/// Режим записи: оставлять ли восстановимый бэкап `_original`.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum WriteMode {
    /// Обычный: exiftool делает `_original` (обратимо). По умолчанию.
    Backup,
    /// Необратимый: `-overwrite_original`, без бэкапа.
    Overwrite,
}

/// Аргументы exiftool для набора правок (без пути). Пустой список → пустой вектор.
/// `Overwrite` добавляет `-overwrite_original`.
pub fn edit_args(edits: &[TagEdit], mode: WriteMode) -> Vec<String> {
    if edits.is_empty() {
        return Vec::new();
    }
    let mut args = edits_to_args(edits);
    if mode == WriteMode::Overwrite {
        args.push("-overwrite_original".to_string());
    }
    args
}

/// Аргументы стирания всех метаданных с сохранением Orientation + ICC (всегда необратимо).
pub fn strip_args() -> Vec<String> {
    vec![
        "-all=".to_string(),
        "-tagsfromfile".to_string(),
        "@".to_string(),
        "-orientation".to_string(),
        "-icc_profile".to_string(),
        "-overwrite_original".to_string(),
    ]
}

/// Применить набор правок in-place (все форматы). Пустой список — no-op (успех).
pub fn write_edits(path: &Path, edits: &[TagEdit], mode: WriteMode) -> Result<()> {
    let args = edit_args(edits, mode);
    if args.is_empty() {
        return Ok(());
    }
    run_exiftool(path, &args)
}

/// Стереть все метаданные (Orientation + ICC сохраняются), необратимо.
pub fn strip_all(path: &Path) -> Result<()> {
    run_exiftool(path, &strip_args())
}

/// Запустить exiftool с аргументами + `-- path`. `--` завершает разбор опций
/// (защита от подмены флагов именем файла).
fn run_exiftool(path: &Path, args: &[String]) -> Result<()> {
    let out = Command::new(crate::exif::read::exiftool_path())
        .args(args)
        .arg("--")
        .arg(path)
        .output()
        .map_err(|e| LuminaError::Exif(path.to_path_buf(), format!("запуск exiftool: {e}")))?;
    if !out.status.success() {
        let err = String::from_utf8_lossy(&out.stderr).trim().to_string();
        return Err(LuminaError::Exif(path.to_path_buf(), err));
    }
    Ok(())
}
```
> Старый `use crate::decoder::{ext_lower, Decoder, RawDecoder};` и ветка `-o %d%f.xmp` удаляются полностью.

- [ ] **Step 4: Обновить единственный вызов в `app.rs`**

В `src/app.rs`, в методе `exif_save`, строку
```rust
            let result = crate::exif::write::write_edits(&path, &edits).map_err(|e| e.to_string());
```
заменить на (Фаза 1 — фиксируем обычный режим; реальный режим протянем в Task 5):
```rust
            let result = crate::exif::write::write_edits(&path, &edits, crate::exif::write::WriteMode::Backup).map_err(|e| e.to_string());
```

- [ ] **Step 5: Сборка и тесты**

Run: `cargo build`
Expected: компилируется (sidecar-ветка убрана; единственный вызов обновлён).

Run: `cargo test --bin lumina write::tests`
Expected: PASS (4 чистых теста; 3 `#[ignore]` пропущены).

- [ ] **Step 6: (опц.) интеграционные на реальном exiftool**

Run: `cargo test --bin lumina write:: -- --ignored`
Expected: PASS (`write_real_jpg_set_and_backup`, `write_overwrite_no_backup`, `strip_all_removes_pii_keeps_orientation`).

- [ ] **Step 7: Commit**

```bash
git add src/exif/write.rs src/app.rs
git commit -m "fix(exif): RAW пишется in-place (sidecar терял правки) + WriteMode/strip_all" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: тема + геометрия футера (тоггл/strip)

**Files:**
- Modify: `src/ui/theme.rs`
- Modify: `src/ui/layout.rs`
- Test: `src/ui/layout.rs` (модуль `tests`)

- [ ] **Step 1: Поле темы `danger_bg`**

В `src/ui/theme.rs` в `struct ThemePalette` (после `pub pending_mark: [f32; 4],`) добавить:
```rust
    /// Фон опасного действия (необратимый тоггл / «Стереть всё»).
    pub danger_bg: [f32; 4],
```
В `ThemePalette::dark()` (после `pending_mark: ...`) добавить:
```rust
            danger_bg: rgba(0xe0, 0x5a, 0x4a, 0.22),
```

- [ ] **Step 2: Написать падающий тест геометрии**

В `#[cfg(test)] mod tests` в `src/ui/layout.rs` добавить:
```rust
    #[test]
    fn popup_footer_toggle_and_strip_geometry() {
        let win = Vec2::new(1280.0, 800.0);
        let p = popup_layout(win, 1.0);
        let tog = popup_footer_toggle(&p, 1.0);
        let strip = popup_footer_strip(&p, 1.0);
        let (_save, cancel) = popup_footer_buttons(&p, 1.0);
        // тоггл у левого края футера
        assert!((tog.x - (p.footer.x + 12.0)).abs() < 0.5);
        // strip слева от cancel и правее тоггла (без наложения)
        assert!(strip.x < cancel.x);
        assert!(strip.x > tog.x + tog.w);
        // внутри футера по вертикали
        assert!(strip.y >= p.footer.y - 0.5 && strip.y + strip.h <= p.footer.y + p.footer.h + 0.5);
    }
```

- [ ] **Step 3: Запустить — не компилируется**

Run: `cargo test --bin lumina layout::tests::popup_footer_toggle`
Expected: ошибка — нет `popup_footer_toggle`/`popup_footer_strip`.

- [ ] **Step 4: Реализовать в `layout.rs`**

В `src/ui/layout.rs` после функции `popup_footer_buttons` добавить:
```rust
/// Кликабельная зона чекбокс-тоггла «Необратимо» (левый край футера): бокс + подпись.
pub fn popup_footer_toggle(p: &PopupLayout, scale: f32) -> Rect {
    let pad = theme::POPUP_PAD * scale;
    let h = 20.0 * scale;
    Rect { x: p.footer.x + pad, y: p.footer.y + (p.footer.h - h) * 0.5, w: 150.0 * scale, h }
}

/// Кнопка «Стереть всё» — слева от cancel (показывается только в необратимом режиме).
pub fn popup_footer_strip(p: &PopupLayout, scale: f32) -> Rect {
    let (_save, cancel) = popup_footer_buttons(p, scale);
    let bw = 96.0 * scale;
    let gap = 8.0 * scale;
    Rect { x: cancel.x - gap - bw, y: cancel.y, w: bw, h: cancel.h }
}
```

- [ ] **Step 5: Запустить тесты**

Run: `cargo test --bin lumina layout::tests`
Expected: PASS (прежние + новый).

- [ ] **Step 6: Commit**

```bash
git add src/ui/theme.rs src/ui/layout.rs
git commit -m "feat(ui): danger_bg + геометрия футер-тоггла и кнопки «Стереть всё»" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `scene` — ConfirmKind, поля состояния, отрисовка тоггла/strip/confirm

> Бинарь не скомпилируется после этой задачи (app.rs ещё использует `confirm_close`) — это ожидаемо, прогон в Task 5.

**Files:**
- Modify: `src/ui/scene.rs`

- [ ] **Step 1: Добавить `ConfirmKind` и обновить `PopupEditState`**

В `src/ui/scene.rs` рядом с `PendingOp` (перед `PopupEditState`) добавить:
```rust
/// Какой бар подтверждения показан в футере (вместо булева флага части 2).
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum ConfirmKind {
    None,
    CloseWithPending,
    OverwriteSave,
    StripAll,
}
```
В `struct PopupEditState<'a>` **заменить** поле `pub confirm_close: bool,` на:
```rust
    /// Активный бар подтверждения (закрытие/перезапись/стирание).
    pub confirm: ConfirmKind,
    /// Необратимый режим (тоггл): показывать кнопку «Стереть всё», danger-стиль.
    pub overwrite_mode: bool,
```

- [ ] **Step 2: Переписать блок футера в `build_popup`**

В `src/ui/scene.rs`, в конце `build_popup`, блок от
`// Футер: всегда виден. При confirm_close — бар подтверждения.` до строки `    cmds` (включая ветку `if edit.confirm_close { ... } else { ... }`) **заменить** на:
```rust
    // Футер: при confirm != None — бар подтверждения; иначе тоггл + действия.
    let (save, cancel) = crate::ui::layout::popup_footer_buttons(&p, scale);
    cmds.push(DrawCmd::Rect { rect: p.footer, color: theme.popup_group_bg, radius: 0.0, layer: RectLayer::Bg });
    let pad = theme::POPUP_PAD * scale;
    let btn_text = |cmds: &mut Vec<DrawCmd>, rect: Rect, text: &str, bg: [f32; 4], fg: [f32; 4]| {
        cmds.push(DrawCmd::Rect { rect, color: bg, radius: 6.0 * scale, layer: RectLayer::Bg });
        cmds.push(DrawCmd::Text { rect, text: text.to_string(), size: theme::POPUP_BTN_SIZE * scale, color: fg, align: Align::Center, clip: Some(rect) });
    };
    let label_rect = Rect { x: p.footer.x + pad, y: p.footer.y, w: cancel.x - p.footer.x - pad, h: p.footer.h };
    match edit.confirm {
        ConfirmKind::CloseWithPending => {
            cmds.push(DrawCmd::Text { rect: label_rect, text: "Несохранённые изменения".to_string(), size: theme::POPUP_BTN_SIZE * scale, color: theme.text_primary, align: Align::Left, clip: Some(label_rect) });
            let keep = Rect { x: cancel.x - (save.x - cancel.x), y: cancel.y, w: cancel.w, h: cancel.h };
            btn_text(&mut cmds, keep, "Продолжить", theme.button_hover, theme.text_primary);
            btn_text(&mut cmds, cancel, "Отменить", theme.button_hover, theme.text_primary);
            btn_text(&mut cmds, save, "Сохранить", theme.save_bg, theme.text_primary);
        }
        ConfirmKind::OverwriteSave => {
            cmds.push(DrawCmd::Text { rect: label_rect, text: "Необратимо, бэкапа не будет".to_string(), size: theme::POPUP_BTN_SIZE * scale, color: theme.danger, align: Align::Left, clip: Some(label_rect) });
            btn_text(&mut cmds, cancel, "Отмена", theme.button_hover, theme.text_primary);
            btn_text(&mut cmds, save, "Перезаписать", theme.danger, theme.text_primary);
        }
        ConfirmKind::StripAll => {
            cmds.push(DrawCmd::Text { rect: label_rect, text: "Удалить ВСЕ метаданные, необратимо".to_string(), size: theme::POPUP_BTN_SIZE * scale, color: theme.danger, align: Align::Left, clip: Some(label_rect) });
            btn_text(&mut cmds, cancel, "Отмена", theme.button_hover, theme.text_primary);
            btn_text(&mut cmds, save, "Стереть", theme.danger, theme.text_primary);
        }
        ConfirmKind::None => {
            // тоггл «Необратимо» (слева): бокс + подпись
            let tog = crate::ui::layout::popup_footer_toggle(&p, scale);
            let box_sz = 14.0 * scale;
            let box_r = Rect { x: tog.x, y: tog.y + (tog.h - box_sz) * 0.5, w: box_sz, h: box_sz };
            let box_bg = if edit.overwrite_mode { theme.danger } else { theme.popup_field_bg };
            cmds.push(DrawCmd::Rect { rect: box_r, color: box_bg, radius: 3.0 * scale, layer: RectLayer::Bg });
            let lbl = Rect { x: box_r.x + box_sz + 6.0 * scale, y: tog.y, w: tog.w - box_sz - 6.0 * scale, h: tog.h };
            let lbl_col = if edit.overwrite_mode { theme.danger } else { theme.text_secondary };
            cmds.push(DrawCmd::Text { rect: lbl, text: "Необратимо".to_string(), size: theme::POPUP_BTN_SIZE * scale, color: lbl_col, align: Align::Left, clip: Some(lbl) });
            // «Стереть всё» — только в необратимом режиме
            if edit.overwrite_mode {
                let strip = crate::ui::layout::popup_footer_strip(&p, scale);
                btn_text(&mut cmds, strip, "Стереть всё", theme.danger_bg, theme.danger);
            }
            btn_text(&mut cmds, cancel, "Отменить всё", theme.button_hover, theme.text_primary);
            let (sbg, sfg) = if edit.has_pending { (theme.save_bg, theme.text_primary) } else { (theme.button_hover, theme.text_secondary) };
            btn_text(&mut cmds, save, "Сохранить", sbg, sfg);
        }
    }
    cmds
```
> Прежний блок части 2 использовал `let pad = ...` уже выше по функции — здесь `pad` объявляется заново в области футера; если возникнет конфликт имён, переименуй локально (`let fpad = ...`). `theme::POPUP_PAD` уже импортирован через `theme::`.

- [ ] **Step 3: Обновить тестовый хелпер и тесты части 2**

В `#[cfg(test)] mod tests` в `src/ui/scene.rs`:

(а) В `empty_edit` **заменить** `confirm_close: false,` на:
```rust
            confirm: ConfirmKind::None,
            overwrite_mode: false,
```

(б) В тесте `popup_pending_set_marks_new_value` в инлайн-конструкторе `PopupEditState { ... }` **заменить** `confirm_close: false,` на `confirm: ConfirmKind::None, overwrite_mode: false,`.

(в) В тесте `popup_confirm_close_bar` строку `edit.confirm_close = true;` **заменить** на `edit.confirm = ConfirmKind::CloseWithPending;`.

- [ ] **Step 4: Добавить новые тесты отрисовки**

В тот же модуль добавить:
```rust
    #[test]
    fn popup_overwrite_toggle_shows_strip() {
        let win = glam::Vec2::new(1280.0, 800.0);
        let theme = ThemePalette::dark();
        let tags = sample_tags();
        let search = crate::ui::textedit::TextEdit::new();
        let editor = crate::ui::textedit::TextEdit::new();
        let mut edit = empty_edit(&editor);
        edit.overwrite_mode = true;
        let cmds = build_popup(win, 1.0, &theme, "a.jpg", Some(&tags), &search, 0.0, None, 0.0, None, true, true, &edit);
        assert!(cmds.iter().any(|c| matches!(c, DrawCmd::Text { text, .. } if text == "Стереть всё")));
        assert!(cmds.iter().any(|c| matches!(c, DrawCmd::Text { text, .. } if text == "Необратимо")));
    }

    #[test]
    fn popup_overwrite_save_confirm_bar() {
        let win = glam::Vec2::new(1280.0, 800.0);
        let theme = ThemePalette::dark();
        let tags = sample_tags();
        let search = crate::ui::textedit::TextEdit::new();
        let editor = crate::ui::textedit::TextEdit::new();
        let mut edit = empty_edit(&editor);
        edit.confirm = ConfirmKind::OverwriteSave;
        let cmds = build_popup(win, 1.0, &theme, "a.jpg", Some(&tags), &search, 0.0, None, 0.0, None, true, true, &edit);
        assert!(cmds.iter().any(|c| matches!(c, DrawCmd::Text { text, .. } if text == "Перезаписать")));
        assert!(cmds.iter().any(|c| matches!(c, DrawCmd::Text { text, .. } if text.contains("бэкапа"))));
    }

    #[test]
    fn popup_strip_confirm_bar() {
        let win = glam::Vec2::new(1280.0, 800.0);
        let theme = ThemePalette::dark();
        let tags = sample_tags();
        let search = crate::ui::textedit::TextEdit::new();
        let editor = crate::ui::textedit::TextEdit::new();
        let mut edit = empty_edit(&editor);
        edit.confirm = ConfirmKind::StripAll;
        let cmds = build_popup(win, 1.0, &theme, "a.jpg", Some(&tags), &search, 0.0, None, 0.0, None, true, true, &edit);
        assert!(cmds.iter().any(|c| matches!(c, DrawCmd::Text { text, .. } if text == "Стереть")));
        assert!(cmds.iter().any(|c| matches!(c, DrawCmd::Text { text, .. } if text.contains("ВСЕ метаданные"))));
    }
```

- [ ] **Step 5: Commit** (прогон тестов — в Task 5)

```bash
git add src/ui/scene.rs
git commit -m "feat(ui): ConfirmKind + тоггл «Необратимо»/«Стереть всё» + варианты confirm-бара (scene)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: `hit` — регионы тоггла/strip + обобщённые confirm-регионы

> Бинарь не скомпилируется после этой задачи — прогон в Task 5.

**Files:**
- Modify: `src/ui/hit.rs`

- [ ] **Step 1: Обновить enum `PopupRegion`**

В `src/ui/hit.rs` в `enum PopupRegion` **заменить** строки
```rust
    ConfirmSave,
    ConfirmDiscard,
    ConfirmKeep,
```
на:
```rust
    FooterToggle,
    FooterStrip,
    ConfirmPrimary,   // правая кнопка бара (Сохранить/Перезаписать/Стереть)
    ConfirmSecondary, // средняя/левая (Отменить/Отмена)
    ConfirmTertiary,  // третья (Продолжить) — только CloseWithPending
```
> Остальные варианты (`Close/Search/Body/Outside/RowEdit/RowDelete/GpsDeleteAll/FooterSave/FooterCancel`) сохраняются.

- [ ] **Step 2: Переписать `hit_popup_edit`**

В `src/ui/hit.rs` функцию `hit_popup_edit` **заменить целиком** на:
```rust
/// Хит-тест popup в режиме редактирования (часть 2/v0.4d): футер с тогглом/strip,
/// бар подтверждения (по `confirm`), действия строк, GPS-delete-all.
#[allow(clippy::too_many_arguments)]
pub fn hit_popup_edit(
    win: Vec2,
    scale: f32,
    cursor: Vec2,
    tags: &crate::exif::tags::ExifTags,
    filter: &str,
    scroll: f32,
    confirm: crate::ui::scene::ConfirmKind,
    overwrite_mode: bool,
) -> PopupRegion {
    use crate::ui::scene::ConfirmKind;
    let p = crate::ui::layout::popup_layout(win, scale);
    if p.close.contains(cursor) {
        return PopupRegion::Close;
    }
    if p.footer.contains(cursor) {
        let (save, cancel) = crate::ui::layout::popup_footer_buttons(&p, scale);
        if confirm != ConfirmKind::None {
            if save.contains(cursor) {
                return PopupRegion::ConfirmPrimary;
            }
            if cancel.contains(cursor) {
                return PopupRegion::ConfirmSecondary;
            }
            // третья кнопка (Продолжить) есть только у CloseWithPending
            if confirm == ConfirmKind::CloseWithPending {
                return PopupRegion::ConfirmTertiary;
            }
            return PopupRegion::Body;
        }
        if save.contains(cursor) {
            return PopupRegion::FooterSave;
        }
        if cancel.contains(cursor) {
            return PopupRegion::FooterCancel;
        }
        if overwrite_mode {
            let strip = crate::ui::layout::popup_footer_strip(&p, scale);
            if strip.contains(cursor) {
                return PopupRegion::FooterStrip;
            }
        }
        let tog = crate::ui::layout::popup_footer_toggle(&p, scale);
        if tog.contains(cursor) {
            return PopupRegion::FooterToggle;
        }
        return PopupRegion::Body;
    }
    if p.search.contains(cursor) {
        return PopupRegion::Search;
    }
    if p.body.contains(cursor) {
        let rows = crate::ui::scene::popup_rows(tags, filter, scale, scroll, p.body);
        for (i, r) in rows.iter().enumerate() {
            if cursor.y < r.y || cursor.y >= r.y + r.h {
                continue;
            }
            match r.kind {
                crate::ui::scene::PopupRowKind::Group if r.group == "GPS" => {
                    let (_e, del) = crate::ui::scene::popup_row_actions(r, p.body, scale);
                    if del.contains(cursor) {
                        return PopupRegion::GpsDeleteAll(i);
                    }
                }
                crate::ui::scene::PopupRowKind::Tag if r.editable => {
                    let (edit, del) = crate::ui::scene::popup_row_actions(r, p.body, scale);
                    if edit.contains(cursor) {
                        return PopupRegion::RowEdit(i);
                    }
                    if del.contains(cursor) {
                        return PopupRegion::RowDelete(i);
                    }
                }
                _ => {}
            }
        }
        return PopupRegion::Body;
    }
    PopupRegion::Outside
}
```

- [ ] **Step 3: Обновить тесты `hit`**

В `#[cfg(test)] mod tests` в `src/ui/hit.rs`:

(а) В `popup_footer_save_cancel_hit` — оба вызова `hit_popup_edit(win, 1.0, .., &tags, "", 0.0, false)` заменить хвост `false` на `crate::ui::scene::ConfirmKind::None, false`.

(б) В `popup_row_edit_delete_hit` — оба вызова: хвост `&tags, "", 0.0, false` → `&tags, "", 0.0, crate::ui::scene::ConfirmKind::None, false`.

(в) В `popup_close_search_body_outside` — все четыре вызова: хвост `&tags, "", 0.0, false` → `&tags, "", 0.0, crate::ui::scene::ConfirmKind::None, false`.

(г) Добавить новые тесты:
```rust
    #[test]
    fn popup_footer_toggle_and_strip_hit() {
        use crate::ui::scene::ConfirmKind;
        let win = Vec2::new(1280.0, 800.0);
        let p = crate::ui::layout::popup_layout(win, 1.0);
        let tags = crate::exif::tags::parse(r#"[{"SourceFile":"a","EXIF:Make":"X"}]"#);
        let tog = crate::ui::layout::popup_footer_toggle(&p, 1.0);
        let strip = crate::ui::layout::popup_footer_strip(&p, 1.0);
        let tc = Vec2::new(tog.x + tog.w * 0.5, tog.y + tog.h * 0.5);
        let sc = Vec2::new(strip.x + strip.w * 0.5, strip.y + strip.h * 0.5);
        // тоггл кликается всегда
        assert_eq!(hit_popup_edit(win, 1.0, tc, &tags, "", 0.0, ConfirmKind::None, false), PopupRegion::FooterToggle);
        // strip — только в необратимом режиме
        assert_eq!(hit_popup_edit(win, 1.0, sc, &tags, "", 0.0, ConfirmKind::None, true), PopupRegion::FooterStrip);
        assert_eq!(hit_popup_edit(win, 1.0, sc, &tags, "", 0.0, ConfirmKind::None, false), PopupRegion::Body);
    }

    #[test]
    fn popup_confirm_regions_hit() {
        use crate::ui::scene::ConfirmKind;
        let win = Vec2::new(1280.0, 800.0);
        let p = crate::ui::layout::popup_layout(win, 1.0);
        let (save, cancel) = crate::ui::layout::popup_footer_buttons(&p, 1.0);
        let tags = crate::exif::tags::parse(r#"[{"SourceFile":"a","EXIF:Make":"X"}]"#);
        let sc = Vec2::new(save.x + save.w * 0.5, save.y + save.h * 0.5);
        let cc = Vec2::new(cancel.x + cancel.w * 0.5, cancel.y + cancel.h * 0.5);
        assert_eq!(hit_popup_edit(win, 1.0, sc, &tags, "", 0.0, ConfirmKind::OverwriteSave, false), PopupRegion::ConfirmPrimary);
        assert_eq!(hit_popup_edit(win, 1.0, cc, &tags, "", 0.0, ConfirmKind::OverwriteSave, false), PopupRegion::ConfirmSecondary);
    }
```

- [ ] **Step 4: Commit** (прогон — в Task 5)

```bash
git add src/ui/hit.rs
git commit -m "feat(ui): хит-тест футер-тоггла/strip + обобщённые confirm-регионы" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: `app.rs` — состояние, поток, проброс; полный прогон

**Files:**
- Modify: `src/app.rs`

> Интеграционная задача. Реализатор ЧИТАЕТ `src/app.rs`, находит точки части 2 и адаптирует. После неё бинарь снова компилируется и тесты проходят.

- [ ] **Step 1: Поля состояния**

В `struct AppState` **заменить** строку `pub exif_confirm_close: bool,` на:
```rust
    pub exif_confirm: crate::ui::scene::ConfirmKind,
    pub exif_overwrite_mode: bool,
    pub exif_pending_strip_all: bool,
    pub exif_close_after_save: bool,
```
В `AppState::new()` **заменить** `exif_confirm_close: false,` на:
```rust
            exif_confirm: crate::ui::scene::ConfirmKind::None,
            exif_overwrite_mode: false,
            exif_pending_strip_all: false,
            exif_close_after_save: false,
```

- [ ] **Step 2: Сброс в `toggle_exif` (ветка открытия)**

В `toggle_exif`, в ветке открытия, строку `self.state.exif_confirm_close = false;` **заменить** на:
```rust
            self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
            self.state.exif_overwrite_mode = false;
            self.state.exif_pending_strip_all = false;
            self.state.exif_close_after_save = false;
```

- [ ] **Step 3: `exif_save(mode)` + `exif_strip_all`**

В `impl App` метод `exif_save` **заменить сигнатуру и тело rayon-вызова**:
- сигнатуру `fn exif_save(&mut self) {` → `fn exif_save(&mut self, mode: crate::exif::write::WriteMode) {`
- строку `let result = crate::exif::write::write_edits(&path, &edits, crate::exif::write::WriteMode::Backup).map_err(|e| e.to_string());`
  → `let result = crate::exif::write::write_edits(&path, &edits, mode).map_err(|e| e.to_string());`

Рядом добавить новый метод:
```rust
    /// Стереть все метаданные текущего файла (необратимо) на rayon.
    fn exif_strip_all(&mut self) {
        let Some(cat) = &self.state.catalog else { return };
        let path = cat.current_path().to_path_buf();
        self.state.exif_generation += 1;
        let generation = self.state.exif_generation;
        let proxy = self.proxy.clone();
        rayon::spawn(move || {
            let result = crate::exif::write::strip_all(&path).map_err(|e| e.to_string());
            let _ = proxy.send_event(UserEvent::ExifSaved { generation, result });
        });
    }
```

- [ ] **Step 4: Обновить успешную ветку `ExifSaved`**

В `fn user_event`, рукав `UserEvent::ExifSaved`, в ветке `Ok(()) => { ... }` блок
```rust
                        self.state.exif_pending.clear();
                        self.state.exif_pending_delete_gps = false;
                        self.state.exif_editing = None;
                        self.state.exif_error = None;
                        if self.state.exif_confirm_close {
                            self.state.exif_confirm_close = false;
                            self.toggle_exif(); // закрыть после сохранения по подтверждению
                            return;
                        }
```
**заменить** на:
```rust
                        self.state.exif_pending.clear();
                        self.state.exif_pending_delete_gps = false;
                        self.state.exif_pending_strip_all = false;
                        self.state.exif_editing = None;
                        self.state.exif_error = None;
                        self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
                        self.state.exif_overwrite_mode = false; // безопасность: следующее сохранение снова осознанное
                        if self.state.exif_close_after_save {
                            self.state.exif_close_after_save = false;
                            self.toggle_exif(); // закрыть после сохранения по подтверждению
                            return;
                        }
```

- [ ] **Step 5: Диспетчер кликов popup**

В обработчике `MouseInput`, в блоке `if self.state.ui.exif_open { ... }`:

(а) Вызов `hit::hit_popup_edit(win, self.state.scale, self.state.cursor, tags, &filter, self.state.exif_scroll, self.state.exif_confirm_close)` **заменить** на:
```rust
                            let region = hit::hit_popup_edit(win, self.state.scale, self.state.cursor, tags, &filter, self.state.exif_scroll, self.state.exif_confirm, self.state.exif_overwrite_mode);
```

(б) В `match region` **заменить** рукава `Close|Outside`, `FooterSave`, `FooterCancel` и три `Confirm*` на новый набор (остальные рукава `Search/Body/RowEdit/RowDelete/GpsDeleteAll` — без изменений):
```rust
                                hit::PopupRegion::Close | hit::PopupRegion::Outside => {
                                    if self.exif_has_pending() { self.state.exif_confirm = crate::ui::scene::ConfirmKind::CloseWithPending; }
                                    else { self.toggle_exif(); }
                                }
                                hit::PopupRegion::FooterToggle => {
                                    self.state.exif_overwrite_mode = !self.state.exif_overwrite_mode;
                                    if !self.state.exif_overwrite_mode { self.state.exif_pending_strip_all = false; }
                                }
                                hit::PopupRegion::FooterStrip => {
                                    self.state.exif_confirm = crate::ui::scene::ConfirmKind::StripAll;
                                }
                                hit::PopupRegion::FooterSave => {
                                    if self.state.exif_overwrite_mode && self.exif_has_pending() {
                                        self.state.exif_confirm = crate::ui::scene::ConfirmKind::OverwriteSave;
                                    } else {
                                        self.exif_save(crate::exif::write::WriteMode::Backup);
                                    }
                                }
                                hit::PopupRegion::FooterCancel => {
                                    self.state.exif_pending.clear();
                                    self.state.exif_pending_delete_gps = false;
                                    self.state.exif_editing = None;
                                }
                                hit::PopupRegion::ConfirmPrimary => {
                                    match self.state.exif_confirm {
                                        crate::ui::scene::ConfirmKind::CloseWithPending => {
                                            self.state.exif_close_after_save = true;
                                            if self.state.exif_overwrite_mode {
                                                self.state.exif_confirm = crate::ui::scene::ConfirmKind::OverwriteSave; // цепочка: подтвердить необратимость
                                            } else {
                                                self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
                                                self.exif_save(crate::exif::write::WriteMode::Backup);
                                            }
                                        }
                                        crate::ui::scene::ConfirmKind::OverwriteSave => {
                                            self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
                                            self.exif_save(crate::exif::write::WriteMode::Overwrite);
                                        }
                                        crate::ui::scene::ConfirmKind::StripAll => {
                                            self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
                                            self.exif_strip_all();
                                        }
                                        crate::ui::scene::ConfirmKind::None => {}
                                    }
                                }
                                hit::PopupRegion::ConfirmSecondary => {
                                    let was = self.state.exif_confirm;
                                    self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
                                    self.state.exif_close_after_save = false;
                                    if was == crate::ui::scene::ConfirmKind::CloseWithPending { self.toggle_exif(); }
                                }
                                hit::PopupRegion::ConfirmTertiary => {
                                    self.state.exif_confirm = crate::ui::scene::ConfirmKind::None; // Продолжить
                                }
```

- [ ] **Step 6: Клавиатура — Esc и confirm-бар**

В обработчике `KeyboardInput`, в блоке `if self.state.ui.exif_open { ... }`, ветку Esc (часть 2)
```rust
                        if let Key::Named(NamedKey::Escape) = event.logical_key.as_ref() {
                            if self.state.exif_editing.is_some() {
                                self.state.exif_editing = None;
                                self.state.exif_editor.clear();
                                if let Some(w) = &self.window { w.request_redraw(); }
                                return;
                            }
                            self.toggle_exif();
                            return;
                        }
```
**заменить** на:
```rust
                        if let Key::Named(NamedKey::Escape) = event.logical_key.as_ref() {
                            if self.state.exif_editing.is_some() {
                                self.state.exif_editing = None;
                                self.state.exif_editor.clear();
                                if let Some(w) = &self.window { w.request_redraw(); }
                                return;
                            }
                            if self.state.exif_confirm != crate::ui::scene::ConfirmKind::None {
                                self.state.exif_confirm = crate::ui::scene::ConfirmKind::None;
                                self.state.exif_close_after_save = false;
                                if let Some(w) = &self.window { w.request_redraw(); }
                                return;
                            }
                            self.toggle_exif();
                            return;
                        }
```

- [ ] **Step 7: Проброс в `build_popup` (RedrawRequested)**

В сборке `PopupEditState` (в `RedrawRequested`) **заменить** строку `confirm_close: self.state.exif_confirm_close,` на:
```rust
                            confirm: self.state.exif_confirm,
                            overwrite_mode: self.state.exif_overwrite_mode,
```

- [ ] **Step 8: Сборка и полный прогон**

Run: `cargo build`
Expected: компилируется без новых warnings.

Run: `cargo test --bin lumina`
Expected: PASS — все юнит-тесты (вкл. новые scene/hit/layout/write); `#[ignore]` пропущены.

Run: `cargo test --bin lumina write:: -- --ignored`
Expected: PASS (3 интеграционных).

- [ ] **Step 9: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): режимы записи EXIF (бэкап/необратимо) + «Стереть всё» + ConfirmKind" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Ручная приёмка и ROADMAP

- [ ] **Step 1: Приёмка** (на копии реальных файлов — оригиналы не трогать)

Run: `cargo run -- "путь\к\фото.jpg"` и отдельно на RAF.

Проверить:
- **Починка RAW:** правка тега на RAF → Сохранить (обычный режим) → значение перечиталось/осталось; рядом `*.RAF_original`; `.xmp` НЕ создаётся.
- **Обычный режим:** тоггл «Необратимо» выкл; Save оставляет `_original`.
- **Необратимый режим:** клик тоггла → красный; появляется «Стереть всё»; Save с правками → бар «Необратимо, бэкапа не будет — Перезаписать / Отмена» → запись без `_original`.
- **Стереть всё:** в необратимом режиме «Стереть всё» → бар «Удалить ВСЕ метаданные…» → после стирания персональные теги исчезли, фото отображается верно (Orientation/ICC целы).
- **Esc** на баре подтверждения отменяет бар (не закрывает popup).
- **Сброс тоггла:** после успешного сохранения тоггл снова выкл.
- **Закрытие с правками** в необратимом режиме → бар закрытия → «Сохранить» ведёт к подтверждению перезаписи (цепочка).

- [ ] **Step 2: ROADMAP**

В `ROADMAP.md` добавить строку v0.4d в таблицу (после v0.4c) и краткий блок прогресса: починка записи RAW (in-place вместо sidecar — баг v0.4b), режимы Backup/Overwrite, «Стереть всё» (`-all=` с сохранением Orientation/ICC), обобщённый `ConfirmKind`; число тестов. Отметить, что sidecar отвергнут осознанно (брейншторм), бандл exiftool — v0.5.

- [ ] **Step 3: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): v0.4d — починка записи EXIF + режимы бэкапа/перезаписи" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-review (выполнен при написании)

- **Покрытие спека:** RAW in-place + WriteMode + strip_args/edit_args → Task 1; danger_bg + геометрия футера → Task 2; ConfirmKind + отрисовка тоггла/strip/баров → Task 3; хит-тест регионов → Task 4; состояние/поток/strip_all/проброс → Task 5; приёмка + ROADMAP → Task 6. Sidecar отвергнут (не-цель) — не реализуется.
- **Согласованность типов:** `WriteMode` (Task 1) = `exif_save`/диспетчер (Task 5); `ConfirmKind` (Task 3, scene) = `PopupEditState.confirm` (Task 3), `hit_popup_edit` (Task 4), состояние `exif_confirm` + диспетчер (Task 5); `popup_footer_toggle`/`popup_footer_strip` (Task 2) = scene-отрисовка (Task 3) и hit (Task 4); регионы `FooterToggle`/`FooterStrip`/`ConfirmPrimary/Secondary/Tertiary` (Task 4) = разбор кликов (Task 5); `strip_all`/`exif_strip_all` (Tasks 1/5) = ConfirmPrimary→StripAll.
- **Гейтинг ввода:** перехваты части 2 сохранены; Esc приоритет: редактор → confirm-бар → закрытие (Task 5 Step 6). Тоггл/strip кликаются только при `confirm==None`; strip — только при `overwrite_mode` (Task 4).
- **Бинарный крейт:** Tasks 3/4 коммитятся без прогона (как scene в части 2); полный build+test — Task 5 Step 8. Отмечено явно.
- **Плейсхолдеров нет:** весь новый код приведён; для app-интеграции — точные замены строк части 2 с контекстом.
