# Lumina v0.4a — Трансформации — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Авто-ориентация фото по EXIF при открытии + ручной поворот/отражение (`R`/`Shift+R`/`H`/`V`/`Ctrl+Z`/кнопка) с сессионной памятью, без записи на диск.

**Architecture:** Два слоя поворота, разведённые по уровням. (1) EXIF-ориентация «запекается» в пиксели в слое декодера — каждый декодер возвращает upright-изображение. (2) Ручной поворот/отражение живёт в матрице вида (`view.rs`), мгновенно, без передекода; стартует с identity для каждого фото. Сессионная память ручных трансформаций — `HashMap<PathBuf, Transform>` в `AppState`.

**Tech Stack:** Rust, wgpu, glam (матрицы), `image` 0.25 (imageops для поворота пикселей), `kamadak-exif` (чтение тега Orientation), `rawler` (RAW). Чистое ядро `view.rs`/`exif` покрыто юнит-тестами.

Дизайн: `docs/superpowers/specs/2026-06-16-lumina-v0.4a-transforms-design.md`.

---

## Файловая структура

- **Создаётся** `src/exif/mod.rs` — `Orientation` (1..8), `from_exif_u16`, `read_orientation(path)`, `apply_to_image(img, orientation)`.
- **Изменяется** `src/view.rs` — `Transform`-тип, поворот/отражение в `matrix()`, `effective_dims`, rotation-aware `clamp_pan`/`fit_zoom`, API трансформаций.
- **Изменяется** `src/decoder/standard.rs` — применение EXIF-ориентации.
- **Изменяется** `src/decoder/raw.rs` — ориентация только в develop-ветке (Bayer-full).
- **Изменяется** `src/decoder/heic.rs` — приёмочный `#[ignore]`-тест upright (libheif уже ориентирует).
- **Изменяется** `src/app.rs` — сессионная память, диспетчинг клавиш, клик кнопки поворота, rotation-aware пересчёт fit.
- **Изменяется** `src/main.rs` — `mod exif;`.
- **Изменяется** `Cargo.toml` — `kamadak-exif`.
- **Изменяется** `ROADMAP.md` — отметка v0.4a.

---

## Task 1: `Transform`-тип и API ручных трансформаций в `view.rs`

Чистая логика, без GPU. Рефакторим существующий задел (`pub rotation: u8` и пр.) в самостоятельный тип `Transform` и добавляем методы. **Важно:** `rotation` хранится в **градусах** ∈ {0,90,180,270} как `u16` (в текущем коде `u8` — для 270 это переполнение, исправляем).

**Files:**
- Modify: `src/view.rs:7-20` (поля структуры), `src/view.rs:36-49` (`new`)
- Test: `src/view.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты**

В модуль `#[cfg(test)] mod tests` в `src/view.rs` добавить:

```rust
#[test]
fn transform_default_is_identity() {
    let t = Transform::default();
    assert!(t.is_identity());
    assert_eq!(t.rotation, 0);
}

#[test]
fn rotate_cw_cycles_through_four() {
    let mut v = ViewTransform::new();
    v.rotate_cw();
    assert_eq!(v.transform().rotation, 90);
    v.rotate_cw();
    assert_eq!(v.transform().rotation, 180);
    v.rotate_cw();
    assert_eq!(v.transform().rotation, 270);
    v.rotate_cw();
    assert_eq!(v.transform().rotation, 0); // 360 % 360
}

#[test]
fn rotate_ccw_wraps_to_270() {
    let mut v = ViewTransform::new();
    v.rotate_ccw();
    assert_eq!(v.transform().rotation, 270);
}

#[test]
fn flips_toggle() {
    let mut v = ViewTransform::new();
    v.flip_horizontal();
    assert!(v.transform().flip_h);
    v.flip_horizontal();
    assert!(!v.transform().flip_h);
    v.flip_vertical();
    assert!(v.transform().flip_v);
}

#[test]
fn reset_transform_restores_identity() {
    let mut v = ViewTransform::new();
    v.rotate_cw();
    v.flip_horizontal();
    v.reset_transform();
    assert!(v.transform().is_identity());
}

#[test]
fn set_transform_roundtrips() {
    let mut v = ViewTransform::new();
    let t = Transform { rotation: 180, flip_h: true, flip_v: false };
    v.set_transform(t);
    assert_eq!(v.transform(), t);
}
```

- [ ] **Step 2: Запустить тесты — убедиться, что не компилируются/падают**

Run: `cargo test --lib view::tests`
Expected: ошибка компиляции — нет типа `Transform`, методов `rotate_cw`/`transform`/… .

- [ ] **Step 3: Реализовать `Transform` и методы**

В начало `src/view.rs` (после `use`/констант) добавить тип:

```rust
/// Ручная трансформация отображения (поворот по часовой + отражения).
/// rotation — градусы ∈ {0, 90, 180, 270}.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Transform {
    pub rotation: u16,
    pub flip_h: bool,
    pub flip_v: bool,
}

impl Default for Transform {
    fn default() -> Self {
        Self { rotation: 0, flip_h: false, flip_v: false }
    }
}

impl Transform {
    pub fn is_identity(&self) -> bool {
        self.rotation == 0 && !self.flip_h && !self.flip_v
    }

    /// Эффективные размеры изображения на экране с учётом поворота:
    /// для 90°/270° ширина и высота меняются местами.
    pub fn effective_dims(&self, img: Vec2) -> Vec2 {
        if self.rotation == 90 || self.rotation == 270 {
            Vec2::new(img.y, img.x)
        } else {
            img
        }
    }
}
```

В структуре `ViewTransform` заменить три поля задела:

```rust
    // было: pub rotation: u8, pub flip_h: bool, pub flip_v: bool,
    transform: Transform,
```

В `ViewTransform::new()` заменить инициализацию этих полей на:

```rust
            transform: Transform::default(),
```

Добавить методы в `impl ViewTransform` (рядом с остальными аксессорами):

```rust
    pub fn transform(&self) -> Transform {
        self.transform
    }
    pub fn set_transform(&mut self, t: Transform) {
        self.transform = t;
    }
    pub fn rotate_cw(&mut self) {
        self.transform.rotation = (self.transform.rotation + 90) % 360;
    }
    pub fn rotate_ccw(&mut self) {
        self.transform.rotation = (self.transform.rotation + 270) % 360;
    }
    pub fn flip_horizontal(&mut self) {
        self.transform.flip_h = !self.transform.flip_h;
    }
    pub fn flip_vertical(&mut self) {
        self.transform.flip_v = !self.transform.flip_v;
    }
    pub fn reset_transform(&mut self) {
        self.transform = Transform::default();
    }
```

- [ ] **Step 4: Запустить тесты — убедиться, что проходят**

Run: `cargo test --lib view::tests`
Expected: PASS (включая новые 6 тестов + старые).

- [ ] **Step 5: Commit**

```bash
git add src/view.rs
git commit -m "feat(view): тип Transform и API ручных трансформаций (rotate/flip/reset)"
```

---

## Task 2: Поворот/отражение в матрице, `effective_dims`, rotation-aware `clamp_pan` и fit

Вшиваем `transform` в геометрию. Матрица переходит на центрированную формулу: `model = T(win/2 + pan) · R(rot) · S(±scaled) · T(-0.5)`, где знак масштаба кодирует отражение. Для rot=0, flip=none формула совпадает с текущей (origin = (win-scaled)/2 + pan). fit и clamp используют эффективные размеры.

**Files:**
- Modify: `src/view.rs` (`clamp_pan`, `matrix`, добавить `screen_quad`, `fit_zoom`-метод)
- Test: `src/view.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты**

```rust
#[test]
fn rotated_90_swaps_effective_dims() {
    let t = Transform { rotation: 90, flip_h: false, flip_v: false };
    let eff = t.effective_dims(Vec2::new(6000.0, 4000.0));
    assert_eq!(eff, Vec2::new(4000.0, 6000.0));
    let t0 = Transform::default();
    assert_eq!(t0.effective_dims(Vec2::new(6000.0, 4000.0)), Vec2::new(6000.0, 4000.0));
}

#[test]
fn fit_zoom_uses_effective_dims_after_rotation() {
    // окно 800×800, картинка 4000×2000 (ландшафт). fit без поворота = 800/4000 = 0.2.
    let win = Vec2::new(800.0, 800.0);
    let img = Vec2::new(4000.0, 2000.0);
    let mut v = ViewTransform::new();
    let fit0 = v.fit_zoom(win, img);
    assert!((fit0 - 0.2).abs() < 1e-6);
    // после поворота на 90° эффективный размер 2000×4000 → fit = 800/4000 = 0.2 (тот же,
    // т.к. ограничивает большая сторона), но для 3000×2000→ проверим реальный своп:
    v.rotate_cw();
    let img2 = Vec2::new(3000.0, 1000.0); // ландшафт 3:1
    let fit_land = ViewTransform::new().fit_zoom(win, img2);   // 800/3000
    let fit_rot = v.fit_zoom(win, img2);                       // эфф. 1000×3000 → 800/3000
    assert!((fit_land - 800.0 / 3000.0).abs() < 1e-6);
    assert!((fit_rot - 800.0 / 3000.0).abs() < 1e-6);
    // повёрнут портретно: ограничивает высота 3000 → тот же знаменатель, но проверим 90°-своп
    // на несимметричном кадре:
    let mut vr = ViewTransform::new();
    vr.rotate_cw();
    let asym = Vec2::new(1000.0, 4000.0); // портрет, эфф. после 90° = 4000×1000
    assert!((vr.fit_zoom(win, asym) - 800.0 / 4000.0).abs() < 1e-6);
    assert!((ViewTransform::new().fit_zoom(win, asym) - 800.0 / 4000.0).abs() < 1e-6);
}

#[test]
fn clamp_pan_uses_effective_dims_when_rotated() {
    // картинка 2000×800, окно 1000×1000. Без поворота: scaled.x=2000>1000 → limit_x=500; по Y вписана.
    // После 90°: эфф. 800×2000 → теперь по Y вылезает (limit_y=500), по X вписана.
    let win = Vec2::new(1000.0, 1000.0);
    let img = Vec2::new(2000.0, 800.0);
    let mut v = ViewTransform::new();
    v.set_zoom_immediate(1.0);
    v.rotate_cw();
    v.set_pan(Vec2::new(10_000.0, 10_000.0));
    v.clamp_pan(win, img);
    // эфф. 800×2000: limit_x = (800-1000)/2<0 → 0; limit_y = (2000-1000)/2 = 500
    assert_eq!(v.pan(), Vec2::new(0.0, 500.0));
}

#[test]
fn screen_quad_bbox_matches_effective_size() {
    // bounding box повёрнутого quad'а на экране = эффективные размеры × zoom.
    let win = Vec2::new(1000.0, 800.0);
    let img = Vec2::new(400.0, 200.0);
    let mut v = ViewTransform::new();
    v.set_zoom_immediate(2.0);
    v.rotate_cw(); // 90°: эфф. 200×400
    let q = v.screen_quad(win, img);
    let min_x = q.iter().map(|p| p.x).fold(f32::INFINITY, f32::min);
    let max_x = q.iter().map(|p| p.x).fold(f32::NEG_INFINITY, f32::max);
    let min_y = q.iter().map(|p| p.y).fold(f32::INFINITY, f32::min);
    let max_y = q.iter().map(|p| p.y).fold(f32::NEG_INFINITY, f32::max);
    assert!(((max_x - min_x) - 200.0 * 2.0).abs() < 0.5); // эфф.x × zoom
    assert!(((max_y - min_y) - 400.0 * 2.0).abs() < 0.5); // эфф.y × zoom
}

#[test]
fn matrix_finite_for_all_transforms() {
    let win = Vec2::new(1000.0, 800.0);
    let img = Vec2::new(400.0, 300.0);
    for rot in [0u16, 90, 180, 270] {
        for fh in [false, true] {
            for fv in [false, true] {
                let mut v = ViewTransform::new();
                v.set_transform(Transform { rotation: rot, flip_h: fh, flip_v: fv });
                let m = v.matrix(win, img);
                assert!(m.to_cols_array().iter().all(|c| c.is_finite()),
                    "rot={rot} fh={fh} fv={fv}");
            }
        }
    }
}
```

- [ ] **Step 2: Запустить — убедиться, что падают**

Run: `cargo test --lib view::tests`
Expected: ошибка компиляции — нет `fit_zoom`-метода и `screen_quad`; `clamp_pan`/`matrix` не учитывают поворот.

- [ ] **Step 3: Реализовать rotation-aware геометрию**

В `src/view.rs` заменить тело `clamp_pan` на использование эффективных размеров:

```rust
    pub fn clamp_pan(&mut self, win: Vec2, img: Vec2) {
        let eff = self.transform.effective_dims(img);
        let scaled = eff * self.zoom;
        let limit_x = ((scaled.x - win.x) * 0.5).max(0.0);
        let limit_y = ((scaled.y - win.y) * 0.5).max(0.0);
        self.pan.x = self.pan.x.clamp(-limit_x, limit_x);
        self.pan.y = self.pan.y.clamp(-limit_y, limit_y);
    }
```

Добавить rotation-aware метод fit (свободная функция `fit_zoom` остаётся без изменений — её используют тесты input и init):

```rust
    /// fit-zoom с учётом текущего поворота (эффективные размеры).
    pub fn fit_zoom(&self, win: Vec2, img: Vec2) -> f32 {
        fit_zoom(win, self.transform.effective_dims(img))
    }
```

Заменить тело `matrix` на центрированную формулу с поворотом и отражением, и вынести построение `model` так, чтобы его переиспользовал `screen_quad`:

```rust
    /// Модель-матрица: unit-quad [0,1]² → экранные пиксели, с центрированием,
    /// pan, zoom, поворотом и отражением. Картинка вращается вокруг своего центра.
    fn model_matrix(&self, win: Vec2, img: Vec2) -> Mat4 {
        let scaled = img * self.zoom; // исходные размеры × zoom (поворот добавляет геометрия)
        let center = win * 0.5 + self.pan;
        let fx = if self.transform.flip_h { -1.0 } else { 1.0 };
        let fy = if self.transform.flip_v { -1.0 } else { 1.0 };
        let angle = (self.transform.rotation as f32).to_radians();
        Mat4::from_translation(glam::Vec3::new(center.x, center.y, 0.0))
            * Mat4::from_rotation_z(angle)
            * Mat4::from_scale(glam::Vec3::new(scaled.x * fx, scaled.y * fy, 1.0))
            * Mat4::from_translation(glam::Vec3::new(-0.5, -0.5, 0.0))
    }

    pub fn matrix(&self, win: Vec2, img: Vec2) -> Mat4 {
        let proj = Mat4::orthographic_rh(0.0, win.x, win.y, 0.0, -1.0, 1.0);
        proj * self.model_matrix(win, img)
    }

    /// Четыре угла изображения в экранных пикселях (для тестов геометрии и хитов).
    pub fn screen_quad(&self, win: Vec2, img: Vec2) -> [Vec2; 4] {
        let m = self.model_matrix(win, img);
        let corners = [
            glam::Vec4::new(0.0, 0.0, 0.0, 1.0),
            glam::Vec4::new(1.0, 0.0, 0.0, 1.0),
            glam::Vec4::new(0.0, 1.0, 0.0, 1.0),
            glam::Vec4::new(1.0, 1.0, 0.0, 1.0),
        ];
        corners.map(|c| {
            let p = m * c;
            Vec2::new(p.x, p.y)
        })
    }
```

> Примечание: для `rotation=0, flip=none` `model_matrix` даёт тот же результат, что прежняя формула (центр = (win+pan), угол (0,0) → (win-scaled)/2+pan). Существующие тесты `zoom_at_cursor_keeps_point_fixed`, `pan_clamped_to_image_bounds`, `rescale_*` должны продолжать проходить без изменений.

- [ ] **Step 4: Запустить все тесты `view`**

Run: `cargo test --lib view::`
Expected: PASS (новые 5 + все прежние).

- [ ] **Step 5: Commit**

```bash
git add src/view.rs
git commit -m "feat(view): поворот/отражение в матрице, effective_dims, rotation-aware clamp_pan/fit"
```

---

## Task 3: Модуль `src/exif` — чтение и применение EXIF Orientation

Новый модуль: собственный enum `Orientation` (независим от kamadak/rawler), чтение тега из файла, применение к `image::DynamicImage` через `imageops`-методы.

**Files:**
- Create: `src/exif/mod.rs`
- Modify: `src/main.rs:1-10` (`mod exif;`)
- Modify: `Cargo.toml` (зависимость)
- Test: `src/exif/mod.rs` (модуль `tests`)

- [ ] **Step 1: Добавить зависимость и модуль**

В `Cargo.toml` в секцию `[dependencies]` добавить (lib называется `exif`):

```toml
kamadak-exif = "0.5"             # чтение EXIF Orientation (pure-Rust)
```

В `src/main.rs` добавить объявление модуля (в алфавитном порядке между `error` и `input`):

```rust
mod exif;
```

- [ ] **Step 2: Написать падающие тесты**

Создать `src/exif/mod.rs` с тестами (реализация — следующий шаг):

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn from_u16_maps_all_values() {
        assert_eq!(Orientation::from_exif_u16(1), Orientation::Normal);
        assert_eq!(Orientation::from_exif_u16(2), Orientation::FlipH);
        assert_eq!(Orientation::from_exif_u16(3), Orientation::Rotate180);
        assert_eq!(Orientation::from_exif_u16(4), Orientation::FlipV);
        assert_eq!(Orientation::from_exif_u16(5), Orientation::Transpose);
        assert_eq!(Orientation::from_exif_u16(6), Orientation::Rotate90);
        assert_eq!(Orientation::from_exif_u16(7), Orientation::Transverse);
        assert_eq!(Orientation::from_exif_u16(8), Orientation::Rotate270);
    }

    #[test]
    fn from_u16_unknown_is_normal() {
        assert_eq!(Orientation::from_exif_u16(0), Orientation::Normal);
        assert_eq!(Orientation::from_exif_u16(99), Orientation::Normal);
    }

    #[test]
    fn apply_rotate90_swaps_dims_and_moves_corner() {
        // 2×1 картинка: пиксель(0,0)=красный, пиксель(1,0)=синий.
        // Rotate90 (по часовой) → размер 1×2; верхний пиксель = бывший левый-нижний.
        let mut img = image::RgbaImage::new(2, 1);
        img.put_pixel(0, 0, image::Rgba([255, 0, 0, 255]));
        img.put_pixel(1, 0, image::Rgba([0, 0, 255, 255]));
        let dynimg = image::DynamicImage::ImageRgba8(img);
        let out = apply_to_image(dynimg, Orientation::Rotate90);
        assert_eq!((out.width(), out.height()), (1, 2));
    }

    #[test]
    fn apply_normal_is_noop_dims() {
        let dynimg = image::DynamicImage::ImageRgba8(image::RgbaImage::new(3, 2));
        let out = apply_to_image(dynimg, Orientation::Normal);
        assert_eq!((out.width(), out.height()), (3, 2));
    }

    #[test]
    fn read_orientation_missing_file_is_normal() {
        let o = read_orientation(std::path::Path::new("nonexistent_xyz.jpg"));
        assert_eq!(o, Orientation::Normal);
    }
}
```

- [ ] **Step 3: Запустить — убедиться, что падают**

Run: `cargo test --lib exif::`
Expected: ошибка компиляции — нет типов/функций.

- [ ] **Step 4: Реализовать модуль**

В начало `src/exif/mod.rs` (над `#[cfg(test)]`) добавить:

```rust
use image::DynamicImage;
use std::path::Path;

/// EXIF Orientation (TIFF tag 0x0112), значения 1..=8.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum Orientation {
    #[default]
    Normal,    // 1
    FlipH,     // 2
    Rotate180, // 3
    FlipV,     // 4
    Transpose, // 5
    Rotate90,  // 6  поворот по часовой 90°
    Transverse,// 7
    Rotate270, // 8  поворот по часовой 270° (= против часовой 90°)
}

impl Orientation {
    /// Маппинг сырого значения тега. Неизвестное/0 → Normal.
    pub fn from_exif_u16(v: u16) -> Self {
        match v {
            2 => Orientation::FlipH,
            3 => Orientation::Rotate180,
            4 => Orientation::FlipV,
            5 => Orientation::Transpose,
            6 => Orientation::Rotate90,
            7 => Orientation::Transverse,
            8 => Orientation::Rotate270,
            _ => Orientation::Normal, // 1 и всё неизвестное
        }
    }
}

/// Прочитать EXIF Orientation из файла. Любая ошибка (нет файла, нет EXIF,
/// нет тега, формат без EXIF) → Normal.
pub fn read_orientation(path: &Path) -> Orientation {
    let Ok(file) = std::fs::File::open(path) else {
        return Orientation::Normal;
    };
    let mut reader = std::io::BufReader::new(&file);
    let exif_reader = exif::Reader::new();
    let Ok(exif) = exif_reader.read_from_container(&mut reader) else {
        return Orientation::Normal;
    };
    match exif.get_field(exif::Tag::Orientation, exif::In::PRIMARY) {
        Some(f) => Orientation::from_exif_u16(f.value.get_uint(0).unwrap_or(1) as u16),
        None => Orientation::Normal,
    }
}

/// Применить ориентацию к изображению, приведя его к upright-виду.
/// Используется декодерами разово на потоке декода.
pub fn apply_to_image(img: DynamicImage, orientation: Orientation) -> DynamicImage {
    match orientation {
        Orientation::Normal => img,
        Orientation::FlipH => img.fliph(),
        Orientation::Rotate180 => img.rotate180(),
        Orientation::FlipV => img.flipv(),
        Orientation::Transpose => img.rotate90().fliph(),
        Orientation::Rotate90 => img.rotate90(),
        Orientation::Transverse => img.rotate270().fliph(),
        Orientation::Rotate270 => img.rotate270(),
    }
}
```

- [ ] **Step 5: Запустить тесты**

Run: `cargo test --lib exif::`
Expected: PASS (5 тестов).

- [ ] **Step 6: Commit**

```bash
git add Cargo.toml Cargo.lock src/main.rs src/exif/mod.rs
git commit -m "feat(exif): чтение EXIF Orientation (kamadak-exif) и применение к image"
```

---

## Task 4: `StandardDecoder` применяет EXIF-ориентацию

JPEG/TIFF/WebP крейт `image` сам не ориентирует — читаем тег и применяем перед `to_rgba8`.

**Files:**
- Modify: `src/decoder/standard.rs:18-24` (`decode_full`)
- Modify: `tests/make_fixtures.rs` (фикстура с тегом Orientation)
- Test: `src/decoder/standard.rs` (новый тест)

- [ ] **Step 1: Сгенерировать фикстуру JPEG с тегом Orientation=6**

> `image` не пишет EXIF, поэтому делаем фикстуру с реальным EXIF-блоком вручную. В `tests/make_fixtures.rs` добавить функцию, которая берёт уже сгенерированный `red_2x3.jpg` (2×3, портрет) и оборачивает минимальный JPEG APP1/EXIF с Orientation=6. Проще и надёжнее — добавить готовый бинарный образец в репозиторий.

В `tests/make_fixtures.rs` добавить генерацию ландшафтной фикстуры с тегом ориентации. Поскольку прямая запись EXIF из Rust громоздка, используем заранее заготовленный байтовый литерал минимального EXIF APP1-сегмента с Orientation=6 и вставляем его в JPEG:

```rust
/// Записать JPEG 3×2 (ландшафт) с EXIF Orientation=6 (показывать повёрнутым на 90° CW → портрет 2×3).
fn make_oriented_jpeg() {
    use std::io::Write;
    // 1) Базовый JPEG 3×2 через image.
    let mut buf: Vec<u8> = Vec::new();
    {
        let img = image::RgbaImage::from_pixel(3, 2, image::Rgba([10, 200, 30, 255]));
        let dynimg = image::DynamicImage::ImageRgba8(img);
        dynimg
            .write_to(&mut std::io::Cursor::new(&mut buf), image::ImageFormat::Jpeg)
            .expect("encode jpeg");
    }
    // 2) Минимальный EXIF APP1 с тегом Orientation(0x0112)=6, big-endian (MM).
    //    TIFF-заголовок + IFD0 с одним полем.
    let exif_payload: &[u8] = &[
        0x45, 0x78, 0x69, 0x66, 0x00, 0x00,             // "Exif\0\0"
        0x4D, 0x4D, 0x00, 0x2A, 0x00, 0x00, 0x00, 0x08, // TIFF header (MM, 42, offset=8)
        0x00, 0x01,                                     // IFD0: 1 entry
        0x01, 0x12, 0x00, 0x03, 0x00, 0x00, 0x00, 0x01, // tag=Orientation, type=SHORT, count=1
        0x00, 0x06, 0x00, 0x00,                         // value=6 (в старших 2 байтах SHORT)
        0x00, 0x00, 0x00, 0x00,                         // next IFD offset = 0
    ];
    // 3) Собрать APP1-сегмент: маркер FFE1 + длина (вкл. 2 байта длины) + payload.
    let app1_len = (exif_payload.len() + 2) as u16;
    let mut app1 = vec![0xFF, 0xE1, (app1_len >> 8) as u8, (app1_len & 0xFF) as u8];
    app1.extend_from_slice(exif_payload);
    // 4) Вставить APP1 сразу после SOI (первые 2 байта FFD8).
    let mut out = Vec::with_capacity(buf.len() + app1.len());
    out.extend_from_slice(&buf[..2]); // SOI
    out.extend_from_slice(&app1);
    out.extend_from_slice(&buf[2..]);
    let path = std::path::Path::new("tests/fixtures/oriented_3x2_or6.jpg");
    std::fs::File::create(path).unwrap().write_all(&out).unwrap();
    println!("written {path:?}");
}
```

Вызвать `make_oriented_jpeg();` в `main()` `make_fixtures.rs` рядом с существующими вызовами.

Run: `cargo run --bin make_fixtures`
Expected: создан `tests/fixtures/oriented_3x2_or6.jpg`; в логе строка `written ".../oriented_3x2_or6.jpg"`.

- [ ] **Step 2: Написать падающий тест**

В `src/decoder/standard.rs` добавить модуль тестов (его сейчас нет):

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    fn fixture(name: &str) -> PathBuf {
        PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures").join(name)
    }

    #[test]
    fn applies_orientation_6_rotates_landscape_to_portrait() {
        // исходный JPEG 3×2 (ландшафт) с Orientation=6 → после авто-ориентации 2×3 (портрет).
        let d = StandardDecoder;
        let img = d.decode_full(&fixture("oriented_3x2_or6.jpg")).unwrap();
        assert_eq!((img.width, img.height), (2, 3));
    }
}
```

- [ ] **Step 3: Запустить — убедиться, что падает**

Run: `cargo test --lib decoder::standard`
Expected: FAIL — сейчас вернётся 3×2 (ориентация не применена).

- [ ] **Step 4: Применить ориентацию в `decode_full`**

В `src/decoder/standard.rs` заменить тело `decode_full`:

```rust
    fn decode_full(&self, path: &Path) -> Result<DecodedImage> {
        let img = image::open(path)
            .map_err(|e| LuminaError::Decode(path.to_path_buf(), e))?;
        // EXIF Orientation: image сам не ориентирует — приводим к upright здесь.
        let orientation = crate::exif::read_orientation(path);
        let img = crate::exif::apply_to_image(img, orientation);
        let rgba = img.to_rgba8();
        let (width, height) = rgba.dimensions();
        Ok(DecodedImage { rgba: rgba.into_raw(), width, height })
    }
```

- [ ] **Step 5: Запустить тест**

Run: `cargo test --lib decoder::standard`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/decoder/standard.rs tests/make_fixtures.rs tests/fixtures/oriented_3x2_or6.jpg
git commit -m "feat(decoder): авто-ориентация JPEG/TIFF/WebP по EXIF Orientation"
```

---

## Task 5: `RawDecoder` применяет ориентацию в develop-ветке

Встроенный JPEG камеры (preview и non-Bayer full) уже в дисплейной ориентации — не трогаем. Развёрнутый Bayer (`RawDevelop`) выходит в ориентации сенсора — применяем EXIF-ориентацию из файла.

**Files:**
- Modify: `src/decoder/raw.rs:77-88` (Bayer-ветка `decode_full`)
- Test: `src/decoder/raw.rs` (новый `#[ignore]`-тест приёмки)

- [ ] **Step 1: Применить ориентацию к развёрнутому Bayer**

В `src/decoder/raw.rs` в `decode_full`, в Bayer-ветке, заменить финал на чтение и применение ориентации:

```rust
        // Байер (или fallback): полноценный develop.
        let raw = decoder
            .raw_image(&source, &Default::default(), false)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let intermediate = RawDevelop::default()
            .develop_intermediate(&raw)
            .map_err(|e| LuminaError::Raw(path.to_path_buf(), e.to_string()))?;
        let dynimg = intermediate
            .to_dynamic_image()
            .ok_or_else(|| LuminaError::Raw(path.to_path_buf(), "develop вернул None".into()))?;
        // rawler develop НЕ применяет ориентацию (а RawImage.orientation в 0.7.2 часто
        // захардкожен в Normal). Берём ориентацию из EXIF файла (TIFF-based RAW парсятся
        // kamadak-exif) и приводим развёрнутый кадр к upright.
        // Встроенный JPEG (preview/non-Bayer) уже ориентирован камерой — там это НЕ делаем.
        let orientation = crate::exif::read_orientation(path);
        let dynimg = crate::exif::apply_to_image(dynimg, orientation);
        Ok(dynamic_to_decoded(dynimg))
```

- [ ] **Step 2: Проверить, что юнит-тесты RAW не сломались**

Run: `cargo test --lib decoder::raw`
Expected: PASS (существующие не-ignore тесты `supports_*`, `*_is_bayer` проходят; реальные образцы помечены `#[ignore]`).

- [ ] **Step 3: Добавить приёмочный `#[ignore]`-тест**

В `src/decoder/raw.rs` модуль `tests` добавить:

```rust
    // Приёмка ориентации: реальный портретный Bayer-RAW (напр. NEF/ARW/CR2), снятый
    // вертикально, должен выйти портретным (height > width). Запускать вручную на образце.
    // cargo test raw_portrait_is_upright -- --ignored
    #[test]
    #[ignore]
    fn raw_portrait_is_upright() {
        let d = RawDecoder;
        let path = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"))
            .join("tests/fixtures/portrait_bayer.nef");
        let img = d.decode_full(&path).unwrap();
        assert!(img.height > img.width, "ожидался портрет: {}×{}", img.width, img.height);
    }
```

- [ ] **Step 4: Commit**

```bash
git add src/decoder/raw.rs
git commit -m "feat(decoder): ориентация развёрнутого Bayer-RAW по EXIF (develop-ветка)"
```

- [ ] **Step 5: Ручная приёмка (требует реальных файлов)**

Положить вертикально снятый Bayer-RAW в `tests/fixtures/portrait_bayer.nef` и запустить:

Run: `cargo test raw_portrait_is_upright -- --ignored`
Expected: PASS. Также вручную открыть в приложении портретные NEF/ARW/CR2 **и** Fuji RAF — убедиться, что ни один не повёрнут дважды (встроенный JPEG не перекручен). Если встроенный preview окажется НЕ ориентирован на какой-то камере — зафиксировать в ROADMAP как известное ограничение (не расширять охват в v0.4a).

---

## Task 6: `HeicDecoder` — приёмочный тест upright

libheif применяет `irot`/`imir` при декоде сам. Кода менять не нужно — фиксируем поведение тестом.

**Files:**
- Test: `src/decoder/heic.rs` (новый `#[ignore]`-тест)

- [ ] **Step 1: Добавить `#[ignore]`-тест**

В `src/decoder/heic.rs` модуль `tests` добавить:

```rust
    // Приёмка: портретный HEIC (снят вертикально) должен выйти портретным —
    // libheif применяет irot/imir сам, повторно НЕ ориентируем.
    // cargo test heic_portrait_is_upright -- --ignored
    #[test]
    #[ignore]
    fn heic_portrait_is_upright() {
        let d = HeicDecoder;
        let path = std::path::PathBuf::from(env!("CARGO_MANIFEST_DIR"))
            .join("tests/fixtures/portrait.heic");
        let img = d.decode_full(&path).unwrap();
        assert!(img.height > img.width, "ожидался портрет: {}×{}", img.width, img.height);
    }
```

- [ ] **Step 2: Сборка тестов компилируется**

Run: `cargo test --lib decoder::heic -- --list`
Expected: тест присутствует в списке; обычные тесты проходят, `heic_portrait_is_upright` помечен ignored.

- [ ] **Step 3: Commit**

```bash
git add src/decoder/heic.rs
git commit -m "test(decoder): приёмка upright HEIC (libheif ориентирует сам)"
```

---

## Task 7: Интеграция в `app.rs` — клавиши, кнопка, сессионная память

Связываем `view`-API с вводом и UI: `R`/`Shift+R`/`H`/`V`/`Ctrl+Z`, клик кнопки поворота, память `HashMap<PathBuf, Transform>`, rotation-aware пересчёт fit.

**Files:**
- Modify: `src/app.rs` (поля `AppState`, `new`, `load_current`, `open_file`, обработчики)

- [ ] **Step 1: Добавить поля состояния**

В `src/app.rs`:
- В `use`-блок добавить: `use std::collections::HashMap;` и `use crate::view::Transform;` (рядом с `use crate::view::ViewTransform;`).
- В `struct AppState` добавить поля:

```rust
    pub shift_down: bool,
    pub transforms: HashMap<PathBuf, Transform>,
```

- В `AppState::new()` добавить инициализацию:

```rust
            shift_down: false,
            transforms: HashMap::new(),
```

- [ ] **Step 2: Восстанавливать transform при загрузке, очищать при смене папки**

В `load_current` (после `let path = catalog.current_path().to_path_buf();`, перед `self.state.generation += 1;`) добавить восстановление сохранённой трансформации:

```rust
        let saved = self.state.transforms.get(&path).copied().unwrap_or_default();
        self.state.view.set_transform(saved);
```

В `open_file`, в ветке `Ok(cat)`, рядом с `self.state.thumbs.reset();` добавить очистку памяти (новая папка — новая сессия трансформаций):

```rust
                self.state.transforms.clear();
```

- [ ] **Step 3: Добавить хелпер применения трансформации**

В `impl App` добавить метод (рядом с `toggle_fit`):

```rust
    /// После ручной трансформации: запомнить по пути, пересчитать fit/clamp, перерисовать.
    fn after_transform_change(&mut self) {
        if let Some(cat) = &self.state.catalog {
            let path = cat.current_path().to_path_buf();
            self.state.transforms.insert(path, self.state.view.transform());
        }
        if let Some(r) = &self.renderer {
            if let Some(img) = r.image_size() {
                let win = r.viewer_size();
                let fit = self.state.view.fit_zoom(win, img);
                self.state.view.set_min_zoom(fit);
                if self.state.view.is_fit() {
                    self.state.view.set_zoom_immediate(fit);
                    self.state.view.set_pan(glam::Vec2::ZERO);
                }
                self.state.view.clamp_pan(win, img);
            }
        }
        if let Some(w) = &self.window {
            w.request_redraw();
        }
    }
```

- [ ] **Step 4: Оживить кнопку поворота**

В `src/app.rs` в обработчике `MouseInput`, заменить инертную ветку:

```rust
                                    hit::Region::ActionRotate => { return; }  // инертна (поворот — v0.4)
```

на:

```rust
                                    hit::Region::ActionRotate => {
                                        self.state.view.rotate_cw();
                                        self.after_transform_change();
                                        return;
                                    }
```

- [ ] **Step 5: Добавить клавиши трансформаций**

В обработчике `WindowEvent::KeyboardInput`, в ветке `ModifiersChanged` обновить shift:

```rust
            WindowEvent::ModifiersChanged(mods) => {
                self.state.ctrl_down = mods.state().control_key();
                self.state.shift_down = mods.state().shift_key();
            }
```

В блоке `if event.state.is_pressed()`, в `else if self.state.ctrl_down` добавить `Ctrl+Z`:

```rust
                            match c {
                                "0" => self.set_fit_view(),
                                "1" => self.set_actual_size(),
                                "z" | "Z" => {
                                    self.state.view.reset_transform();
                                    self.after_transform_change();
                                }
                                _ => {}
                            }
```

И добавить обработку `R`/`H`/`V` без Ctrl. После блока `if let Some(k) = nav { … } else if self.state.ctrl_down { … }` добавить ещё одну ветку (превратив в цепочку `else if`), либо отдельный блок до nav. Вставить **между** перехватом fullscreen и блоком `let nav = …`:

```rust
                    // Трансформации (без Ctrl): R/Shift+R поворот, H/V отражение.
                    if !self.state.ctrl_down {
                        if let Key::Character(c) = event.logical_key.as_ref() {
                            let handled = match c {
                                "r" | "R" => {
                                    if self.state.shift_down { self.state.view.rotate_ccw(); }
                                    else { self.state.view.rotate_cw(); }
                                    true
                                }
                                "h" | "H" => { self.state.view.flip_horizontal(); true }
                                "v" | "V" => { self.state.view.flip_vertical(); true }
                                _ => false,
                            };
                            if handled {
                                self.after_transform_change();
                                return;
                            }
                        }
                    }
```

> Примечание: `Key` уже импортирован в этом обработчике (`use winit::keyboard::{Key, NamedKey};`). Клавиша `f` (fullscreen) перехватывается выше и до этого блока не доходит.

- [ ] **Step 6: Rotation-aware fit в остальных точках пересчёта**

Чтобы повёрнутое фото оставалось вписанным при ресайзе/сворачивании панели, заменить вызовы свободной `crate::view::fit_zoom(...)` на rotation-aware метод в `src/app.rs`:

- В `WindowEvent::Resized` (строка ~469): `let z = self.state.view.fit_zoom(r.viewer_size(), img);`
- В `RedrawRequested`, блок пересчёта viewer (строка ~530): `let z = self.state.view.fit_zoom(r.viewer_size(), img);`
- В `user_event` инициализация первого кадра (строка ~394): `let z = self.state.view.fit_zoom(win, new_img);`
- В `toggle_fit` (строка ~195): `let fit = self.state.view.fit_zoom(win, img);`
- В `set_fit_view` (строка ~213): `let fit = self.state.view.fit_zoom(r.viewer_size(), img);`

> Все эти места ранее звали `crate::view::fit_zoom`; метод `ViewTransform::fit_zoom` учитывает текущий поворот, при identity результат идентичен. Borrow: сначала считается f32 (иммутабельный заём `&self.state.view`), затем мутация — конфликта нет, т.к. заём завершается до `set_*`.

- [ ] **Step 7: Сборка и полный прогон тестов**

Run: `cargo build`
Expected: компилируется без ошибок/ворнингов о неиспользуемых `shift_down`/`transforms`.

Run: `cargo test`
Expected: PASS — все юнит-тесты (прежние 74 + новые из Task 1–4), RAW/HEIC-приёмка `#[ignore]`.

- [ ] **Step 8: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): клавиши R/Shift+R/H/V/Ctrl+Z, кнопка поворота, сессионная память трансформаций"
```

---

## Task 8: Ручная приёмка и обновление ROADMAP

- [ ] **Step 1: Ручная приёмка на реальных файлах**

Run: `cargo run -- "путь\к\портретному_фото_с_телефона.jpg"`
Проверить:
- Портретный JPEG со смартфона открывается **вертикально** (а не «на боку») — авто-ориентация работает.
- `R` поворачивает по часовой, `Shift+R` против; фото остаётся вписанным в окно (fit пересчитывается).
- `H`/`V` отражают.
- `Ctrl+Z` возвращает к авто-ориентированному виду (identity ручного слоя).
- Кнопка поворота в bottom bar работает как `R`.
- Навигация next→prev в той же папке возвращает фото с сохранённым ручным поворотом; зум/ресайз/сворачивание панели сохраняют корректный fit.
- HEIC и портретные RAW (NEF/ARW/CR2/RAF) не повёрнуты дважды.

- [ ] **Step 2: Обновить ROADMAP**

В `ROADMAP.md` в таблице стадий строку v0.4 разбить/обновить: пометить «v0.4a — трансформации» как 🟢 со ссылками на дизайн и план; «v0.4b — EXIF popup/запись» оставить ⚪. Добавить раздел «Прогресс v0.4a (детально)» по образцу предыдущих фаз с перечнем сделанного и числом тестов.

- [ ] **Step 3: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): v0.4a (трансформации) готова; v0.4b (EXIF) — отдельной под-фазой"
```

---

## Self-review (выполнен при написании)

- **Покрытие спека:** §1 (два слоя) → Task 1–6; §2.1 exif → Task 3; §2.2 декодеры → Task 4/5/6; §2.3 view → Task 1/2; §2.4 память → Task 7; §3 ввод/UI → Task 7; §4 крайние случаи → Task 2 (fit/clamp), Task 7 (fit recompute); §5 тесты → распределены по задачам; §7 зависимость kamadak-exif → Task 3.
- **Плейсхолдеры:** нет — весь код приведён. Реальные файлы для `#[ignore]`-приёмки — ожидаемо внешние (RAW/HEIC).
- **Согласованность типов:** `Transform { rotation: u16, flip_h, flip_v }` един во `view.rs` и `app.rs`; `view::Transform` импортируется в `app.rs`; `exif::Orientation`/`apply_to_image`/`read_orientation` совпадают между Task 3 и вызовами в Task 4/5; метод `ViewTransform::fit_zoom(&self, win, img)` — единое имя в Task 2 и Task 7.
- **Коррекция спека:** `rotation` хранится как `u16` (в дизайне/SPEC указан `u8`, что для 270° переполнение) — отражено в Task 1.
```
