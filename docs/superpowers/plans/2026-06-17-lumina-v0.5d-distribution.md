# Lumina v0.5d — дистрибутив (portable .zip) + иконка — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать самодостаточный portable-дистрибутив Lumina (`lumina.exe` + DLL + exiftool + иконка),
запускаемый на чистой Windows без установки, vcpkg и внешнего exiftool.

**Architecture:** Иконка генерируется из мастера `app-icon.png` example-бинарём `make_icon` в
`assets/icon/{lumina.ico,lumina-256.png}` (коммитятся). `build.rs` (embed-resource) встраивает ICO в
exe; `app.rs` ставит иконку окна из PNG в рантайме. PowerShell-скрипт `packaging/package.ps1` собирает
release и пакует exe+DLL+exiftool+иконку+лицензии в zip. Код `exiftool_path()` уже совместим — не меняется.

**Tech Stack:** Rust, крейты `ico` (dev — генерация ICO), `embed-resource` (build — ресурс exe),
`image` (декод PNG для иконки окна), `winit` (`with_window_icon`), PowerShell (упаковка).

## Global Constraints

- Крейт `lumina` **бинарный**: тесты — `cargo test --bin lumina <фильтр>`, сборка — `cargo build`.
- Bash-инструмент НЕ видит cargo/env сборки. В начале КАЖДОГО bash-вызова с cargo выставлять inline:
  `export PATH="$HOME/.cargo/bin:$HOME/vcpkg/installed/x64-windows/bin:$PATH" VCPKG_ROOT="$HOME/vcpkg" LIBCLANG_PATH="C:\\Program Files\\LLVM\\bin" VCPKGRS_DYNAMIC=1`
- Комментарии и документация — на русском.
- Коммиты завершать трейлером: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- Работа в ветке `v0.5d-distribution` (от `main`).
- Дизайн: `docs/superpowers/specs/2026-06-17-lumina-v0.5d-distribution-design.md`.
- Цель — **только portable .zip**, **только x64**. Нативные DLL: `heif.dll`, `libde265.dll`,
  `libx265.dll`. Мастер иконки: `app-icon.png` (1254×1254). Инсталлятор/реестр — вне объёма (v0.5e).

## Файловая структура

- **Создаётся** `examples/make_icon.rs` — генератор `lumina.ico` + `lumina-256.png` из `app-icon.png`.
- **Создаётся** `assets/icon/lumina.ico`, `assets/icon/lumina-256.png` (артефакты, коммитятся).
- **Создаётся** `assets/icon/lumina.rc` — ресурс-скрипт (ICON).
- **Создаётся** `build.rs` — встраивание ресурса через `embed-resource` (только Windows).
- **Изменяется** `Cargo.toml` — `[build-dependencies] embed-resource`, `[dev-dependencies] ico`, `build = "build.rs"`.
- **Изменяется** `src/app.rs` — иконка окна в `resumed`.
- **Создаётся** `packaging/package.ps1` — сборка дистрибутива в zip.
- **Создаётся** `packaging/README.txt` — инструкция для дистрибутива.
- **Изменяется** `ROADMAP.md`.

---

## Task 1: Генератор иконки `make_icon`

**Files:**
- Modify: `Cargo.toml` (`[dev-dependencies] ico`)
- Create: `examples/make_icon.rs`
- Create: `assets/icon/lumina.ico`, `assets/icon/lumina-256.png` (вывод запуска)

**Interfaces:**
- Consumes: `app-icon.png` (корень репо); крейты `image`, `ico`.
- Produces: артефакты иконки в `assets/icon/`.

- [ ] **Step 1: Добавить dev-зависимость `ico`**

Run: `cargo add ico --dev`
Expected: в `Cargo.toml` секция `[dev-dependencies]` с `ico = "<latest>"` (на момент написания — 0.3+).

- [ ] **Step 2: Написать `examples/make_icon.rs`**

Создать `examples/make_icon.rs`:
```rust
//! Генератор иконки приложения из мастера app-icon.png.
//! Запуск: cargo run --example make_icon
//! Пишет assets/icon/lumina.ico (многоразмерный) и assets/icon/lumina-256.png (для окна).
use image::imageops::FilterType;

fn main() {
    let master = image::open("app-icon.png").expect("app-icon.png в корне репо");
    std::fs::create_dir_all("assets/icon").expect("создать assets/icon");

    // 256×256 PNG для иконки окна (winit)
    let png256 = master.resize_exact(256, 256, FilterType::Lanczos3).to_rgba8();
    png256.save("assets/icon/lumina-256.png").expect("сохранить lumina-256.png");

    // Многоразмерный ICO для ресурса exe
    let mut dir = ico::IconDir::new(ico::ResourceType::Icon);
    for size in [16u32, 24, 32, 48, 64, 128, 256] {
        let resized = master.resize_exact(size, size, FilterType::Lanczos3).to_rgba8();
        let (w, h) = (resized.width(), resized.height());
        let icon_image = ico::IconImage::from_rgba_data(w, h, resized.into_raw());
        dir.add_entry(ico::IconDirEntry::encode(&icon_image).expect("кодирование ICO-слоя"));
    }
    let file = std::fs::File::create("assets/icon/lumina.ico").expect("создать lumina.ico");
    dir.write(file).expect("записать lumina.ico");

    println!("готово: assets/icon/lumina.ico (7 размеров) + lumina-256.png");
}
```
> Если API крейта `ico` отличается (имена `IconDir`/`IconImage::from_rgba_data`/`IconDirEntry::encode`),
> сверить с `cargo doc -p ico --open` и адаптировать, сохранив результат: валидный многоразмерный .ico
> и 256-PNG.

- [ ] **Step 3: Сгенерировать артефакты**

Run: `cargo run --example make_icon`
Expected: вывод «готово…»; появились `assets/icon/lumina.ico` и `assets/icon/lumina-256.png`.

Run: `ls -la assets/icon/ && target/debug/exiftool.exe -ImageSize -- assets/icon/lumina-256.png`
Expected: `lumina-256.png` существует, размер 256×256; `lumina.ico` существует (ненулевой).

- [ ] **Step 4: Commit**

```bash
git add Cargo.toml Cargo.lock examples/make_icon.rs assets/icon/lumina.ico assets/icon/lumina-256.png
git commit -m "feat(icon): генератор make_icon + артефакты lumina.ico/lumina-256.png" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Встроить иконку exe (build.rs + ресурс)

**Files:**
- Create: `assets/icon/lumina.rc`
- Create: `build.rs`
- Modify: `Cargo.toml` (`build = "build.rs"`, `[build-dependencies] embed-resource`)

**Interfaces:**
- Consumes: `assets/icon/lumina.ico` (Task 1).
- Produces: иконка в `lumina.exe` (taskbar/проводник/Alt+Tab).

- [ ] **Step 1: Создать ресурс-скрипт**

Создать `assets/icon/lumina.rc`:
```
IDI_ICON1 ICON "lumina.ico"
```
> Путь к `lumina.ico` указан относительно самого `.rc` (они в одной папке). Первый ICON в ресурсах
> становится иконкой приложения по умолчанию.

- [ ] **Step 2: Добавить build-зависимость и `build.rs`**

Run: `cargo add embed-resource --build`
Expected: в `Cargo.toml` секция `[build-dependencies]` с `embed-resource = "<latest>"` (≈2.x).

В `Cargo.toml` в секцию `[package]` добавить строку (если её нет):
```toml
build = "build.rs"
```

Создать `build.rs` в корне репо:
```rust
//! Встраивание иконки приложения в exe (только Windows) через ресурс.
fn main() {
    #[cfg(windows)]
    {
        embed_resource::compile("assets/icon/lumina.rc", embed_resource::NONE);
    }
}
```
> Если установленная версия `embed-resource` имеет иную сигнатуру `compile` (в части версий —
> `compile(path)` без второго аргумента, в 2.x — `compile(path, embed_resource::NONE)`), сверить с
> `cargo doc -p embed-resource --open` и адаптировать вызов.

- [ ] **Step 3: Сборка**

Run: `cargo build`
Expected: компилируется; build.rs отрабатывает (rc компилируется через rc.exe из Windows SDK —
он уже есть, проект собирается под MSVC). Без новых warnings.

- [ ] **Step 4: Проверка иконки exe (ручная)**

Run: `cargo build --release` затем открыть `target/release/` в проводнике — у `lumina.exe`
кастомная иконка (кот). (В debug тоже встроится.)

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml Cargo.lock build.rs assets/icon/lumina.rc
git commit -m "feat(icon): встроить иконку в exe через embed-resource (build.rs)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Иконка окна в рантайме (winit)

**Files:**
- Modify: `src/app.rs` (`resumed` — `with_window_icon`)

**Interfaces:**
- Consumes: `assets/icon/lumina-256.png` (Task 1) через `include_bytes!`; `image`; `winit::window::Icon`.
- Produces: иконка окна (заголовок окна/Alt+Tab в рантайме).

- [ ] **Step 1: Добавить загрузчик иконки и применить к окну**

В `src/app.rs`, рядом со свободными функциями модуля (например, после `make_thumb`), добавить:
```rust
/// Иконка окна из встроенного PNG (256×256). None при ошибке декода.
fn window_icon() -> Option<winit::window::Icon> {
    let bytes = include_bytes!("../assets/icon/lumina-256.png");
    let img = image::load_from_memory(bytes).ok()?.to_rgba8();
    let (w, h) = (img.width(), img.height());
    winit::window::Icon::from_rgba(img.into_raw(), w, h).ok()
}
```

В `src/app.rs::resumed`, заменить построение `attrs`:
```rust
        let attrs = Window::default_attributes()
            .with_title("Lumina")
            .with_decorations(false)
            .with_inner_size(winit::dpi::LogicalSize::new(1280.0, 800.0));
```
на:
```rust
        let mut attrs = Window::default_attributes()
            .with_title("Lumina")
            .with_decorations(false)
            .with_inner_size(winit::dpi::LogicalSize::new(1280.0, 800.0));
        if let Some(icon) = window_icon() {
            attrs = attrs.with_window_icon(Some(icon));
        }
```

- [ ] **Step 2: Сборка и тесты**

Run: `cargo build`
Expected: компилируется без новых warnings.

Run: `cargo test --bin lumina`
Expected: PASS.

- [ ] **Step 3: Ручная проверка**

Run: `cargo run -- "tests/fixtures/red_2x3.jpg"`
Expected: у окна в taskbar/Alt+Tab кастомная иконка (кот).

- [ ] **Step 4: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): иконка окна из встроенного PNG (winit with_window_icon)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Скрипт упаковки дистрибутива

**Files:**
- Create: `packaging/package.ps1`
- Create: `packaging/README.txt`

**Interfaces:**
- Consumes: `target/release/lumina.exe`; DLL из vcpkg bin; standalone exiftool (путь — параметр).
- Produces: `dist/Lumina-<версия>-x64.zip`.

- [ ] **Step 1: Создать README дистрибутива**

Создать `packaging/README.txt`:
```
Lumina — просмотрщик фотографий (portable, x64)

Запуск: двойной клик по lumina.exe, либо перетащить фото на exe,
либо командой:  lumina.exe "путь\к\фото.jpg"

В комплекте (должны лежать рядом с lumina.exe):
  heif.dll, libde265.dll, libx265.dll   — поддержка HEIC/HEIF
  exiftool.exe + папка exiftool_files\  — чтение/запись EXIF-метаданных

Ничего устанавливать не нужно. Папку можно перемещать целиком.

Лицензии бандлимых компонентов — в папке licenses\.
```

- [ ] **Step 2: Создать `packaging/package.ps1`**

Создать `packaging/package.ps1`:
```powershell
# Сборка portable-дистрибутива Lumina в dist/Lumina-<версия>-x64.zip
# Запуск:  pwsh -File packaging/package.ps1 -ExiftoolDir "C:\path\to\exiftool-standalone"
param(
    [string]$VcpkgBin = "$env:VCPKG_ROOT\installed\x64-windows\bin",
    [Parameter(Mandatory=$true)][string]$ExiftoolDir  # папка с exiftool.exe + exiftool_files\
)
$ErrorActionPreference = "Stop"
$root = Split-Path -Parent $PSScriptRoot
Set-Location $root

# Версия из Cargo.toml
$version = (Select-String -Path "Cargo.toml" -Pattern '^version\s*=\s*"([^"]+)"').Matches[0].Groups[1].Value

Write-Host "cargo build --release..."
cargo build --release

$stage = Join-Path $root "dist\Lumina"
if (Test-Path $stage) { Remove-Item $stage -Recurse -Force }
New-Item -ItemType Directory -Path $stage | Out-Null
New-Item -ItemType Directory -Path (Join-Path $stage "licenses") | Out-Null

# 1) exe
Copy-Item "target\release\lumina.exe" $stage

# 2) нативные DLL
foreach ($dll in @("heif.dll","libde265.dll","libx265.dll")) {
    Copy-Item (Join-Path $VcpkgBin $dll) $stage
}

# 3) exiftool standalone (+ exiftool_files)
Copy-Item (Join-Path $ExiftoolDir "exiftool.exe") $stage
Copy-Item (Join-Path $ExiftoolDir "exiftool_files") $stage -Recurse

# 4) README
Copy-Item "packaging\README.txt" $stage

# 5) zip
$zip = Join-Path $root "dist\Lumina-$version-x64.zip"
if (Test-Path $zip) { Remove-Item $zip -Force }
Compress-Archive -Path (Join-Path $stage "*") -DestinationPath $zip
Write-Host "готово: $zip"
```
> Лицензии (`licenses\`) кладутся вручную/отдельным шагом — папка создаётся пустой как заготовка;
> наполнение файлами лицензий exiftool/libheif/libde265/libx265 — задача ручной финализации релиза.

- [ ] **Step 3: Прогнать упаковку (если доступен standalone exiftool)**

Подготовить папку со standalone exiftool (скачать «Windows Executable» с exiftool.org, распаковать;
переименовать `exiftool(-k).exe` → `exiftool.exe`, рядом — `exiftool_files\`).

Run (в PowerShell, не в bash-инструменте):
```
pwsh -File packaging/package.ps1 -ExiftoolDir "C:\tools\exiftool"
```
Expected: создан `dist/Lumina-0.1.0-x64.zip`, внутри `Lumina/` с exe, тремя DLL, exiftool.exe,
`exiftool_files/`, README.

- [ ] **Step 4: Commit**

```bash
git add packaging/package.ps1 packaging/README.txt
git commit -m "feat(packaging): portable-дистрибутив в zip (exe+DLL+exiftool+README)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: `.gitignore`, ROADMAP, приёмка

**Files:**
- Modify: `.gitignore` (исключить `dist/`)
- Modify: `ROADMAP.md`

- [ ] **Step 1: Игнорировать `dist/`**

В `.gitignore` добавить строку (если ещё нет):
```
/dist/
```

- [ ] **Step 2: ROADMAP**

В строке стадии **v0.5** добавить ссылку на дизайн дистрибутива. В блок прогресса v0.5 добавить:
```markdown
- [x] Иконка приложения: `make_icon` (example) из `app-icon.png` → `assets/icon/{lumina.ico,lumina-256.png}`;
      `build.rs`+embed-resource встраивает ICO в exe; `app.rs` ставит иконку окна (winit)
- [x] Portable-дистрибутив (`packaging/package.ps1`): `lumina.exe` + `heif/libde265/libx265.dll` +
      standalone `exiftool`(+`exiftool_files`) + README → `dist/Lumina-<версия>-x64.zip`; запуск на
      чистой Windows без vcpkg/PATH. Инсталлятор/ассоциации — отдельный цикл v0.5e
      ([дизайн](docs/superpowers/specs/2026-06-17-lumina-v0.5d-distribution-design.md))
```

- [ ] **Step 3: Полная сборка и тесты**

Run: `cargo build && cargo test --bin lumina`
Expected: PASS; иконка в exe; юнит-тесты зелёные.

- [ ] **Step 4: Ключевая ручная приёмка (на чистой машине / без vcpkg в PATH)**

Распаковать `dist/Lumina-<версия>-x64.zip` на машине без vcpkg и без установленного exiftool →
запустить `lumina.exe`:
- открыть JPG/PNG — показывается;
- открыть HEIC — декодируется (DLL подхватились из папки);
- EXIF popup — теги читаются (exiftool рядом); правка тега → сохраняется;
- иконка окна и в taskbar — кот.

- [ ] **Step 5: Commit**

```bash
git add .gitignore ROADMAP.md
git commit -m "docs(roadmap): v0.5d — дистрибутив portable.zip + иконка; ignore dist/" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-review (выполнен при написании)

- **Покрытие спека:** генерация иконки (ICO+PNG из мастера) → Task 1; встраивание в exe
  (build.rs/embed-resource/.rc) → Task 2; иконка окна winit → Task 3; бандл DLL+exiftool+скрипт zip →
  Task 4; .gitignore/ROADMAP/приёмка → Task 5. Границы (только zip, только x64, exiftool_path не
  меняется) отражены в Global Constraints и тексте задач.
- **Плейсхолдеров нет:** весь код/скрипт приведён. Версионно-зависимые API (`ico`, `embed-resource`)
  помечены инструкцией сверки с `cargo doc` при сохранении контракта. Папка `licenses/` и standalone
  exiftool — внешние артефакты, явно отмечены как ручная финализация (нельзя сгенерировать кодом).
- **Согласованность:** `assets/icon/lumina.ico` создаётся в Task 1, потребляется .rc (Task 2);
  `assets/icon/lumina-256.png` создаётся в Task 1, потребляется `include_bytes!` (Task 3); DLL-набор
  (`heif/libde265/libx265`) согласован между спекой и скриптом (Task 4). `exiftool_path()` не
  меняется — подтверждено осмотром `src/exif/read.rs`.
- **Риск:** точный набор транзитивных DLL для HEIC подтверждается финальной приёмкой на чистой машине
  (Task 5 Step 4); на текущей машине vcpkg bin содержит ровно три перечисленных.
