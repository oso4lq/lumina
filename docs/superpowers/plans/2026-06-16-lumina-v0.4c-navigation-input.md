# Lumina v0.4c — Навигация и эргономика ввода — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Раскладко-независимые шорткаты (по физической клавише), экранные стрелки ‹ › на hover для перелистывания (оконный режим), и свайп drag-жестом при размере по умолчанию.

**Architecture:** Логика в чистом ядре (`src/input/` — маппинг клавиш и порог свайпа; `src/ui/{layout,hit,scene}` — геометрия/хит/отрисовка стрелок; `src/view.rs` — транзиентное смещение свайпа в матрице), всё юнит-тестируется. `src/app.rs` — тонкий диспетчер, исполняющий решения ядра.

**Tech Stack:** Rust, winit (`KeyCode`/`PhysicalKey` — типы-значения, без событийного цикла), glam, wgpu (рендер не меняется). Дизайн: `docs/superpowers/specs/2026-06-16-lumina-v0.4c-navigation-input-design.md`.

---

## ВАЖНО для всех задач (окружение и команды)

- Крейт `lumina` **бинарный** (нет `[lib]`): `cargo test --lib` падает. Тесты гонять `cargo test --bin lumina <фильтр>`, сборка `cargo build`.
- Bash-инструмент НЕ видит cargo/env сборки. В начале КАЖДОГО bash-вызова с cargo выставлять inline:
  `export PATH="$HOME/.cargo/bin:$PATH" VCPKG_ROOT="$HOME/vcpkg" LIBCLANG_PATH="C:\\Program Files\\LLVM\\bin" VCPKGRS_DYNAMIC=1`
- Сообщения коммитов завершать трейлером: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- Работа в ветке `v0.4a-transforms` (текущая ветка разработки).

---

## Файловая структура

- **Изменяется** `src/input/mod.rs` — `Action`, `map_key(code,ctrl,shift)`, `NavDir`, `on_swipe_release(dx,viewer_w)`.
- **Изменяется** `src/view.rs` — поле `swipe_offset` + `set_swipe_offset`/`swipe_offset()` + учёт в `model_matrix`.
- **Изменяется** `src/ui/theme.rs` — `NAV_ARROW_W`, `NAV_CHEVRON_SIZE`.
- **Изменяется** `src/ui/layout.rs` — `nav_prev`/`nav_next` в `UiLayout`.
- **Изменяется** `src/ui/hit.rs` — регионы `NavPrev`/`NavNext`.
- **Изменяется** `src/ui/scene.rs` — глифы шевронов, отрисовка стрелок, поля `UiState` (`nav_alpha`/`can_prev`/`can_next`).
- **Изменяется** `src/app.rs` — диспетчинг по `physical_key`+`map_key` (`apply_action`); интеграция стрелок (анимация альфы, `can_*`, клики); свайп (`swiping`/`swipe_press_x`/`swipe_anim`, режимы press/move/release, лерп отката).
- **Изменяется** `ROADMAP.md` — отметка v0.4c.

---

## Task 1: `input::Action` и `map_key` (раскладко-независимые шорткаты)

Чистый маппинг физической клавиши в действие. `KeyCode` — тип-значение winit (без событийного цикла), тесты конструируют его напрямую.

**Files:**
- Modify: `src/input/mod.rs`
- Test: `src/input/mod.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты**

В `#[cfg(test)] mod tests` в `src/input/mod.rs` добавить:

```rust
    #[test]
    fn map_key_rotate_cw_and_ccw() {
        use winit::keyboard::KeyCode;
        assert_eq!(map_key(KeyCode::KeyR, false, false), Some(Action::RotateCw));
        assert_eq!(map_key(KeyCode::KeyR, false, true), Some(Action::RotateCcw));
    }

    #[test]
    fn map_key_flips_and_fullscreen() {
        use winit::keyboard::KeyCode;
        assert_eq!(map_key(KeyCode::KeyH, false, false), Some(Action::FlipH));
        assert_eq!(map_key(KeyCode::KeyV, false, false), Some(Action::FlipV));
        assert_eq!(map_key(KeyCode::KeyF, false, false), Some(Action::ToggleFullscreen));
        // F под Ctrl — не fullscreen
        assert_eq!(map_key(KeyCode::KeyF, true, false), None);
    }

    #[test]
    fn map_key_ctrl_combos() {
        use winit::keyboard::KeyCode;
        assert_eq!(map_key(KeyCode::KeyZ, true, false), Some(Action::ResetTransform));
        assert_eq!(map_key(KeyCode::KeyZ, false, false), None); // Z без Ctrl — ничего
        assert_eq!(map_key(KeyCode::Digit0, true, false), Some(Action::FitView));
        assert_eq!(map_key(KeyCode::Digit1, true, false), Some(Action::ActualSize));
    }

    #[test]
    fn map_key_unknown_is_none() {
        use winit::keyboard::KeyCode;
        assert_eq!(map_key(KeyCode::KeyA, false, false), None);
        assert_eq!(map_key(KeyCode::KeyR, true, false), None); // R под Ctrl — не поворот
    }
```

- [ ] **Step 2: Запустить — убедиться, что не компилируется**

Run: `cargo test --bin lumina input::tests`
Expected: ошибка компиляции — нет `Action`/`map_key`.

- [ ] **Step 3: Реализовать `Action` и `map_key`**

В `src/input/mod.rs` добавить (после существующих `use`, рядом с `NavKey`):

```rust
use winit::keyboard::KeyCode;

/// Действие, вызванное физической клавишей (раскладко-независимо).
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Action {
    RotateCw,
    RotateCcw,
    FlipH,
    FlipV,
    ResetTransform,
    ToggleFullscreen,
    FitView,
    ActualSize,
}

/// Маппинг физической клавиши + модификаторов в действие.
/// Матчинг по позиции клавиши (KeyCode), а не по символу — работает на любой
/// раскладке (кириллица К/Р/М/… и др.).
pub fn map_key(code: KeyCode, ctrl: bool, shift: bool) -> Option<Action> {
    match (code, ctrl) {
        (KeyCode::KeyR, false) => Some(if shift { Action::RotateCcw } else { Action::RotateCw }),
        (KeyCode::KeyH, false) => Some(Action::FlipH),
        (KeyCode::KeyV, false) => Some(Action::FlipV),
        (KeyCode::KeyF, false) => Some(Action::ToggleFullscreen),
        (KeyCode::KeyZ, true) => Some(Action::ResetTransform),
        (KeyCode::Digit0, true) => Some(Action::FitView),
        (KeyCode::Digit1, true) => Some(Action::ActualSize),
        _ => None,
    }
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test --bin lumina input::tests`
Expected: PASS (4 новых + старые `wheel_*`/`nav_keys_*`).

- [ ] **Step 5: Commit**

```bash
git add src/input/mod.rs
git commit -m "feat(input): Action + map_key — шорткаты по физической клавише (раскладко-независимо)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Диспетчинг клавиш в `app.rs` через `physical_key` + `map_key`

Переключаем обработчик `KeyboardInput` с матчинга символов на физические клавиши. NamedKey (F11/Escape/стрелки/Home/End) остаются по `logical_key` (они уже layout-independent).

**Files:**
- Modify: `src/app.rs` (метод-хелпер `apply_action`, ветка `WindowEvent::KeyboardInput`)

- [ ] **Step 1: Добавить хелпер `apply_action`**

В `impl App` (рядом с `after_transform_change`) добавить:

```rust
    /// Исполнить действие, замапленное с клавиатуры (см. input::map_key).
    fn apply_action(&mut self, action: crate::input::Action) {
        use crate::input::Action;
        match action {
            Action::RotateCw => { self.state.view.rotate_cw(); self.after_transform_change(); }
            Action::RotateCcw => { self.state.view.rotate_ccw(); self.after_transform_change(); }
            Action::FlipH => { self.state.view.flip_horizontal(); self.after_transform_change(); }
            Action::FlipV => { self.state.view.flip_vertical(); self.after_transform_change(); }
            Action::ResetTransform => { self.state.view.reset_transform(); self.after_transform_change(); }
            Action::ToggleFullscreen => self.toggle_fullscreen(),
            Action::FitView => self.set_fit_view(),
            Action::ActualSize => self.set_actual_size(),
        }
    }
```

- [ ] **Step 2: Переписать ветку `KeyboardInput`**

В `src/app.rs::window_event` заменить весь блок `WindowEvent::KeyboardInput { event, .. } => { ... }` на:

```rust
            WindowEvent::KeyboardInput { event, .. } => {
                use winit::keyboard::{Key, NamedKey, PhysicalKey};
                if event.state.is_pressed() {
                    // NamedKey-перехваты (сами по себе раскладко-независимы).
                    match event.logical_key.as_ref() {
                        Key::Named(NamedKey::F11) => { self.toggle_fullscreen(); return; }
                        Key::Named(NamedKey::Escape) => {
                            if self.state.ui.fullscreen { self.toggle_fullscreen(); }
                            return;
                        }
                        _ => {}
                    }
                    // Навигация стрелками/Home/End.
                    let nav = match event.logical_key.as_ref() {
                        Key::Named(NamedKey::ArrowRight) => Some(crate::input::NavKey::Next),
                        Key::Named(NamedKey::ArrowLeft) => Some(crate::input::NavKey::Prev),
                        Key::Named(NamedKey::Home) => Some(crate::input::NavKey::First),
                        Key::Named(NamedKey::End) => Some(crate::input::NavKey::Last),
                        _ => None,
                    };
                    if let Some(k) = nav {
                        if let Some(n) = crate::input::on_nav_key(k).navigate {
                            self.navigate(n);
                        }
                        return;
                    }
                    // Действия по ФИЗИЧЕСКОЙ клавише (раскладко-независимо): R/H/V/F/Ctrl+Z/Ctrl+0/Ctrl+1.
                    if let PhysicalKey::Code(code) = event.physical_key {
                        if let Some(action) = crate::input::map_key(code, self.state.ctrl_down, self.state.shift_down) {
                            self.apply_action(action);
                            return;
                        }
                    }
                }
            }
```

> Примечание: старый блок матчил `Key::Character("f"/"r"/"h"/"v"/"z"/"0"/"1")` — он полностью заменён. `ModifiersChanged` (ctrl/shift) НЕ трогаем — он уже есть ниже.

- [ ] **Step 3: Сборка и ручная компиляция-проверка**

Run: `cargo build`
Expected: компилируется без ошибок; нет предупреждений о неиспользуемых импортах (`Key`/`NamedKey`/`PhysicalKey` все задействованы).

- [ ] **Step 4: Полный прогон тестов (регрессия)**

Run: `cargo test --bin lumina`
Expected: PASS — все прежние тесты + Task 1.

- [ ] **Step 5: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): диспетчинг шорткатов по физической клавише через input::map_key" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `input::NavDir` и `on_swipe_release` (порог свайпа)

Чистая логика решения свайпа: смещение `dx` и ширина viewer → направление навигации либо None.

**Files:**
- Modify: `src/input/mod.rs`
- Test: `src/input/mod.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты**

В `#[cfg(test)] mod tests` добавить:

```rust
    #[test]
    fn swipe_past_threshold_navigates() {
        // порог = 15% ширины. viewer_w=1000 → порог 150.
        assert_eq!(on_swipe_release(-200.0, 1000.0), Some(NavDir::Next)); // тянули влево → следующее
        assert_eq!(on_swipe_release(200.0, 1000.0), Some(NavDir::Prev));  // тянули вправо → предыдущее
    }

    #[test]
    fn swipe_below_threshold_is_none() {
        assert_eq!(on_swipe_release(100.0, 1000.0), None);
        assert_eq!(on_swipe_release(-149.0, 1000.0), None);
    }

    #[test]
    fn swipe_zero_width_is_none() {
        assert_eq!(on_swipe_release(500.0, 0.0), None);
    }
```

- [ ] **Step 2: Запустить — убедиться, что не компилируется**

Run: `cargo test --bin lumina input::tests`
Expected: ошибка компиляции — нет `NavDir`/`on_swipe_release`.

- [ ] **Step 3: Реализовать**

В `src/input/mod.rs` добавить:

```rust
/// Направление перелистывания свайпом.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NavDir {
    Prev,
    Next,
}

/// Доля ширины viewer, после которой свайп считается перелистыванием.
pub const SWIPE_THRESHOLD_FRAC: f32 = 0.15;

/// Решение свайпа по горизонтальному смещению `dx` и ширине viewer.
/// Тянули влево (dx < -порог) → Next; вправо (dx > порог) → Prev; иначе None.
pub fn on_swipe_release(dx: f32, viewer_w: f32) -> Option<NavDir> {
    if viewer_w <= 0.0 {
        return None;
    }
    let threshold = SWIPE_THRESHOLD_FRAC * viewer_w;
    if dx <= -threshold {
        Some(NavDir::Next)
    } else if dx >= threshold {
        Some(NavDir::Prev)
    } else {
        None
    }
}
```

- [ ] **Step 4: Запустить тесты**

Run: `cargo test --bin lumina input::tests`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/input/mod.rs
git commit -m "feat(input): NavDir + on_swipe_release (порог перелистывания свайпом)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: `swipe_offset` в `view.rs` (транзиентное смещение фото)

Единственное хранилище смещения свайпа — в `ViewTransform`, входит в `center.x` матрицы, не клампится и переживает ежекадровый fit-сброс pan.

**Files:**
- Modify: `src/view.rs`
- Test: `src/view.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающий тест**

В `#[cfg(test)] mod tests` в `src/view.rs` добавить:

```rust
    #[test]
    fn swipe_offset_shifts_quad_horizontally() {
        let win = Vec2::new(1000.0, 800.0);
        let img = Vec2::new(400.0, 200.0);
        let mut v = ViewTransform::new();
        v.set_zoom_immediate(1.0);
        let q0 = v.screen_quad(win, img);
        v.set_swipe_offset(123.0);
        assert_eq!(v.swipe_offset(), 123.0);
        let q1 = v.screen_quad(win, img);
        for i in 0..4 {
            assert!((q1[i].x - q0[i].x - 123.0).abs() < 1e-3, "угол {i} по X");
            assert!((q1[i].y - q0[i].y).abs() < 1e-3, "угол {i} по Y");
        }
    }
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `cargo test --bin lumina view::tests::swipe_offset_shifts_quad_horizontally`
Expected: ошибка компиляции — нет `set_swipe_offset`/`swipe_offset`.

- [ ] **Step 3: Реализовать**

В `src/view.rs`:

1) В структуру `ViewTransform` добавить поле (после `transform: Transform,`):
```rust
    /// Транзиентное горизонтальное смещение фото (свайп-перелистывание).
    /// Не клампится и не сбрасывается fit-логикой; управляется извне.
    swipe_offset: f32,
```

2) В `ViewTransform::new()` добавить инициализацию (после `transform: Transform::default(),`):
```rust
            swipe_offset: 0.0,
```

3) Добавить аксессоры (рядом с `transform()`/`set_transform()`):
```rust
    pub fn swipe_offset(&self) -> f32 {
        self.swipe_offset
    }

    pub fn set_swipe_offset(&mut self, dx: f32) {
        self.swipe_offset = dx;
    }
```

4) В `model_matrix` учесть смещение — заменить строку `let center = win * 0.5 + self.pan;` на:
```rust
        let center = win * 0.5 + self.pan + Vec2::new(self.swipe_offset, 0.0);
```

- [ ] **Step 4: Запустить тесты `view`**

Run: `cargo test --bin lumina view::`
Expected: PASS (новый + все прежние; `swipe_offset=0` по умолчанию → старые тесты матрицы не меняются).

- [ ] **Step 5: Commit**

```bash
git add src/view.rs
git commit -m "feat(view): транзиентное swipe_offset в матрице (горизонтальное смещение фото)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Геометрия стрелок в `layout.rs` + константы темы

Тонкие вертикальные полосы у краёв viewer; нулевые в fullscreen.

**Files:**
- Modify: `src/ui/theme.rs` (константы)
- Modify: `src/ui/layout.rs` (`UiLayout` + `compute`)
- Test: `src/ui/layout.rs` (модуль `tests`)

- [ ] **Step 1: Добавить константы темы**

В `src/ui/theme.rs` (после `ACTIONS_WIDTH`) добавить:

```rust
/// Ширина (толщина) экранной стрелки навигации — тонкая полоса у края viewer (логические px).
pub const NAV_ARROW_W: f32 = 44.0;
/// Кегль глифа-шеврона стрелки (логические px).
pub const NAV_CHEVRON_SIZE: f32 = 20.0;
```

- [ ] **Step 2: Написать падающие тесты**

В `#[cfg(test)] mod tests` в `src/ui/layout.rs` добавить:

```rust
    #[test]
    fn nav_arrows_geometry_windowed() {
        let l = compute(Vec2::new(1280.0, 800.0), 1.0, 1.0, false);
        assert_eq!(l.nav_prev.x, 0.0);
        assert_eq!(l.nav_prev.w, 44.0);
        assert_eq!(l.nav_prev.y, l.viewer.y);
        assert_eq!(l.nav_prev.h, l.viewer.h);
        assert_eq!(l.nav_next.x, 1280.0 - 44.0);
        assert_eq!(l.nav_next.w, 44.0);
        assert_eq!(l.nav_next.y, l.viewer.y);
        assert_eq!(l.nav_next.h, l.viewer.h);
    }

    #[test]
    fn nav_arrows_zero_in_fullscreen() {
        let l = compute(Vec2::new(1280.0, 800.0), 1.0, 1.0, true);
        assert_eq!(l.nav_prev, ZERO);
        assert_eq!(l.nav_next, ZERO);
    }
```

- [ ] **Step 3: Запустить — убедиться, что не компилируется**

Run: `cargo test --bin lumina layout::tests`
Expected: ошибка компиляции — нет полей `nav_prev`/`nav_next`.

- [ ] **Step 4: Реализовать**

В `src/ui/layout.rs`:

1) В `struct UiLayout` добавить поля (после `btn_fs_exit: Rect,`):
```rust
    /// Тонкие полосы навигации у краёв viewer (нулевые в fullscreen).
    pub nav_prev: Rect,
    pub nav_next: Rect,
```

2) В fullscreen-ветке `compute` (в литерале `UiLayout { ... }`, после `btn_fs_exit: ...`) добавить:
```rust
            nav_prev: ZERO,
            nav_next: ZERO,
```

3) В оконной ветке `compute` — перед финальным `UiLayout { ... }` посчитать полосы (после строки с `viewer`):
```rust
    let nav_w = theme::NAV_ARROW_W * scale;
    let nav_prev = Rect { x: 0.0, y: viewer.y, w: nav_w, h: viewer.h };
    let nav_next = Rect { x: win.x - nav_w, y: viewer.y, w: nav_w, h: viewer.h };
```
и добавить их в литерал `UiLayout { ... }` (после `btn_fs_play: ZERO, btn_fs_exit: ZERO,`):
```rust
        nav_prev, nav_next,
```

- [ ] **Step 5: Запустить тесты `layout`**

Run: `cargo test --bin lumina layout::`
Expected: PASS (2 новых + все прежние).

- [ ] **Step 6: Commit**

```bash
git add src/ui/theme.rs src/ui/layout.rs
git commit -m "feat(ui): геометрия экранных стрелок навигации (тонкие полосы у краёв viewer)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Регионы `NavPrev`/`NavNext` в `hit.rs`

Хит-тест полос; крайние 6px остаются под resize (приоритет).

**Files:**
- Modify: `src/ui/hit.rs`
- Test: `src/ui/hit.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты**

В `#[cfg(test)] mod tests` в `src/ui/hit.rs` добавить:

```rust
    #[test]
    fn nav_prev_region() {
        let win = Vec2::new(1280.0, 800.0);
        let l = compute(win, 1.0, 1.0, false);
        // левая полоса, ниже titlebar, не в крайних 6px ресайза
        assert_eq!(hit(&l, win, Vec2::new(20.0, 400.0), 1.0), Region::NavPrev);
    }

    #[test]
    fn nav_next_region() {
        let win = Vec2::new(1280.0, 800.0);
        let l = compute(win, 1.0, 1.0, false);
        assert_eq!(hit(&l, win, Vec2::new(1280.0 - 20.0, 400.0), 1.0), Region::NavNext);
    }

    #[test]
    fn resize_edge_priority_over_nav() {
        let win = Vec2::new(1280.0, 800.0);
        let l = compute(win, 1.0, 1.0, false);
        // крайние 6px → ресайз, не NavPrev
        assert_eq!(hit(&l, win, Vec2::new(2.0, 400.0), 1.0), Region::Resize(Edge::Left));
    }
```

- [ ] **Step 2: Запустить — убедиться, что не компилируется**

Run: `cargo test --bin lumina hit::tests`
Expected: ошибка компиляции — нет `Region::NavPrev`/`NavNext`.

- [ ] **Step 3: Реализовать**

В `src/ui/hit.rs`:

1) В `enum Region` добавить варианты (после `SlideshowPlay,`):
```rust
    NavPrev,
    NavNext,
```

2) В функции `hit`, перед финальным `Region::None` (после блока `if layout.carousel.contains(cursor) { ... }`) добавить:
```rust
    if layout.nav_prev.contains(cursor) {
        return Region::NavPrev;
    }
    if layout.nav_next.contains(cursor) {
        return Region::NavNext;
    }
```

> Края/углы ресайза проверяются в начале `hit` и имеют приоритет — поэтому крайние 6px дают `Resize`, а полоса навигации срабатывает чуть внутри.

- [ ] **Step 4: Запустить тесты `hit`**

Run: `cargo test --bin lumina hit::`
Expected: PASS (3 новых + все прежние, включая `viewer_is_none` — центр окна не в полосах).

- [ ] **Step 5: Commit**

```bash
git add src/ui/hit.rs
git commit -m "feat(ui): хит-регионы NavPrev/NavNext (ресайз сохраняет приоритет у краёв)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Отрисовка стрелок и поля `UiState` в `scene.rs`

Шевроны Tabler, проявление по `nav_alpha`, инертность по `can_prev`/`can_next`.

**Files:**
- Modify: `src/ui/scene.rs`
- Test: `src/ui/scene.rs` (модуль `tests`)

- [ ] **Step 1: Написать падающие тесты**

В `#[cfg(test)] mod tests` в `src/ui/scene.rs` добавить:

```rust
    #[test]
    fn nav_arrow_shown_on_hover_when_can_navigate() {
        let cmds = fixture(|s| { s.can_next = true; s.nav_alpha = [0.0, 1.0]; });
        let has_right = cmds.iter().any(|c| matches!(c, DrawCmd::Icon { glyph, .. } if *glyph == GLYPH_CHEVRON_RIGHT));
        assert!(has_right);
    }

    #[test]
    fn nav_arrow_hidden_when_cannot_navigate() {
        let cmds = fixture(|s| { s.can_next = false; s.nav_alpha = [0.0, 1.0]; });
        let has_right = cmds.iter().any(|c| matches!(c, DrawCmd::Icon { glyph, .. } if *glyph == GLYPH_CHEVRON_RIGHT));
        assert!(!has_right);
    }

    #[test]
    fn nav_arrow_hidden_when_alpha_zero() {
        let cmds = fixture(|s| { s.can_prev = true; s.nav_alpha = [0.0, 0.0]; });
        let has_left = cmds.iter().any(|c| matches!(c, DrawCmd::Icon { glyph, .. } if *glyph == GLYPH_CHEVRON_LEFT));
        assert!(!has_left);
    }
```

- [ ] **Step 2: Запустить — убедиться, что не компилируется**

Run: `cargo test --bin lumina scene::tests`
Expected: ошибка компиляции — нет полей `can_prev`/`can_next`/`nav_alpha` и глифов.

- [ ] **Step 3: Реализовать**

В `src/ui/scene.rs`:

1) Добавить глифы шевронов (рядом с прочими `GLYPH_*` Tabler):
```rust
pub const GLYPH_CHEVRON_LEFT: char = '\u{EA60}';  // ti-chevron-left
pub const GLYPH_CHEVRON_RIGHT: char = '\u{EA61}'; // ti-chevron-right
```
> Если при ручной приёмке отрисуется не та иконка — сверить кодпоинты `ti-chevron-left`/`ti-chevron-right` в `assets/fonts/` (tabler-icons.css 3.44.0) и поправить.

2) В `struct UiState` добавить поля (после `scroll: f32,`):
```rust
    /// Прозрачность экранных стрелок [prev, next] (0..1): к 1 при hover, иначе к 0.
    pub nav_alpha: [f32; 2],
    /// Можно ли листать prev/next (нет первого/последнего — стрелка скрыта и инертна).
    pub can_prev: bool,
    pub can_next: bool,
```

3) В `UiState::new()` добавить инициализацию (после `scroll: 0.0,`):
```rust
            nav_alpha: [0.0, 0.0],
            can_prev: false,
            can_next: false,
```

4) В `build`, в ОКОННОЙ ветке (не fullscreen), перед финальным `cmds` (после блока `if state.bottom_factor > 0.0 { ... }`) добавить отрисовку стрелок:
```rust
    // --- Экранные стрелки навигации (поверх фото; проявляются на hover) ---
    let chev = theme::NAV_CHEVRON_SIZE * scale;
    for (i, (rect, can, glyph)) in [
        (layout.nav_prev, state.can_prev, GLYPH_CHEVRON_LEFT),
        (layout.nav_next, state.can_next, GLYPH_CHEVRON_RIGHT),
    ]
    .into_iter()
    .enumerate()
    {
        let a = state.nav_alpha[i].clamp(0.0, 1.0);
        if !can || a <= 0.01 {
            continue;
        }
        let mut bg = theme.overlay_bg;
        bg[3] *= a;
        let mut ic = theme.text_primary;
        ic[3] *= a;
        cmds.push(DrawCmd::Rect { rect, color: bg, radius: 8.0 * scale, layer: RectLayer::Bg });
        cmds.push(DrawCmd::Icon { rect, glyph, size: chev, color: ic, font: IconFont::Tabler });
    }
```

- [ ] **Step 4: Запустить тесты `scene`**

Run: `cargo test --bin lumina scene::`
Expected: PASS (3 новых + все прежние; глифы-шевроны не появляются в прежних тестах, т.к. `can_*`/`nav_alpha` по умолчанию выключены).

- [ ] **Step 5: Commit**

```bash
git add src/ui/scene.rs
git commit -m "feat(ui): отрисовка экранных стрелок (шевроны Tabler, hover-альфа, инертность на краях)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Интеграция стрелок в `app.rs` (hover-альфа, can_*, клики)

**Files:**
- Modify: `src/app.rs` (`load_current`, `RedrawRequested`, `MouseInput`)

- [ ] **Step 1: Обновлять `can_prev`/`can_next` при загрузке**

В `src/app.rs::load_current`, в блоке `if let Some(cat) = &self.state.catalog { ... }` (где задаются `thumb_count`/`active_index`), добавить:

```rust
            let idx = cat.current_index();
            self.state.ui.can_prev = idx > 0;
            self.state.ui.can_next = idx + 1 < n;
```
> `n` — уже объявленная в этом блоке длина `files().len()`.

- [ ] **Step 2: Анимировать `nav_alpha` в `RedrawRequested`**

В `src/app.rs`, ветка `WindowEvent::RedrawRequested`, после блока анимации `bottom_factor` (после строки `self.state.ui.bottom_factor = ...`) добавить:

```rust
                // экранные стрелки: альфа к 1 при hover полосы (если можно листать), иначе к 0
                let nav_targets = [
                    if self.state.ui.hovered == hit::Region::NavPrev && self.state.ui.can_prev { 1.0 } else { 0.0 },
                    if self.state.ui.hovered == hit::Region::NavNext && self.state.ui.can_next { 1.0 } else { 0.0 },
                ];
                let mut nav_animating = false;
                for i in 0..2 {
                    let a = self.state.ui.nav_alpha[i];
                    let t = nav_targets[i];
                    if (a - t).abs() > 0.001 {
                        self.state.ui.nav_alpha[i] = a + (t - a) * (dt / 0.12).min(1.0);
                        nav_animating = true;
                    } else {
                        self.state.ui.nav_alpha[i] = t;
                    }
                }
```

Затем в конце ветки `RedrawRequested` расширить условие повторной перерисовки — заменить:
```rust
                if self.state.view.is_animating() || animating || overlay_active {
                    if let Some(w) = &self.window { w.request_redraw(); }
                }
```
на:
```rust
                if self.state.view.is_animating() || animating || overlay_active || nav_animating {
                    if let Some(w) = &self.window { w.request_redraw(); }
                }
```

- [ ] **Step 3: Обрабатывать клики по стрелкам**

В `src/app.rs`, ветка `MouseInput` → `ElementState::Pressed`, в `match region { ... }` добавить (рядом с прочими регионами, до `_ => {}`):

```rust
                                    hit::Region::NavPrev => {
                                        if self.state.ui.can_prev { self.navigate(-1); }
                                        return;
                                    }
                                    hit::Region::NavNext => {
                                        if self.state.ui.can_next { self.navigate(1); }
                                        return;
                                    }
```

- [ ] **Step 4: Сборка и тесты**

Run: `cargo build`
Expected: компилируется без новых warnings.

Run: `cargo test --bin lumina`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): экранные стрелки — hover-альфа, обновление can_prev/next, клики листают" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Интеграция свайпа в `app.rs` (press/move/release + откат)

**Files:**
- Modify: `src/app.rs` (`AppState` поля + `new`, `MouseInput`, `CursorMoved`, `RedrawRequested`)

- [ ] **Step 1: Добавить поля состояния свайпа**

В `src/app.rs`:
- В `struct AppState` добавить (после `cursor_idle: f32,`):
```rust
    pub swiping: bool,
    pub swipe_press_x: f32,
    pub swipe_anim: bool,
```
- В `AppState::new()` добавить (после `cursor_idle: 0.0,`):
```rust
            swiping: false,
            swipe_press_x: 0.0,
            swipe_anim: false,
```

- [ ] **Step 2: Старт свайпа vs pan в `Pressed`**

В `src/app.rs`, ветка `MouseInput` → `ElementState::Pressed`, в `else`-ветке (не двойной клик) заменить блок:
```rust
                            } else {
                                self.state.last_click = Some((now, self.state.cursor));
                                // pan только когда фото увеличено зумом сверх fit
                                let can_pan = self.state.view.zoom() > self.state.view.min_zoom();
                                self.state.dragging = can_pan;
                                if can_pan {
                                    if let Some(w) = &self.window {
                                        w.set_cursor(winit::window::Cursor::Icon(
                                            winit::window::CursorIcon::Grabbing,
                                        ));
                                    }
                                }
                            }
```
на:
```rust
                            } else {
                                self.state.last_click = Some((now, self.state.cursor));
                                // pan только когда фото увеличено зумом сверх fit; иначе — свайп-перелистывание
                                let can_pan = self.state.view.zoom() > self.state.view.min_zoom();
                                if can_pan {
                                    self.state.dragging = true;
                                    if let Some(w) = &self.window {
                                        w.set_cursor(winit::window::Cursor::Icon(
                                            winit::window::CursorIcon::Grabbing,
                                        ));
                                    }
                                } else {
                                    // размер по умолчанию (fit) → начинаем свайп
                                    self.state.swiping = true;
                                    self.state.swipe_press_x = self.state.cursor.x;
                                    self.state.swipe_anim = false;
                                    self.state.view.set_swipe_offset(0.0);
                                }
                            }
```

- [ ] **Step 3: Смещение фото при движении в `CursorMoved`**

В `src/app.rs`, ветка `WindowEvent::CursorMoved`, заменить блок:
```rust
                if self.state.dragging {
                    let delta = pos - self.state.last_cursor;
                    self.state.view.set_pan(self.state.view.pan() + delta);
                    if let Some(r) = &self.renderer {
                        if let Some(img) = r.image_size() {
                            self.state.view.clamp_pan(r.viewer_size(), img);
                        }
                    }
                    if let Some(w) = &self.window { w.request_redraw(); }
                }
```
на:
```rust
                if self.state.dragging {
                    let delta = pos - self.state.last_cursor;
                    self.state.view.set_pan(self.state.view.pan() + delta);
                    if let Some(r) = &self.renderer {
                        if let Some(img) = r.image_size() {
                            self.state.view.clamp_pan(r.viewer_size(), img);
                        }
                    }
                    if let Some(w) = &self.window { w.request_redraw(); }
                } else if self.state.swiping {
                    let dx = pos.x - self.state.swipe_press_x;
                    self.state.view.set_swipe_offset(dx);
                    if let Some(w) = &self.window { w.request_redraw(); }
                }
```

- [ ] **Step 4: Решение свайпа в `Released`**

В `src/app.rs`, ветка `MouseInput` → `ElementState::Released`, заменить блок:
```rust
                        ElementState::Released => {
                            self.state.dragging = false;
                            if let Some(w) = &self.window {
                                w.set_cursor(winit::window::Cursor::Icon(
                                    winit::window::CursorIcon::Default,
                                ));
                            }
                        }
```
на:
```rust
                        ElementState::Released => {
                            self.state.dragging = false;
                            if let Some(w) = &self.window {
                                w.set_cursor(winit::window::Cursor::Icon(
                                    winit::window::CursorIcon::Default,
                                ));
                            }
                            if self.state.swiping {
                                self.state.swiping = false;
                                let dx = self.state.view.swipe_offset();
                                let vw = self.renderer.as_ref().map(|r| r.viewer_size().x).unwrap_or(0.0);
                                match crate::input::on_swipe_release(dx, vw) {
                                    Some(crate::input::NavDir::Next) if self.state.ui.can_next => {
                                        self.state.view.set_swipe_offset(0.0);
                                        self.navigate(1);
                                    }
                                    Some(crate::input::NavDir::Prev) if self.state.ui.can_prev => {
                                        self.state.view.set_swipe_offset(0.0);
                                        self.navigate(-1);
                                    }
                                    _ => {
                                        // не дотянули / край каталога → плавный откат
                                        self.state.swipe_anim = true;
                                        if let Some(w) = &self.window { w.request_redraw(); }
                                    }
                                }
                            }
                        }
```

- [ ] **Step 5: Откат-анимация в `RedrawRequested`**

В `src/app.rs`, ветка `WindowEvent::RedrawRequested`, после блока анимации `nav_alpha` (из Task 8) добавить:

```rust
                // свайп: плавный откат смещения к 0, если перелистывания не случилось
                if self.state.swipe_anim {
                    let cur = self.state.view.swipe_offset();
                    let next = cur + (0.0 - cur) * (dt / 0.2).min(1.0);
                    if next.abs() < 0.5 {
                        self.state.view.set_swipe_offset(0.0);
                        self.state.swipe_anim = false;
                    } else {
                        self.state.view.set_swipe_offset(next);
                    }
                }
```

Затем расширить условие повторной перерисовки в конце ветки — заменить:
```rust
                if self.state.view.is_animating() || animating || overlay_active || nav_animating {
                    if let Some(w) = &self.window { w.request_redraw(); }
                }
```
на:
```rust
                if self.state.view.is_animating() || animating || overlay_active || nav_animating || self.state.swipe_anim {
                    if let Some(w) = &self.window { w.request_redraw(); }
                }
```

- [ ] **Step 6: Сборка и полный прогон**

Run: `cargo build`
Expected: компилируется без новых warnings (`swiping`/`swipe_press_x`/`swipe_anim` все задействованы).

Run: `cargo test --bin lumina`
Expected: PASS — все юнит-тесты + `#[ignore]` RAW/HEIC.

- [ ] **Step 7: Commit**

```bash
git add src/app.rs
git commit -m "feat(app): свайп-перелистывание при fit (следование за курсором, порог, откат)" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: Ручная приёмка и обновление ROADMAP

- [ ] **Step 1: Ручная приёмка на реальных файлах**

Run: `cargo run -- "путь\к\папке с фото\фото.jpg"`
(Для запуска добавить в PATH `~/vcpkg/installed/x64-windows/bin` ради heif.dll/libde265.dll.)

Проверить:
- **#1 Раскладки:** переключить раскладку на русскую (РУС) — `К`/`Р`/`М` поворачивают/отражают (R/H/V), `Ctrl+Я` сбрасывает (Ctrl+Z), `Ф` входит/выходит из fullscreen (F), `Ctrl+0`/`Ctrl+1` — fit/100%. На английской раскладке те же клавиши работают как прежде.
- **#3 Стрелки:** в оконном режиме навести курсор к левому/правому краю — проявляется тонкая полупрозрачная стрелка ‹/›; клик листает фото; на первом фото левая стрелка не появляется, на последнем — правая; в fullscreen стрелок нет.
- **#4 Свайп:** при размере по умолчанию (fit) — перетаскивание мышью двигает фото за курсором; отпускание за порогом (≈15% ширины) листает (влево → следующее, вправо → предыдущее); недотянутое — плавно откатывается; на краю каталога — откат. При зуме (после double-click/Ctrl+1) перетаскивание по-прежнему панорамирует (не листает).

- [ ] **Step 2: Обновить ROADMAP**

В `ROADMAP.md` в таблице стадий добавить строку v0.4c (🟢) со ссылками на дизайн/план, и раздел «Прогресс v0.4c (детально)» по образцу предыдущих фаз (перечень сделанного + число тестов). Отметить, что #2 (запись EXIF Orientation в оригиналы) остаётся в v0.4b.

- [ ] **Step 3: Commit**

```bash
git add ROADMAP.md
git commit -m "docs(roadmap): v0.4c (навигация и эргономика ввода) готова" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-review (выполнен при написании)

- **Покрытие спека:** #1 (раскладки) → Task 1 (map_key) + Task 2 (диспетчинг); #3 (стрелки) → Task 5 (layout) + Task 6 (hit) + Task 7 (scene/UiState) + Task 8 (app: альфа/can_*/клики); #4 (свайп) → Task 3 (on_swipe_release) + Task 4 (view swipe_offset) + Task 9 (app: press/move/release/откат); ROADMAP → Task 10. #2 явно вне scope (v0.4b).
- **Плейсхолдеры:** нет — весь код приведён. Кодпоинты шевронов даны (EA60/EA61) с примечанием сверить при визуальной приёмке.
- **Согласованность типов:** `Action`/`map_key` (Task 1) = вызовы в `apply_action` (Task 2); `NavDir`/`on_swipe_release` (Task 3) = вызов в Released (Task 9); `swipe_offset()`/`set_swipe_offset()` (Task 4) едины в Task 9 и view-тесте; поля `UiState` `nav_alpha`/`can_prev`/`can_next` (Task 7) = чтение/запись в Task 8; поля `UiLayout` `nav_prev`/`nav_next` (Task 5) = `hit` (Task 6) и `scene` (Task 7); `Region::NavPrev`/`NavNext` (Task 6) = `scene`/`app` (Task 7/8). `hit::Region` уже импортирован в `app.rs` (`use crate::ui::{hit, layout};`).
- **Единственное хранилище swipe_offset** — `view.rs`; `app.rs` управляет через сеттер/геттер (нет дублирования состояния).
